# PROGRESS.md — Student Success Intelligence Platform (SSIP)

## Current State (as of 2026-05-06)

**Stack:** Python 3.11 · FastAPI · SQLAlchemy async (PostgreSQL) · pyodbc (SQL Server read-only) · APScheduler · Anthropic Claude API · Docker Compose  
**Execution mode:** `SHADOW` (no real outbound communication until `.env` credentials are filled and mode is flipped to `LIVE`)  
**Note on CLAUDE.md folder refs:** CLAUDE.md was written for a Node.js/Express/React stack. The actual layer mapping is `/app/` → backend, `/frontend/index.html` → frontend, `/app/services/` → business logic. Governance rules, escalation model, and PROGRESS.md gate all apply as written.

---

## Phase 1 — MVP Core Platform

- [x] Initial project scaffold
  - Date: 2026-05-05
  - What changed: repo created, FastAPI skeleton, requirements.txt, pytest.ini, seed_data.py
  - Verification: commit a6f58cb

- [x] MVP backend — core services and routers
  - Date: 2026-05-05
  - What changed: Full FastAPI app with routers (health, outreach, students, metrics, actions, webhook, sync, dashboard), SQLAlchemy models (StudentTriggerData, StudentOutreachTracking, OutreachHistory, StateTransitionLog, ProcessedEvents), state machine, eligibility engine, decision engine, channel selector, scheduler (APScheduler daily batch), outreach orchestration, all integration stubs (GHL, Synthflow, SMS, email, LLM)
  - Verification: commit e334fd8; `tsc --noEmit` N/A (Python); app boots in Docker

- [x] Port moved to 8080
  - Date: 2026-05-05
  - What changed: SSIP service bound to port 8080 in Docker Compose
  - Verification: commit a76ed07

- [x] MVP dashboard fixes
  - Date: 2026-05-05
  - What changed: frontend/index.html corrections post-MVP
  - Verification: commit f23f97a

- [x] Testing and UX documentation
  - Date: 2026-05-05
  - What changed: tests/test_scenarios.md, tests/edge_cases.md, tests/load_tests.md, tests/test_dashboard.py, tests/test_actions.py, tests/test_shadow_safety.py added
  - Verification: commit 19320e4; test files confirmed present

---

## Phase 2 — Production System with Shadow Write

- [x] Production-grade backend rewrite
  - Date: 2026-05-05
  - What changed: 32 files touched — config.py (mssql_dsn builder, is_shadow property), database.py (SQL Server pyodbc sync reader), models.py (5 ORM models finalized), all routers hardened, services split into integrations/ subpackage (ghl.py, synthflow.py, sms.py, email.py, llm.py), sync.py (SQL Server → PostgreSQL upsert), outreach.py rewritten for shadow-safe execution, state_machine.py tightened, docker-compose.yml added, .env.example expanded
  - Verification: commit 0183797; SHADOW mode validated — all outbound calls are no-ops when `EXECUTION_MODE=SHADOW`

- [x] Shadow safety test suite
  - Date: 2026-05-05
  - What changed: tests/test_shadow_safety.py — 6 tests covering GHL, Synthflow, SMS, email no-HTTP in shadow mode; OutreachHistory shadow field invariants; CLOSED state transition lock
  - Verification: commit 19320e4 / 0183797; tests cover shadow gate for all 4 channels

---

## Phase 3 — Dashboard UI

- [x] Full ops dashboard (8-section rewrite)
  - Date: 2026-05-06
  - What changed: frontend/index.html rewritten — System Health panel, Operational Alerts, Funnel KPIs + conversion pills, State Distribution + Channel Performance table, Student Lookup (profile, risk badge, dual timelines), Outreach trigger, SQL Server sync control, Manual Actions (Close/Retry/Resolve/Escalate with SHADOW badge), Recent Activity table; auto-refreshes every 60 seconds
  - Verification: commit c1a41fb

- [x] Collapsible cards
  - Date: 2026-05-06
  - What changed: dashboard cards made collapsible
  - Verification: commit 089c1d5

- [x] Home page summary card rows
  - Date: 2026-05-06
  - What changed: Three labeled rows of summary cards (System Status, Pipeline, Actions) with title, status badge, short summary, key metric, and "View details →" footer; hover highlights border in blue
  - Verification: commit 0ddd455

- [x] CLAUDE.md and documentation update
  - Date: 2026-05-06
  - What changed: CLAUDE.md governance contract updated; supporting docs refreshed
  - Verification: commit 22802e3

---

---

## Phase 4 — Production-Grade Student Success Operations Platform

- [x] Expand StudentTriggerData model — 10 new optional fields
  - Date: 2026-05-15
  - What changed: app/models.py — added AttendancePercentage, CurrentSection, IPBCStartDate, Past10DaysLogon, Total_Payments, Total_Credits, PaymentBalance, ClassValue, FeePaid, ClassFeesPaid to StudentTriggerData; added 4 new models: StudentInterviewPrep (JSONB stub), StudentNote, AIInsight, GHLMessage
  - Verification: TypeScript N/A (Python); column migration runs automatically at startup via ALTER TABLE...ADD COLUMN IF NOT EXISTS in init_db()

- [x] Expand SQL Server sync to include new fields + InterviewPrep table
  - Date: 2026-05-15
  - What changed: app/database.py — expanded SELECT to 19 columns; added _fetch_interview_prep_sync() (SELECT *); app/services/sync.py — added _coerce_row() for date→datetime conversion, _serialize_for_jsonb() for JSONB safe types, sync_interview_prep() service; app/routers/sync.py — added POST /sync/interview-prep endpoint
  - Verification: _validate_row() unchanged (only required NOT NULL fields); new columns all Optional; shadow mode unaffected

- [x] Segmentation service + router
  - Date: 2026-05-15
  - What changed: app/services/segmentation.py — NEWCOMERS (≤90 days), HYPER_ACTIVE (logins≥7 AND eff>90), CAP_HOPEFULS (att>30 AND IPBC), LAUNCH_HOPEFULS (att>55 AND CAP), PLACEMENT_HOPEFULS (att>70 AND Launch); filter_by_role() for frontend role filtering; app/routers/segments.py — GET /segments/summary and GET /segments/{segment_name}
  - Verification: classify_student() pure function; no I/O

- [x] GHL message sync service + router (read-only, shadow-safe)
  - Date: 2026-05-15
  - What changed: app/services/ghl_sync.py — phone-based lookup (E.164 normalize → GET /v1/contacts/?phone= → conversation search → message fetch); sync_by_phone(), sync_student_by_user_id(), sync_all_students(); idempotent on ghl_message_id; all calls logged with duration_ms/status/outcome; app/routers/ghl_sync.py — GET /ghl-sync/status, POST /ghl-sync/student (user_id only, phone auto-loaded), POST /ghl-sync/batch (batch all); frontend — "Sync All GHL Messages" button on Overview card; per-student "Sync GHL" button in drawer Outreach tab with live result display
  - Notes: GHL reads are always allowed — shadow mode gates WRITES only; reads are non-destructive; contact_id is never required — phone number lookup is the only supported path

- [x] AI insight generation service + router
  - Date: 2026-05-15
  - What changed: app/services/ai_insights.py — OUTREACH_DRAFT, INTERVENTION, RISK_EXPLANATION, INTERVIEW_COACHING; 24-hour PostgreSQL cache; OpenAI GPT-4o; app/routers/ai_insights.py — GET /ai-insights/{user_id}/{insight_type}, GET /ai-insights/{user_id}
  - Verification: degrades gracefully if LLM_API_KEY not set; shadow mode unaffected (no outbound comm)

- [x] Unified timeline service + router
  - Date: 2026-05-15
  - What changed: app/services/timeline.py — merges OutreachHistory, StateTransitionLog, StudentNote, AIInsight, GHLMessage into chronological event stream; app/routers/student_timeline.py — GET /timeline/{user_id}
  - Verification: pure aggregation; no writes; no shadow gate needed

- [x] Payment reconciliation router with Bundle Deal fix
  - Date: 2026-05-15
  - What changed: app/routers/payment.py — GET /payment/reconciliation, GET /payment/student/{user_id}; bundle detection: Total_Credits>0 AND PaymentBalance==0 AND ClassValue>0 → actual_balance = ClassValue−Total_Payments−Total_Credits; payment_risk field: HIGH/MEDIUM/CLEAR
  - Verification: actual_balance field surfaced alongside stored PaymentBalance; is_bundle_deal flag makes fix auditable

- [x] Notes router
  - Date: 2026-05-15
  - What changed: app/routers/notes.py — POST /notes, GET /notes/{user_id}; StudentNote model; author attribution; AI-generated flag
  - Verification: content/author validated; shadow mode unaffected

- [x] Expanded students router — drawer endpoint + interview prep
  - Date: 2026-05-15
  - What changed: app/routers/students.py — GET /students/{user_id}/drawer (combined profile+payment+segments+outreach+interview_prep), GET /students/{user_id}/interview-prep
  - Verification: bundle-aware balance computed in drawer; segment classification included

- [x] Extended KPI dashboard endpoint
  - Date: 2026-05-15
  - What changed: app/routers/dashboard.py — GET /dashboard/kpi-extended returns avg attendance, efficiency, hw_behind, engagement counts, payment risk, segment counts, section distribution
  - Verification: gracefully returns note when no students synced

- [x] Config expanded with GHL field vars
  - Date: 2026-05-15
  - What changed: app/config.py — GHL_TIMEOUT_SECONDS, GHL_RETRY_MAX, GHL_FIELD_MESSAGE, GHL_TASK_TITLE, GHL_TASK_DESCRIPTION, GHL_TASK_DUE_DATE, GHL_FIELD_VM_EMAIL_HTML, GHL_FIELD_VM_EMAIL_SUBJECT
  - Verification: all have safe defaults; no breaking change to existing .env

- [x] Update main.py — register 6 new routers
  - Date: 2026-05-15
  - What changed: app/main.py — registered segments, payment, student_timeline, ai_insights, notes, ghl_sync routers; version bumped to 2.0.0
  - Verification: all imports verified; startup runs init_db() with column migrations

- [x] Frontend complete rewrite — multi-dashboard SPA
  - Date: 2026-05-15
  - What changed: frontend/index.html — 11 tab navigation (Overview, KPI, Newcomers, Engagement, HW Risk, CAP Hopefuls, Launch Hopefuls, Placement Hopefuls, Payment, Outreach Activity, AI Intervention); Bootstrap 5 Offcanvas right drawer with 10 tabs (Info, Mentor, Instructor, Super Mentor, Timeline, Outreach, AI Insights, Interview Prep, Payment, Notes); Chart.js 4 charts (segments doughnut, HW risk bar, sections bar, outreach funnel); generic DataTable engine with search/sort/paginate/CSV export; role picker in navbar persisted to sessionStorage; auto-refresh every 10 min (600000ms); bundle deal payment risk visualization; conditional row formatting
  - Verification: all API endpoints wired; SHADOW mode badge preserved; GHL read-only indicator on sync card; role picker wired to API role params; CSV export works on filtered rows

---

## Pending / Blockers

- [x] SQL Server credentials set and deployment running in SHADOW mode
  - Date: 2026-05-06
  - What changed: MSSQL_USER + MSSQL_PASS filled in .env; Dockerfile fixed (apt-key → gpg --dearmor, msodbcsql17 → msodbcsql18, python:3.11 → python:3.12 for CVE fix); config.py DSN updated to ODBC Driver 18 + TrustServerCertificate=yes; containers running at localhost:8080
  - Verification: `docker compose logs api` shows all dashboard endpoints returning 200 OK; mode=SHADOW confirmed in startup log

- [x] Switch LLM integration from Anthropic to OpenAI GPT-4o
  - Date: 2026-05-06
  - What changed: app/services/integrations/llm.py rewritten to use `openai.AsyncOpenAI` + `chat.completions.create`; requirements.txt swapped `anthropic>=0.34.0` → `openai>=1.0.0`; config.py default model updated to `gpt-4o`; .env.example updated
  - Verification: pytest tests/ → 19 passed, 0 failed

- [x] Fix sync error messaging — show real SQL Server error instead of generic "not configured"
  - Date: 2026-05-06
  - What changed: database.py _fetch_students_sync now returns (rows, error) tuple; sync.py propagates error field in response; frontend displays actual error string (e.g. permission denied) instead of hardcoded "SQL Server not configured"
  - Verification: container rebuilt and running; error message now reflects actual SQL Server exception

- [x] Bootstrap 5 enterprise UX redesign
  - Date: 2026-05-06
  - What changed: frontend/index.html rewritten from custom dark theme to Bootstrap 5 CDN + navy/white enterprise design matching CLAUDE.md spec — CSS custom properties, card border-0 shadow-sm pattern, table-hover, Bootstrap modal, WCAG 2.1 AA focus indicators, reduced-motion and high-contrast media queries; all API calls and interactions preserved
  - Verification: docker compose up -d --build api succeeded; container running at localhost:8080

- [ ] GHL, Synthflow, LLM (OpenAI) API keys — fill in `.env`
  - Blocker: all outbound integrations are stubbed in shadow mode; real execution requires credentials and `EXECUTION_MODE=LIVE` (strategic decision — requires explicit approval before flip)

- [x] Pytest suite — all 19 tests green
  - Date: 2026-05-06
  - What changed: tests/test_shadow_safety.py had 4 tests passing wrong flat payloads to integration functions that expect builder-constructed nested payloads; fixed tests to use `build_ghl_payload`, `build_call_payload`, `build_sms_payload`, `build_email_payload` helpers
  - Verification: `pytest tests/ -v` → 19 passed, 0 failed (test_shadow_safety.py 6/6, test_actions.py 8/8, test_dashboard.py 5/5)

- [x] SQL Server sync partial-sync tolerance + structured error surfacing
  - Date: 2026-05-12
  - What changed: app/services/sync.py — added _validate_row() guard on UserID/HWsBehind/AvgEffRating; invalid rows are skipped with reason logged and returned in failures[]; valid rows always commit; db.commit() wrapped in try/except so a late DB-level failure returns structured error instead of 500; structured response adds status/rows_scanned/rows_successful/rows_failed/failures fields; frontend/index.html — _renderSyncResult() helper, syncMssqlInline() and syncMssql() rewritten to show scanned/inserted/updated/skipped counts, per-failure UserID+reason table, WARNING badge on partial_success; tests/test_sync.py — 12 new tests (5 unit _validate_row, 7 integration sync_from_mssql) all passing
  - Verification: pytest tests/ → 45 passed, 0 failed (was 33)
  - Notes: model NOT NULL constraints preserved; no silently-defaulted NULL values; shadow mode unaffected; data_quality_issues table deferred as follow-up

- [x] Expand All cards feature
  - Date: 2026-05-06
  - What changed: frontend/index.html — each card gains a ▼ chevron toggle button (stops propagation, doesn't open modal); inline Bootstrap collapse panel renders compact live data below card body; "⊞ Expand All / ⊟ Collapse All" button added to navbar; auto-refreshes open panels every 60 s; static panels (Student Cases, Manual Actions, Trigger, Sync) render interactive forms inline; async panels (Health, Alerts, KPI, States, Channels, Activity) fetch and render on expand
  - Verification: user confirmed

- [x] PROGRESS.md created
  - Date: 2026-05-06
  - What changed: this file created to satisfy CLAUDE.md hard gate; documents full project state through commit 22802e3

## Phase 5 — Lifecycle Tab Reformat (6 tabs, per-spec column schemas)

- [x] Expand StudentTriggerData — 10 additional SQL Server source columns
  - Date: 2026-05-15
  - What changed: app/models.py — added ClassName, ClassSignupsID, ActiveStatus, StatusI, StatusII, StudentStartDate, ClassStartDate, LastActivitySection, LastLoginDays, LastSubmitted to StudentTriggerData; added StudentCampaignActivity and StudentQuickActionLog models; expanded StudentNote with note_type and visibility fields
  - Verification: migration-lite in init_db() adds all new columns via ALTER TABLE IF NOT EXISTS on container restart — safe to run repeatedly

- [x] Migration-lite for Phase 5 columns
  - Date: 2026-05-15
  - What changed: app/database.py — _NEW_TRIGGER_COLS extended with 10 new columns; student_notes ALTER TABLE adds note_type and visibility

- [x] Segmentation service — fix section field and thresholds
  - Date: 2026-05-15
  - What changed: app/services/segmentation.py — replaced CurrentSection references with LastActivitySection (via _section() helper falling back to PathName); CAP_HOPEFULS threshold corrected to attendance > 50 (was > 30); LAUNCH_HOPEFULS filter changed to 'CAP Project' in section (was 'CAP'); rules dict updated to match spec
  - Notes: CurrentSection was always NULL because it does not exist in SQL Server — LastActivitySection is the correct source column

- [x] Sync coerce — generalize date column handling
  - Date: 2026-05-15
  - What changed: app/services/sync.py — _coerce_row() now handles StudentStartDate and ClassStartDate date→datetime conversion alongside IPBCStartDate; LastSubmitted coerced to ISO string for VARCHAR storage

- [x] Lifecycle router — 6 dedicated tab endpoints
  - Date: 2026-05-15
  - What changed: app/routers/lifecycle.py (NEW) — GET /lifecycle/newcomers (IPBCStartDate within 90d), /engagement (all), /hw-risk (IPBCStartDate not null), /cap-hopefuls (attendance>50), /launch-hopefuls (attendance>70 AND 'CAP Project' in section), /placement-hopefuls (attendance>70 AND 'Launch' in section); each endpoint merges latest campaign activity and latest note per student; _compute_common() derives row_id, student_name, weeks_in_program, last_hw_submitted_days, active_student; all null-safe with graceful fallbacks
  - Verification: SHADOW mode unaffected (read-only endpoints)

- [x] Quick actions router — operator button log + campaign activity query
  - Date: 2026-05-15
  - What changed: app/routers/quick_actions.py (NEW) — POST /quick-actions/log (logs StudentQuickActionLog + companion StudentCampaignActivity); GET /campaign-activity/{user_id}; GET /quick-actions/{user_id}; execution_mode stamped on every record; no external communication triggered
  - Verification: SHADOW mode enforced — shadow_only=True on all campaign activity records when in shadow mode

- [x] Register new routers in app/main.py
  - Date: 2026-05-15
  - What changed: lifecycle_router and quick_actions_router registered with Lifecycle and QuickActions tags

- [x] Frontend — 6 lifecycle tabs with per-spec column schemas and action bars
  - Date: 2026-05-15
  - What changed: frontend/index.html — CSS added for .dt-row-selected and .action-bar-wrap; 6 tab pane HTML replaced with structured layout (section label + action bar + table container); buildTable() updated with onSelect callback and selectedId row highlighting via .dt-row-selected class; TAB_ACTIONS and handleAction() implement button click → POST /quick-actions/log flow; 6 dedicated loaders: loadNewcomersTab() (17 cols), loadEngagementTab() (18 cols), loadHwRiskTab() (20 cols), loadCapTab() (20 cols), loadLaunchTab() (20 cols), loadPlacementTab() (28 cols); loadTab() updated to call per-tab loaders; action buttons disabled until row selected; SHADOW badge visible on each action bar
  - Verification: column order matches spec exactly for each tab; campaign/notes columns present but blank until activity logged; Launch and Placement Hopefuls show empty table with descriptive note when filter returns no rows
  - Verification: user confirmed

---

## Phase 6 — Lifecycle Tab Data Fix (Newcomers / HW Risk / CAP Hopefuls empty)

- [x] Diagnose empty lifecycle tabs
  - Date: 2026-05-15
  - What changed: investigation only — no code changed during diagnosis
  - Verification: confirmed `IPBCStartDate` is NULL for all 76 SQL Server students; `AttendancePercentage` was NULL in PostgreSQL despite being populated in SQL Server (scale mismatch + stale sync); 39/76 students skipped by sync validation (HWsBehind/AvgEffRating NULL); PostgreSQL had 10 seed rows + 80 partially-synced real rows

- [x] Fix sync validation — allow students without homework data
  - Date: 2026-05-15
  - What changed: app/services/sync.py — `_REQUIRED_FIELDS` narrowed to `("UserID",)` only; `_coerce_row()` now defaults `HWsBehind → 0` and `AvgEffRating → 0.0` when NULL (students before first HW submission); `AttendancePercentage` normalized from SQL Server's 0–1 fraction to 0–100 scale (×100); `ClassSignupsID` cast to `str` (SQL Server int → PostgreSQL VARCHAR); `Total_Credits`/`ClassValue`/`PaymentBalance`/`ClassFeesPaid` explicitly cast to `float`
  - Verification: sync ran 76/76 scanned, 76 successful, 0 failures; `AttendancePercentage` now 92.3 (was NULL) for sample student

- [x] Fix lifecycle tab filters — remove IPBC gate
  - Date: 2026-05-15
  - What changed: app/routers/lifecycle.py — **Newcomers**: filter changed to `COALESCE(IPBCStartDate, StudentStartDate) within 90 days` via SQLAlchemy `or_/and_`; **HW Risk**: removed `IPBCStartDate IS NOT NULL` gate entirely (shows all 94 students); **CAP Hopefuls**: removed `IPBCStartDate IS NOT NULL`, kept `AttendancePercentage > 50` (now 59 students qualify); `_compute_common()` falls back to `StudentStartDate` for `weeks_in_program` when `IPBCStartDate` is NULL; added `and_` / `or_` to imports
  - Verification: Newcomers=17, HW Risk=94, CAP Hopefuls=59 rows returned after fix

- [x] Fix segmentation service — mirror same logic
  - Date: 2026-05-15
  - What changed: app/services/segmentation.py — NEWCOMERS segment uses `IPBCStartDate or StudentStartDate` for date check; CAP_HOPEFULS removes `has_ipbc_start` gate, classifies on attendance alone; SEGMENT_RULES strings updated to document new logic
  - Verification: consistent with lifecycle endpoint behaviour

- [x] Fix action buttons — row click handler unreachable due to hyphen in containerId
  - Date: 2026-05-15
  - What changed: frontend/index.html — `buildTable()` generated four inline onclick strings using bare identifiers (`__dtRowClick_dt-newcomers(...)`) which JavaScript parses as subtraction; fixed all four generators (row click, prev pager, next pager, sort header) to use `window['__dtXxx_${containerId}'](...)` bracket notation so the hyphenated key is reachable
  - Verification: `onSelect` callback now fires on row click → action buttons un-disable; pager Prev/Next and column sort also unblocked by same fix

- [x] Make ODBC driver configurable — MSSQL_DRIVER env var
  - Date: 2026-05-15
  - What changed: app/config.py — added `MSSQL_DRIVER: str = "ODBC Driver 17 for SQL Server"` setting; `mssql_dsn` property uses `{self.MSSQL_DRIVER}` instead of hardcoded v18; production can override with `MSSQL_DRIVER=ODBC Driver 18 for SQL Server` in `.env`
  - Verification: sync succeeded using Driver 17 locally

---

## Documentation Update — Phase 5 Sync

- [x] Spec, UX, and implementation plan docs updated to reflect Phases 4 & 5
  - Date: 2026-05-15
  - What changed: spec/08_data_model.md — complete rewrite: dual-DB architecture (SQL Server read-only + PostgreSQL app DB), full student_trigger_data column list, new tables (student_campaign_activity, student_quick_action_log), updated student_notes schema, migration-lite strategy documented; spec/07_api_contracts.md — added sections 3.7–3.14 covering sync, lifecycle tabs, quick actions, GHL sync, timeline, notes, AI insights, segments endpoints; ux/admin_dashboard.md — new Section 15 documenting 6 lifecycle tabs, action bar behavior, per-tab column schemas, empty-state messaging; ux/operator_workflows.md — new Section 5 covering all lifecycle tab workflows (navigation, row selection, action button flow, data sync, drawer usage); execution/implementation_plan.md — Phase overview updated to show actual delivered phases alongside original plan
  - Verification: user requested; all docs reflect current codebase state

---

---

## Phase 7 — Architecture Hardening (Pre-Warehouse)

- [x] Add Alembic migration support
  - Date: 2026-05-24
  - What changed: `requirements.txt` — added `alembic>=1.13.0`; `alembic.ini` created with placeholder URL overridden at runtime; `alembic/env.py` — async-compatible env using `create_async_engine` + `NullPool`, offline mode strips `+asyncpg` prefix for SQL script generation; `alembic/script.py.mako` — standard template; `alembic/versions/0001_baseline.py` — full baseline migration for all 11 tables and all indexes as-at Phase 5
  - Verification: alembic.ini and env.py created; migration file covers all ORM models and indexes
  - Notes: Existing deployments must run `alembic stamp head` before running `alembic upgrade` to skip DDL already applied by init_db(). `init_db()` kept intact for now — future schema changes use Alembic only.

- [x] Add warehouse-preparation indexes
  - Date: 2026-05-24
  - What changed: `app/models.py` — added `ix_stl_created_at` to `StateTransitionLog` (was missing), `ix_sot_updated_at` to `StudentOutreachTracking`, `ix_oh_checkpoint` to `OutreachHistory`, `ix_sca_created_at` to `StudentCampaignActivity`; `app/database.py` — added four `CREATE INDEX IF NOT EXISTS` statements in `init_db()` to apply indexes to existing deployments on next restart
  - Verification: indexes defined in model `__table_args__`; migration-lite guards in `init_db()` are idempotent

- [x] Add `risk_level_for_display()` to priority service — consolidate risk scoring
  - Date: 2026-05-24
  - What changed: `app/services/priority.py` — added `risk_level_for_display(profile)`: accepts any object with priority fields (duck-typed, no ORM import), calls `score_student()`, maps URGENT→HIGH, returns HIGH/MEDIUM/LOW/UNKNOWN
  - Verification: removed duplicate `_risk_level()` from `app/routers/students.py`; all three callers (students, work_queue, segments) now use canonical score via priority service

- [x] Extract payment logic to `app/services/payment.py`
  - Date: 2026-05-24
  - What changed: `app/services/payment.py` (NEW) — `compute_balance(student_dict)` (bundle-fix), `payment_risk_label(balance)` (HIGH/MEDIUM/CLEAR), `build_payment_row(orm_row)` (full reconciliation row); `app/routers/payment.py` — removed `_compute_balance()` and `_payment_row()`, delegates to service; `app/routers/students.py` — drawer endpoint replaced inline bundle calculation with `compute_balance()` + `payment_risk_label()`
  - Verification: API response shape unchanged; all payment fields still present

- [x] Extract alert logic to `app/services/alerts.py`
  - Date: 2026-05-24
  - What changed: `app/services/alerts.py` (NEW) — `gather_alerts(db, *, is_shadow, mssql_configured, last_run)` — all five alert categories (CRITICAL: no MSSQL, WARNING: stuck CONTACTED, missed retry, scheduler stale, INFO: shadow mode); `app/routers/dashboard.py` — `dashboard_alerts()` replaced 50-line inline block with single `gather_alerts()` call; removed now-unused `timedelta` and `StudentOutreachTracking` imports from router
  - Verification: API response shape unchanged; all alert severities and messages preserved

- [x] Extract date utilities to `app/utils/date_utils.py`
  - Date: 2026-05-24
  - What changed: `app/utils/__init__.py` (NEW); `app/utils/date_utils.py` (NEW) — `to_isostr`, `weeks_in_program`, `hw_submitted_days`, `active_student_flag`, `serialize_datetime_fields` extracted from `lifecycle.py` private helpers; `app/routers/lifecycle.py` — removed 5 private `_*` functions, imports from `app.utils.date_utils`; `typing.Any` import removed (no longer needed)
  - Verification: all lifecycle tab endpoints retain identical behavior; function logic unchanged during extraction

---

## Phase 8 — Architecture Harmonization & Pre-Warehouse Specification Finalization

- [x] Architecture harmonization analysis — spec consistency review across 5 impact areas
  - Date: 2026-05-25
  - What changed: design-level analysis only; no code or schema changes; produced 5 new required abstraction layers, 6 unresolved design decisions (D-1 through D-6), 8 specification sections requiring revision, dependency sequencing, and readiness assessment
  - Verification: analysis documented in session context; spec not yet updated (analysis phase only)

- [x] Finalize architecture-blocking design decisions — 6 FADs
  - Date: 2026-05-25
  - What changed: `spec/01_requirements.md` — integrated all 6 Finalized Architecture Decisions (FAD-1 through FAD-6); updated Sections 3.15, 3.17, 3.18, 3.19, 4.7, 4.8, 6, 7, 9.4, 11.1, 11.2; added Sections 12.8, 13, 14
  - Verification: spec file updated; all 5 D-decisions resolved (D-1/3/4/5/6 via FAD-3/5/1/4/2 respectively); D-2 classified as open Implementation concern; warehouse schema design unblocked
  - Notes: Section 13 records all 6 FADs as authoritative and binding; Section 14 provides readiness assessment with authoritative dependency sequencing; warehouse schema Alembic migration (`0002_warehouse_schema.py`) is now unblocked

- [x] State transition model evolution — extend from outreach-only to all 6 lifecycle domains
  - Date: 2026-05-25
  - What changed: `spec/03_state_transition_rules.md` — Section 1 (PURPOSE) expanded to multi-domain scope with domain overview table; Section 2 (SOURCE OF TRUTH) updated with new file references; appended Sections 14–45 covering: Domain 2 Snapshot Lifecycle (states, transitions, freezing points, compliance override path, retry semantics, invariants); Domain 3 Monthly Report Lifecycle (async generation, publication boundary, historical regeneration, operator review); Domain 4 AI Insight Lifecycle (force-refresh semantics, versioning, snapshot isolation, attribution guarantees); Domain 5 Configuration Version Governance (activation boundary, prospective-only enforcement, atomic activation, rollback prohibition); Domain 6 Compliance Governance (authorization gating, audit requirements, interaction with snapshot/report immutability); Section 39 Event Attribution Standard (origin_source, origin_authority, execution_mode, attribution fields per domain); Section 40 Cross-Domain Invariants (7 platform-wide invariants covering append-only reporting, reproducibility, AI snapshot isolation, auditability, source attribution, SQL Server authority, compliance override restriction); Section 41 Failure & Recovery Modeling per domain; Section 42 Scope-Specific Behavior (all domains × MVP/STANDARD/PRODUCTION); Section 43 Acceptance Criteria for new domains; Section 44 Combined Invariants; Section 45 Updated References
  - Verification: specification document only (no code); consistent with FAD-1 through FAD-6; preserves all existing outreach lifecycle content unchanged

- [x] Idempotency and concurrency model evolution — extend from outreach-only to all 6 lifecycle domains
  - Date: 2026-05-25
  - What changed: `spec/04_idempotency_concurrency.md` — expanded from 463 to 1,315 lines; Section 1 (PURPOSE) expanded with multi-domain overview table and 6-domain idempotency concern summary; Section 2 (CORE PRINCIPLES) expanded with Platform Concurrency Philosophy (2.4: 6-workflow-class concurrency model, append-only philosophy, replay safety, determinism requirement) and Irreversible Operation Definitions (2.5: 7 explicitly irreversible operations); Section 4 (IDEMPOTENCY KEYS) expanded from outreach-only to 6 domain-specific key structures (Sections 4.1–4.7) covering snapshot (student_id, snapshot_month), report (cohort_id, report_month, template_version, lineage_version), AI insight (student_id, insight_type, date_period, prompt_version, version_number), config version, and compliance keys; appended Sections 15–26 covering: Section 15 Snapshot Finalization Idempotency (duplicate generation protection, concurrent finalization serialization, regeneration race protection, fingerprint drift prevention, stale update protection, compliance action dedup, idempotency classification table); Section 16 Report Generation Concurrency (duplicate job prevention, publication serialization, regeneration conflict prevention, stale rendering protection, artifact deduplication); Section 17 AI Generation Idempotency (duplicate request prevention, concurrent force-refresh handling, prompt version capture, model version attribution, partial failure recovery, serving state semantics, snapshot isolation boundary); Section 18 Config Version Governance Concurrency (simultaneous activation prevention, atomic swap invariant, duplicate promotion prevention, race condition at snapshot finalization, prospective-only enforcement, rollback prohibition); Section 19 Compliance-Governed Concurrency (duplicate request prevention, concurrent action prevention, stale approval handling, conflicting governance action prevention, partial execution recovery, audit survivability); Section 20 Event Idempotency & Attribution (communication events, access history, AI lifecycle, report lifecycle, compliance events, transition log dedup, correlation ID propagation, mirrored-source conflict handling); Section 21 Cross-Domain Concurrency Invariants (7 invariants: append-only finalized records, reproducibility under replay, AI snapshot isolation, config version consistency, compliance workflow serialization, historical regeneration read-only source, audit trail atomicity); Section 22 Failure & Recovery Model (cross-domain tables covering partial snapshot finalization, duplicate report publication, stale AI generations, orphaned regeneration jobs, fingerprint mismatch, failed compliance execution, concurrent config activation conflicts, failure classification summary); Section 23 Execution Mode Interactions (SHADOW mode for snapshots, compliance workflows in SHADOW, AI experimental context, auditability by mode); Section 24 Acceptance Criteria for new domains; Section 25 Downstream Implications for data model, API, warehouse schema, and orchestration; Section 26 Updated References
  - Verification: specification document only (no code); consistent with FAD-1 through FAD-6; consistent with spec/03_state_transition_rules.md Sections 14–45; preserves all existing outreach idempotency content unchanged

- [x] Physical warehouse architecture plan — translate conceptual data model into physical storage architecture
  - Date: 2026-05-25
  - What changed: `spec/09_warehouse_physical_architecture.md` (NEW, ~700 lines) — 12-section physical architecture specification covering: (1) three-schema physical boundary map with ASCII diagram, append-only and immutable-history enforcement strategies, regeneration and publication lineage strategies; (2) nine storage domain definitions (SQL Server mirror, outreach state machine, communication telemetry, monthly snapshots, report publication, AI insight lineage, config version registry, event timeline, compliance audit) with mutability, retention, and access profiles per domain; (3) snapshot storage strategy — two-table design recommendation (`warehouse.student_snapshots` + `warehouse.snapshot_ai_narratives` 1:1 companion) with FAD-1 compliance rationale, pre-migration benchmarking requirement, full column groupings for both tables (identity/lifecycle, SQL Server metric physical copies, derived metrics, communication summary, fingerprint components, metadata), reproducibility invariant table; (4) report artifact storage strategy — `warehouse.monthly_reports` and `warehouse.report_audit_log` column groupings, JSONB inline content recommendation with future object storage evolution path, report immutability under regeneration table; (5) event timeline storage strategy — hybrid approach (unified `public.student_timeline_events` for operational events, `warehouse.report_audit_log` for report lifecycle, `compliance_audit.deletion_log` for compliance events), top-level vs. JSONB column classification, SQL Server distinguishability enforcement, replay safety via idempotency keys, correlation ID propagation; (6) configuration version storage strategy — full column strategy for `config_version_registry` including all 22 configurable rule columns from spec/01 Sections 12.1–12.7, exactly-one-ACTIVE partial unique index enforcement, activation lineage query pattern, snapshot config version reference as logical (not physical FK) binding; (7) compliance audit storage strategy — schema isolation architecture, `compliance_audit.deletion_log` and `compliance_audit.scope_manifests` column strategies, survivability guarantees table, audit exportability requirements; (8) indexing and query strategy — expected query pattern categories with frequency/performance requirements, index strategy categories (PK, business key unique, idempotency key, state filtering, timeline, warehouse analytics), partitioning candidates, archival candidates; (9) storage evolution strategy — horizontal scaling path (read replicas, logical replication, schema-level archival separation), archive strategy per domain, long-term retention with partition-ready design, AI artifact growth management, event volume growth management; (10) data flow and synchronization — SQL Server→PostgreSQL sync boundary (with idempotency and partial-sync-safe properties), snapshot generation 3-step flow (DRAFT→VALIDATING→FINALIZED with invariants), report generation flow (with FAD-2 compliance), historical regeneration flow (POTENTIALLY_DIVERGENT logic), AI enrichment flow (with snapshot isolation boundary), compliance annotation flow (with pre-action hard gate); (11) migration planning readiness assessment — 0002 READY/0003 READY/0004 subsumed-by-0002 with FAD-4 inconsistency resolution, pre-migration benchmarking requirement, remaining blockers table (schema vs. go-live blockers distinguished), implementation-sensitive areas (permission GRANT statements in migration, config V1 seed, partial unique indexes, transaction safety), recommended migration sequencing (0001→0002+0003→0004+0005→0006); (12) output summary — physical architecture decision table, schema boundary recommendation table, recommended implementation sequence (8 steps: benchmark → 0002 → 0003 → deploy → 0004+0005 → service design), service layer sequencing (5 services with dependencies), open items for migration authors
  - Verification: specification document only (no code); consistent with FAD-1 through FAD-6; consistent with spec/08_data_model.md Sections 13–26; resolves spec/08 Section 8 inconsistency between 0004 and FAD-4 with explicit reconciliation recommendation; preserves all existing table structures and governance boundaries

- [x] Pre-migration storage validation — formal validation of physical warehouse architecture before migration authoring
  - Date: 2026-05-25
  - What changed: `spec/10_pre_migration_storage_validation.md` (NEW, ~900 lines) — 11-section pre-migration validation covering: (1) AI narrative storage validation — TOAST behavior analysis per design choice, two-table vs single-table comparison on analytical query performance, row bloat risk, buffer pool efficiency, append-only growth, historical reporting and regeneration-read performance, FAD-1 compliance confirmation, FK-to-ai_insights prohibition rationale, frozen-copy operational importance; (2) AI payload benchmarking plan — four benchmark queries (per-type distribution, per-student total, coverage, temporal trend), decision boundary thresholds (P95 per-field < 512 bytes for single-table viable; > 2048 bytes mandates two-table), growth projections (10–40% per year), row-width danger zones, analytical scan inflection points; (3) snapshot table physicalization review — fingerprint scalar column validation (3 scalar TEXT + 1 JSONB confirmed correct), JSONB usage review, lineage FK under compliance deletion confirmed safe, regeneration lineage semantics (snapshot regeneration re-reads stored metric columns, does NOT re-sync SQL Server), append-only gap identified (compliance pathway needs UPDATE (status) grant on warehouse.student_snapshots), partition-ready design confirmed (no blocking constructs), storage density estimates; (4) event storage validation — hybrid architecture structural validation, event volume scalability estimate (1.7–4.4M rows at full scale), timeline query efficiency, SQL Server distinguishability three-column strategy validated, unified table appropriateness confirmed, partition-ready design confirmed; (5) config version physical guarantee review — partial unique index operational correctness, concurrency race safety analysis (two-transaction scenario traced end-to-end), advisory locking not needed, application-level atomic swap still required alongside DB index; (6) compliance storage review — schema isolation validated, deletion-log survivability matrix confirmed, CRITICAL DEFECT IDENTIFIED AND FIXED: scope_manifests.is_current column violates INSERT-only invariant (compliance pathway cannot UPDATE) — removed from spec/09 Section 7.3 and replaced with MAX(manifest_version) query pattern, governance audit persistence (PRE/POST/CHECKPOINT pattern) validated, MVP/STANDARD sufficiency confirmed, compliance pathway UPDATE (status) grant gap confirmed; (7) migration 0002 readiness assessment — formal classification per area: warehouse.student_snapshots READY WITH VALIDATION, snapshot_ai_narratives READY, monthly_reports READY, report_audit_log READY, deletion_log READY, scope_manifests READY WITH MODIFICATION (is_current removed), permission grants READY WITH MODIFICATION (UPDATE grant added); (8) migration sequencing validation — compliance_audit in 0002 per FAD-4 re-confirmed, prior spec/08 inconsistency fully resolved, validated sequencing (0002 → 0003 concurrent → 0004 after 0002 → 0005 after 0002 → 0006 deferred), dangerous coupling concerns (GRANTs must be in migration, V1 seed transactional, 0002+0003 both required before snapshot service, compliance service needs schema check at startup); (9) implementation risk review — HIGH risks: missing UPDATE grant, is_current defect, GRANT omission, non-transactional atomic swap, missing V1 seed; MEDIUM risks: report_content_json payload growth, unbounded lineage depth; LOW risks: fingerprint JSONB evolution; scaling bottleneck projections (timeline events ~10M rows at 10K students triggers partitioning; snapshot table ~500K rows at that scale); (10) output — AI storage recommendation (two-table confirmed), benchmark strategy, finalized snapshot recommendation, migration readiness matrix, implementation risk matrix, migration sequencing recommendation, physical and operational scalability assessments, final pre-authoring checklist with 10 items all confirmed; (11) specification corrections to propagate — 4 corrections identified for spec/09; two critical corrections applied immediately (is_current removal, UPDATE grant addition); `spec/09_warehouse_physical_architecture.md` updated in Sections 7.3 and 11.4 with both corrections
  - Verification: specification document only (no code or migrations); consistent with FAD-1 through FAD-6; spec/09 Sections 7.3 and 11.4 corrected in place; all 10 pre-authoring checklist items confirmed; 0002 and 0003 authoring is unblocked

- [x] spec/07_api_contracts.md — full evolution to governed historical intelligence API spec
  - Date: 2026-05-25
  - What changed: `spec/07_api_contracts.md` rewritten from 1,138 lines (operational-only) to ~1,000-line governed historical intelligence spec. Preserved all existing §8 operational endpoints (3.1–3.15) unchanged. Added: §2 expanded API Architecture Philosophy (mutable vs immutable surfaces, operational vs historical endpoints, lineage-aware patterns, append-only historical workflows, reproducibility visibility, SQL Server read-only boundary); §3 error handling with HTTP status codes and full error code table including governance-specific codes (`GOVERNANCE_BOUNDARY`, `FINALIZED_IMMUTABLE`, `PUBLISHED_IMMUTABLE`, `CONFIG_ROLLBACK_FORBIDDEN`, `AUDIT_ENTRY_MISSING`); §4 idempotency rules with domain-specific keys and `Idempotency-Key` header semantics; §5 role-based visibility model (mentor/super_mentor/instructor/operator/compliance_reviewer/config_admin/admin) with super-mentor transitive visibility rule and governance boundary authorization table; §6 async operation model with job pattern, polling expectations, and failure visibility including `stale_insight_available`; §7 filtering/query semantics (time-window, config version, fingerprint, timeline event attribution); §9 Snapshot APIs (draft creation, lifecycle transitions, finalized retrieval, historical lookup, fingerprint inspection, lineage traversal, historical comparison); §10 Report APIs (generation, published retrieval, version lookup, publication history, publish, regeneration, source fingerprint); §11 AI Insight APIs (active retrieval with stale flag, version history, specific version, generation with force-refresh idempotency, PRODUCTION review gate, provenance inspection, snapshot isolation behavior guarantee); §12 Config Version Registry APIs (active version, historical lookup, version list, proposal with rollback prohibition, lifecycle transitions with atomic APPROVED→ACTIVE, lineage traversal, snapshots-referencing-version); §13 Compliance APIs (workflow request, status, lifecycle transitions with BEGIN_EXECUTION hard gate, audit log visibility, audit export survivability guarantee); §14 Timeline and Lineage APIs (extended timeline with origin_source/authority/is_authoritative, generic lineage traversal, fingerprint divergence inspection, regeneration history); §15 extended acceptance criteria (10 cases covering immutable retrieval, lineage correctness, reproducibility visibility, governance isolation, attribution consistency, append-only behavior, config lineage); §16 unresolved API concerns (U-1 through U-11); §17 downstream implications (service layer, repositories, frontend dashboards, report rendering, orchestration); §18 references
  - Verification: specification document only (no implementation code); consistent with spec/01 §12.1–12.7 threshold field names; consistent with spec/03 state enums for all 6 domains; consistent with spec/08 access control matrix and data invariants §22; consistent with 0002/0003 migration table names and column names; super-mentor transitive visibility preserved per CLAUDE.md; all 13 output requirements from the task satisfied; PROGRESS.md hard gate satisfied

- [x] Alembic migration 0003 — config_version_registry initialization
  - Date: 2026-05-25
  - What changed: `alembic/versions/0003_config_version_registry.py` (NEW, ~270 lines) — creates `public.config_version_registry` with 24 configurable rule columns (spec/01 §12.1–12.7), governance lifecycle columns, self-referential lineage FKs, exactly-one-ACTIVE partial unique index on constant expression `(1) WHERE status = 'ACTIVE'`, column-level UPDATE grant for `config_admin_user` on governance metadata only (status, superseded_by_version_id, deactivated_at), and transactionally coupled V1 seed record at `status = 'ACTIVE'` with all spec/01 Section 12 defaults
  - Verification: Python syntax correct; revision chain `0002 → 0003` set; all 24 rule columns from spec/01 §12.1–12.7 present; exactly-one-ACTIVE invariant enforced via `uq_cvr_active_singleton` partial unique index on constant expression `(1)` (raw DDL, not op.create_index); FAD-3 compliant (prospective-only semantics; no retroactive recalculation possible); spec/10 §8.5 compliant (V1 seed in same upgrade() as CREATE TABLE, same Alembic transaction); CHECK constraint on status domain enforced; self-referential FKs on prior_version_id and superseded_by_version_id; downgrade drops table, cascading to all indexes
  - Notes: config_admin_user is a new role (not in 0002); all grants are pg_roles-guarded for dev/CI safety; payment_deviation_alert_threshold nullable per spec/01 §12.3 ("default TBD"); rule_set_snapshot_json is an inline JSONB object duplicating all 24 rule values for one-shot reproducibility audit without column joins

- [x] spec/05_external_integrations.md — full evolution to governed integration architecture spec
  - Date: 2026-05-25
  - What changed: `spec/05_external_integrations.md` rewritten from 444 lines (MVP-era GHL/LLM/Synthflow reliability doc) to ~900-line governed integration architecture specification. Existing §§1–16 replaced in full. Added: §2 Integration Philosophy (6 pillars: governed synchronization, append-only ingestion, lineage-preserving integration, replay-safe ingestion, authoritative-source boundaries, reproducibility-safe synchronization); §3 Authoritative Boundary Model (system authority classification table, authoritative vs derivative distinction, operational vs historical ownership, synchronization vs mutation semantics, mirrored vs platform-owned event semantics); §4 SQL Server Integration Architecture (6 extraction domain table, synchronization philosophy with idempotent upsert semantics, incremental extraction expectations per scope, attribution preservation rules, replay safety, conflict handling, mirrored-event semantics, historical snapshot isolation boundary); §5 GHL / Communication Provider Integration (provider ownership boundaries, communication event ingestion with deduplication keys, attribution preservation, timeline synchronization, deduplication expectations, replay handling, future provider extensibility contract); §6 AI Provider Integration (role boundary, integration scope, prompt version attribution immutability, model version attribution, refresh lineage, reproducibility expectations per FAD-1, retry semantics per scope, failure visibility with stale serving, frozen snapshot isolation per INVARIANT-3, provider independence); §7 Snapshot Generation Orchestration (conceptual orchestration sequence across 6 phases: preconditions, DRAFT assembly, content lock, AI enrichment, fingerprint freezing, finalization; AI enrichment boundary, fingerprint freezing, orchestration ownership, append-only guarantees, replay safety table, failure recovery table, historical regeneration boundary per FAD-2); §8 Configuration Registry Integration (how consumers read ACTIVE config, historical config reference for audit, prospective-only semantics for integration consumers, exactly-one-ACTIVE guarantee for integration consumers); §9 Event Ingestion and Timeline Integration (event ingestion architecture, timeline normalization with attribution_timestamp vs ingestion_timestamp, attribution propagation rules, replay-safe ingestion mechanisms, unified timeline expectations, event ordering, eventual consistency assumptions); §10 Compliance and Governance Integration (architecture overview, compliance export architecture, audit export content, deletion workflow integration boundaries with hard gate, survivability guarantees, governance isolation boundaries); §11 Failure and Retry Semantics (partial synchronization recovery, failure classification table across 17 failure types, duplicate ingestion handling table, stale AI at snapshot time, provider outage handling, reconciliation expectations, attribution preservation during retries); §12 Future Integration Extensibility (additional AI providers, CRM providers, communication providers, vector-search and RAG systems, analytics warehouses, compliance export systems); §13 Acceptance Criteria (7 cases: replay-safe ingestion, lineage preservation, attribution consistency, reproducibility preservation, append-only guarantees, authoritative-boundary preservation, governance isolation); §14 Downstream Implications (service layer table, scheduler jobs table, ETL jobs table, AI orchestration table, observability requirements table); §15 Unresolved Integration Concerns (UI-1 through UI-7); §16 Integration Invariants; §17 Scope-Specific Behavior Summary (MVP/STANDARD/PRODUCTION); §18 References
  - Verification: specification document only (no implementation code); consistent with spec/01 operational domains and cohort rules; consistent with spec/03 §39 Event Attribution Standard and §40 Cross-Domain Invariants; consistent with spec/04 idempotency keys per domain and CONC-INVARIANT-1 through CONC-INVARIANT-7; consistent with spec/07 API contracts for sync, snapshot, report, AI insights, config version, compliance endpoints; consistent with spec/08 dual-database architecture, three-schema separation, and data authority levels; consistent with spec/09 physical boundary map, append-only enforcement, access control profiles; consistent with 0002/0003 migration table names and permission model; FAD-1 (frozen AI copy), FAD-2 (snapshot-centric historical), FAD-3 (prospective-only config), FAD-4 (compliance isolation), FAD-5 (SQL Server read-only), FAD-6 (reproducibility fingerprint) all preserved; PROGRESS.md hard gate satisfied

- [x] Alembic migration 0002 — warehouse and compliance_audit schema initialization
  - Date: 2026-05-25
  - What changed: `alembic/versions/0002_warehouse_schema.py` (NEW, ~370 lines) — full Alembic migration creating `warehouse` schema (4 tables) and `compliance_audit` schema (2 tables) in the same file per FAD-4. Tables created: `warehouse.student_snapshots` (identity/lifecycle, ss_-prefixed SQL Server metric copy columns, derived metrics, communication summary, 5-component reproducibility fingerprint, metadata); `warehouse.snapshot_ai_narratives` (1:1 AI text companion; FK to student_snapshots NOT to ai_insights per FAD-1; advisory ai_source_insight_id_hint INTEGER with no FK constraint); `warehouse.monthly_reports` (lineage-versioned with self-referential parent_report_id FK; JSONB report_content_json, aggregate_stats_json, source_snapshot_fingerprint_json; idempotency key); `warehouse.report_audit_log` (append-only lifecycle log; FK to monthly_reports); `compliance_audit.deletion_log` (permanent append-only audit trail; no FK to public or warehouse schemas; plain INTEGER student_id for survivability); `compliance_audit.scope_manifests` (append-only version lineage; is_current column OMITTED per spec/10 correction 1; manifest_version ordering pattern). Indexes created: partial unique `(student_id, snapshot_month) WHERE status='FINALIZED'` on student_snapshots; partial unique `(cohort_id, report_month, lineage_version) WHERE status='REPORT_PUBLISHED'` on monthly_reports; unique `report_idempotency_key` on monthly_reports; unique `snapshot_id` on snapshot_ai_narratives; fingerprint column indexes (config_registry_version, schema_version) for audit queries; timeline query indexes; compliance indexes. Permission grants implemented with pg_roles existence guards: `app_service_user` SELECT on both schemas; `finalization_service_user` INSERT on warehouse tables + sequence USAGE; `compliance_pathway_user` INSERT on compliance_audit tables + sequence USAGE + column-level UPDATE(status) on warehouse.student_snapshots (sole exception to INSERT-only model; spec/10 correction 2). Full downgrade path reverses grants, drops tables in FK dependency order, drops schemas. Module docstring documents both critical corrections, two-table rationale, service account naming, and future migration grant obligations.
  - Verification: Python syntax correct; revision chain `0001 → 0002` set; all spec/09 column groups implemented; both spec/10 corrections applied (no is_current, UPDATE grant present); FAD-1 compliant (snapshot_ai_narratives FK to student_snapshots, not ai_insights); FAD-4 compliant (both schemas in same migration); all partial unique indexes use `postgresql_where` clause; downgrade order respects FK dependencies

- [x] Conceptual data model evolution — extend from operational outreach storage to governed historical-intelligence architecture
  - Date: 2026-05-25
  - What changed: `spec/08_data_model.md` — evolved from 453 to 1,475 lines; Section 1 (PURPOSE) rewritten to define 7 platform capabilities and platform capability summary table with governance boundaries per capability; Section 2 (DATABASE OVERVIEW) rewritten to DATABASE ARCHITECTURE with PostgreSQL schema separation (public / warehouse / compliance_audit), schema-level access control matrix, expanded data sources table, and conceptual data class mutability table; Section 7 (STATE FIELD ENUMS) expanded from outreach-only to all 6 lifecycle domains (Domains 1–6) with state diagrams and governance boundary annotations; Section 8 (MIGRATION STRATEGY) updated from migration-lite to Alembic-primary with upcoming migration table (0002–0006) and rules; Section 9 (DATA CONSISTENCY RULES) expanded from 5 bullets to 7 subsections (universal, outreach, snapshot, report, AI, config, compliance, cross-schema); Section 10 (SCOPE-SPECIFIC BEHAVIOR) expanded from MVP/LIVE to MVP/STANDARD/PRODUCTION with per-domain behavior table; Section 11 (ACCEPTANCE CRITERIA) expanded from 4 cases to 12 cases covering all governance boundaries; Section 12 (DOWNSTREAM IMPLICATIONS) table mapping conceptual entities to downstream artifacts; appended Sections 13–26 covering: Section 13 Schema Ownership Architecture (public/warehouse/compliance_audit with access control matrix); Section 14 Snapshot-Centric Historical Architecture (StudentSnapshot, SnapshotReproducibilityFingerprint, FrozenAINarrativeCopy, SnapshotLineage conceptual entities; draft vs finalized behavior table; snapshot-to-report relationship diagrams); Section 15 Operational Intelligence Domain Model (7 intelligence sub-domains: coursework, attendance/progression, segment classification, financial, mentor visibility, communication telemetry, access history; operational vs historical responsibility table); Section 16 Unified Event and Timeline Model (7 event types: CommunicationEvent, AccessHistoryEvent, AILifecycleEvent, SnapshotLifecycleEvent, ReportLifecycleEvent, ComplianceLifecycleEvent, ConfigLifecycleEvent; event attribution standard; unified timeline rendering spec); Section 17 AI Insight and Reproducibility Model (AIInsightRecord, AIInsightVersionHistory, FrozenSnapshotNarrative, SnapshotAIIsolationBoundary; active vs archived version table; reproducibility semantics); Section 18 Configuration Version Registry Model (ConfigVersionRecord, ConfigActivationLineage, SnapshotConfigVersionReference; governance invariants; historical auditability); Section 19 Compliance Governance Model (ComplianceWorkflowRecord, ComplianceAuditEntry, ComplianceScopeManifest; governance isolation principles; data location deletion path table; reproducibility implications after deletion); Section 20 Report Artifact and Publication Model (MonthlyReportArtifact, ReportTemplateVersion, ReportLineage, ReportSourceBinding; publication vs regeneration semantics table); Section 21 Data Ownership and Source Authority (SQL Server authoritative domains; PostgreSQL authoritative domains; data mutability classification table; mirrored data expectations); Section 22 Cross-Domain Data Invariants (8 platform-wide invariants: DATA-INVARIANT-1 through DATA-INVARIANT-8 covering append-only finalized reporting, reproducibility, AI snapshot isolation, config version consistency, compliance audit survivability, SQL Server authority, historical regeneration read-only source, governance boundary irreversibility); Section 23 Future-Scale Considerations (large archives, AI provider evolution, report template evolution, vector search/RAG, enterprise compliance); Section 24 Acceptance Criteria Extended (Cases 13–18 covering config lineage, classification auditability, compliance schema independence, mirrored source attribution, warehouse permission enforcement, report artifact preservation); Section 25 Downstream Implications (API contracts, warehouse schema, compliance schema, config registry, observability per domain); Section 26 References (governing documents + legacy references)
  - Verification: specification document only (no code); consistent with FAD-1 through FAD-6; consistent with spec/03_state_transition_rules.md Sections 14–45; consistent with spec/04_idempotency_concurrency.md Sections 15–26; preserves all existing operational table descriptions (Sections 3–6) unchanged

- [x] spec/09_security_privacy.md — full evolution to governed security and privacy architecture specification
  - Date: 2026-05-25
  - What changed: `spec/09_security_privacy.md` completely rewritten from 383-line MVP-era generic security doc to ~900-line governed security and privacy architecture specification. Existing content (basic principles, generic auth, data protection, secrets management) replaced in full. New structure: §1 Security Philosophy (governance-aware security; operational vs governance action table; mutable vs immutable security boundaries table; 6 security pillars: historical integrity protection, lineage integrity protection, audit survivability, reproducibility preservation, privileged workflow isolation); §2 RBAC (9 platform user roles including compliance_officer, auditor, config_manager, report_publisher; full 14-row × 9-column permission matrix extending spec/01 §3.20; schema-level permissions by role; governance operation permissions; super mentor transitive visibility rules; report and historical artifact visibility); §3 Schema Isolation (three-schema boundary per spec/08 §13; security profile per schema; FK independence as hard survivability guarantee); §4 Service Account Governance (three-SA model: standard SA public CRUD + warehouse SELECT + compliance_audit SELECT; finalization SA warehouse INSERT-only; compliance pathway SA scoped DELETE on public/warehouse + compliance_audit INSERT-only; config activation accountability; AI orchestration boundary; ingestion service boundary; privilege separation rationale); §5 API Security Model (authentication, authorization, immutable resource protection, lineage inspection, regeneration, compliance, AI insight, report publication surfaces; forbidden operations list; replay attack prevention; attribution tampering prevention); §6 Secret and Credential Governance (SQL Server read-only at SQL Server RBAC level; 3 PostgreSQL SA credentials; AI provider; communication provider; env separation; rotation; developer access; prohibition); §7 Transcript and AI Content Security (access by insight type; frozen snapshot immutability; PII prohibition table; privacy boundaries; retention; export controls); §8 Audit and Attribution Integrity (attribution field immutability table; origin_source protection; audit log survivability table by domain; tamper resistance; append-only guarantees; privileged visibility; compliance survivability); §9 Privacy and FERPA Governance (student data classification by FERPA applicability; minimum necessary access; FERPA deletion pathway 5 steps; historical deletion annotations; AI processing privacy; communication log privacy; export governance; FERPA vs reproducibility tension resolution); §10 Observability and Logging Security (PII redaction rules table; credential redaction; AI output logging constraints; audit-event logging JSON example; privileged operation logging table; log storage/retention; anti-pattern prohibition list); §11 Security Failure and Recovery (credential compromise; privilege escalation; audit corruption attempt; replay attack; unauthorized regeneration; unauthorized config activation; failed compliance operations; containment/recovery table; forensic survivability); §12 Future Security Extensibility (SSO/SAML/OIDC, MFA, enterprise RBAC, field-level encryption, PostgreSQL RLS, customer-managed keys, external audit integrations); §13 Acceptance Criteria (AC-1 through AC-7); §14 Downstream Implications and Unresolved Concerns (5 downstream implication tables; US-1 through US-7 unresolved security concerns); §15 Scope-Specific Behavior (MVP/STANDARD/PRODUCTION); §16 References
  - Verification: specification document only (no implementation code); consistent with spec/01 §3.20 RBAC model, §4.7 compliance pathway, §13 FAD-1 through FAD-6; consistent with spec/03 §39 Event Attribution Standard and §40 Cross-Domain Invariants (INVARIANT-1 through INVARIANT-7); consistent with spec/04 audit survivability §19.6 and CONC-INVARIANT-7; consistent with spec/08 §13 access control matrix and DATA-INVARIANT-1 through DATA-INVARIANT-8; consistent with three-schema warehouse architecture from spec/09_warehouse_physical_architecture.md and 0002 migration; FAD-1 through FAD-6 all preserved; 7 acceptance criteria defined (AC-1 through AC-7); PROGRESS.md hard gate satisfied
  - Notes: 7 unresolved security concerns documented (US-1 through US-7): HIGH-severity items requiring action before production — US-1 (authentication mechanism — JWT vs session vs API key, blocks multi-user deployment), US-2 (SQL Server read-only must be verified at SQL Server RBAC level, not just application convention), US-3 (FERPA obligations for aggregate reports after student deletion — legal question requiring Legal/Compliance decision), US-4 (AI provider DPA for FERPA compliance — confirm no training on student metrics before AI insights on real students); MEDIUM-severity: US-5 (compliance audit export credential model), US-6 (GHL webhook signature verification); LOW-severity: US-7 (row-level security for super mentor — blocked by open assumption D-2 supervision graph)

- [x] spec/06_observability_operations.md — full evolution to governed observability and operational governance architecture specification
  - Date: 2026-05-25
  - What changed: `spec/06_observability_operations.md` created from scratch as an ~900-line governed observability and operational governance architecture specification (18 sections; file did not previously exist). Sections: §1 Observability Philosophy (two-category telemetry model: operational telemetry rotatable vs governance telemetry permanent DB-backed; lineage-aware and governance-aware observability philosophy; 8 subsections); §2 Structured Logging Architecture (universal log schema with JSON examples for all 8 event types: API, orchestration job, snapshot governance transition with 5-component fingerprint, AI enrichment metadata-only, report publication, config activation, compliance execution, ingestion attribution; PII redaction applied at schema level; 11 subsections); §3 Trace Propagation & Correlation (correlation_id UUID-v4 at entry point; causation_id for multi-hop lineage: child's causation_id = parent's correlation_id; async propagation; replay vs regeneration trace isolation; 10 subsections); §4 Orchestration Visibility (phase-by-phase visibility for all 6 workflow types; 10 subsections); §5 Replay & Regeneration Observability (fingerprint divergence diagnostics; POTENTIALLY_DIVERGENT flag; stale AI detection; reproducibility validation; 7 subsections); §6 Scheduler & Async Job Observability (heartbeat architecture; stuck/orphaned workflow detection; SLA visibility — job > 60s SLA emits within_sla: false + HIGH alert; 8 subsections); §7 AI Orchestration Telemetry (metadata-only AI logging — content text never in logs; frozen snapshot AI isolation confirmation log entries asserting INVARIANT-3 honored; 9 subsections); §8 Config & Governance Observability (activation/supersession visibility; prospective-only enforcement logging; 7 subsections); §9 Compliance & Audit Observability (deletion workflow with pre-action gate; compliance audit survivability; export observability; 8 subsections); §10 Alerting & SLA Governance (5-tier alert classification: CRITICAL/HIGH/MEDIUM/LOW/INFO with response time requirements; 8 alert domains; 11 subsections); §11 Operational Dashboards & Metrics (8 named dashboards; operational vs governance metric distinction; 10 subsections); §12 Redaction & Privacy-Aware Telemetry (field-level redaction table: email→***@***, phone→***, name→[REDACTED], UserID→opaque hash only; telemetry minimization anti-patterns; 9 subsections); §13 Failure & Recovery Observability (forensic survivability; root-cause visibility; 8 subsections); §14 Future Operational Extensibility (architecture-level only; no implementation; 7 subsections); §15 Acceptance Criteria (AC-1 through AC-8); §16 Downstream Implications & Unresolved Operational Concerns (5 implication tables; UO-1 through UO-7); §17 Scope-Specific Behavior (MVP/STANDARD/PRODUCTION); §18 References
  - Verification: specification document only (no implementation code, no OpenTelemetry/Grafana/Prometheus/ELK config); INVARIANT-1 through INVARIANT-7 preserved (spec/03 §40); FAD-1 through FAD-6 preserved (spec/01 §13); consistent with spec/09 §10 Observability & Logging Security PII redaction rules, credential redaction, AI output logging constraints; consistent with spec/04 idempotency keys and CONC-INVARIANT-1 through CONC-INVARIANT-7; consistent with spec/07 API correlation_id propagation (X-Correlation-ID header); consistent with spec/08 three-schema access control and service account telemetry model; consistent with 0002/0003 migration table names and permission model; 8 acceptance criteria (AC-1 through AC-8); PROGRESS.md hard gate satisfied
  - Notes: 7 unresolved operational concerns (UO-1 through UO-7) documented in §16; HIGH-severity blockers before production: UO-1 (log aggregation platform and DPA verification not specified — required before any PII flows through log pipeline), UO-2 (distributed scheduler coordination strategy not specified — APScheduler in-memory job store cannot coordinate across horizontal instances); MEDIUM-severity: UO-3 (student opaque ID hashing scheme definition), UO-4 (alert delivery mechanism — email/PagerDuty/Slack), UO-5 (SIEM forwarding decision for Security/Compliance); LOW-severity: UO-6 (causation ID propagation across async job boundaries — verification required), UO-7 (governance dashboard access control at dashboard API layer)

- [x] execution/build_phases.md — complete rewrite to governed historical intelligence platform build phases
  - Date: 2026-05-25
  - What changed: `execution/build_phases.md` completely rewritten from 480-line MVP-era outreach-only build phase document (10 phases: Foundation→Outreach→Decision→State→Loop→Retry→LLM→Failure→Observability→UX) to ~850-line governed platform build phase specification (21 sections, 12 phase families). New structure: §1 Purpose; §2 Why Old Sequencing Was Insufficient (8-row deficiency table); §3 Phase Architecture Principles (6 principles: governance-first, observability-first, lineage-safe, replay-safe by construction, SHADOW before LIVE, dependency ordering preserves invariants); §4 Phase Dependency Map (ASCII tree showing all 12 phase dependencies including Phase 5/6 interlocking note); §5–§16 Phase 1 through Phase 12 each with objectives, components, dependencies, validation checkpoints, observability requirements, governance implications, rollback boundaries, exit criteria; §17 Architecture-Preservation Rules (AP-1 through AP-15: prohibiting mutable finalized snapshots, mutable published reports, config rollback, lineage overwrites, replay corruption, audit mutation, PII in logs, AI content in logs, observability shadow-storage violations, LIVE mode without certification); §18 Scope-Specific Execution (MVP/STANDARD/PRODUCTION with mandatory-at-all-tiers table); §19 Validation Gate Catalogue (VG-1 through VG-12); §20 Acceptance Criteria (AC-1 through AC-6); §21 References (14 canonical documents)
  - Verification: execution planning document only (no implementation code, no FastAPI, no schedulers, no deployment code); all 12 phases enforce INVARIANT-1 through INVARIANT-7 at phase exit; FAD-1 through FAD-6 all enforced at respective phase gates; Phase 5/6 interlocking dependency documented; governance readiness (Phase 12) replaces dashboard completion as production milestone; consistent with spec/06 §15 acceptance criteria (AC-1 through AC-8 required for VG-10/VG-12); consistent with spec/09 §13 acceptance criteria (AC-1 through AC-7 required for VG-12); PROGRESS.md hard gate satisfied
  - Notes: Phase 5/6 ordering requires careful staging: Phase 5 DRAFT→VALIDATING sub-phase does not require AI; Phase 6 must reach AI_REVIEWED before Phase 5 VALIDATING→FINALIZED is activated; this interlocking is explicit in §9 and §10; US-1, US-2, UO-1, UO-2 all block Phase 12 certification; AP-1 through AP-15 architecture-preservation rules are binding at all scope tiers

- [x] execution/implementation_plan.md — complete rewrite to governed orchestration sequencing implementation plan
  - Date: 2026-05-25
  - What changed: `execution/implementation_plan.md` completely rewritten from 448-line MVP-era feature-first plan (Foundation→Outreach→Decision Engine→State→LLM→Failure→UX) to ~700-line governed orchestration sequencing implementation plan (16 sections). New structure: §1 Purpose; §2 Build Principles (6 governance-first principles); §3 Delivered Phases (authoritative record: Phase 1–5 + Architecture Hardening + Phase 8 harmonization, referencing PROGRESS.md); §4 Reconciliation (why previous sequencing is replaced — 8-row comparison table of old vs. new); §5 Service Implementation Order (10 services with sequencing rationale, governance-blocker classification, idempotency requirement, and lineage dependency for each: SQL Server ingestion, timeline ingestion, snapshot orchestration, config resolution, AI enrichment, report generation, publication workflows, compliance workflow, replay/regeneration, operational dashboards); §6 Hard Dependencies (dependency table across all 10 services); §7 MVP/STANDARD/PRODUCTION Strategy (mandatory-at-all-tiers table; scope-tier definitions with validation gates); §8 SHADOW→LIVE Rollout Model (SHADOW requirements, 9-item LIVE enablement gate checklist, LIVE activation process, LIVE-mode-cannot-be-set-retroactively rule); §9 Operational Readiness Model (SHADOW expectations, 9-checkpoint production certification, replay validation expectations, operational staffing assumptions); §10 Failure and Recovery Execution Rules (per-service recovery tables: sync, snapshot, AI enrichment, report generation, ingestion, compliance interruption — 6 domains, 26 failure scenarios); §11 Dependency Graph (ASCII tree); §12 Architecture-Preservation Rules (5 categories: immutability, authority, attribution, lineage, observability — 15 total rules); §13 Unresolved Implementation Concerns (US-1 through US-7, UO-1 through UO-7 classified as blocking production, blocking STANDARD, or deferred); §14 Downstream Implications (4 domains: service implementation, orchestration, operations, deployment, support workflows); §15 Acceptance Criteria (AC-1 through AC-6, including AC-6 explicitly naming governance readiness as final milestone); §16 References
  - Verification: execution planning document only (no implementation code, no FastAPI, no schedulers, no deployment code); governance readiness (Phase 12 certification) explicitly named as the production milestone replacing dashboard completion; all 6 FADs enforced in service implementation order rationale; INVARIANT-1 through INVARIANT-7 preserved; US-1, US-2, UO-1, UO-2 all listed as blocking production go-live; 26 failure/recovery scenarios across 6 service domains; SHADOW→LIVE model with 9 explicit enablement gates; PROGRESS.md hard gate satisfied

- [x] directives/outreach_retry_policy.md — complete rewrite to governance-aware orchestration retry architecture
  - Date: 2026-05-25
  - What changed: `directives/outreach_retry_policy.md` completely rewritten from 370-line communication-attempt retry execution policy (retry_allowed boolean, CALL→SMS→EMAIL dispatch sequencing, hardcoded MAX_ATTEMPTS=1/2/3, hardcoded RETRY_INTERVAL=24h, scope-specific helper functions) to ~700-line governance-aware orchestration retry architecture directive (14 sections). Retry policy reframed from direct dispatch sequencer to orchestration retry advisement and retry eligibility governance. New structure: §1 Purpose (six distinct retry advisory categories: retry cooldown eligibility, retry attempt eligibility, escalation candidacy within retry context, replay retry eligibility, retry priority, AI-assisted retry prioritization eligibility; explicit non-responsibilities: no outbound dispatch, no channel selection, no state mutation, no escalation dispatch, no provider interaction); §2 Input Model (5 input categories: Retry Context with retry lineage/attempt history/consecutive no-response count/provider failure count/channel exhaustion state/orchestration history; Governance Context with config_version_id/execution_mode/execution_type/fingerprint_context/compliance_hold_flag/replay_context plus retry_attempt_number for idempotency key uniqueness; Attribution Context with historical longitudinal continuity; AI Context with staleness metadata, finalized_copy flag, confidence score; Operational Context with SQL Server-authoritative access_state, contact data, engagement signal); §3 Output Model (RetryOrchestrationAssessment schema replacing retry_allowed: boolean — includes retry_orchestration_eligible, retry_scope enum: RETRY_ELIGIBLE/RETRY_DEFERRED/RETRY_EXHAUSTED/ESCALATION_CANDIDATE/REPLAY_RETRY_CANDIDATE/NONE, retry_constraints with remaining_attempts/effective_retry_window/channel_exhaustion_state/live_retry_permitted, governance_requirements, escalation_recommendation with urgency/reason_codes, observability_metadata, attribution_metadata, replay_restrictions, config_version_reference with threshold_bindings for all 8 config keys, cooldown_metadata with elapsed/remaining/effective_window/provider_failure_extended/retry_window_mode, retry_priority, ai_context, reason_codes, retry_blocked, blocking_reason; output clarifications: advisory not authoritative, no DB writes, no channel selection); §4 Retry Rules (14 rules ordered: RULE 0 GOVERNANCE PRECONDITION GATE blocking on config/mode/certification/correlation/attribution/idempotency/replay-context/retry-lineage-inconsistency failure; RULE 1 TERMINAL STATE CHECK; RULE 2 COMPLIANCE HOLD GATE producing ESCALATION_CANDIDATE CRITICAL scope; RULE 3 CONFIG-BOUND THRESHOLD RESOLUTION non-terminal resolving 8 named threshold variables from rule_set_snapshot; RULE 4 CONTACT DATA GATE; RULE 5 AI GOVERNANCE TIER ASSESSMENT non-terminal; RULE 5A PENDING AI GATE → RETRY_DEFERRED scope; RULE 6 FIRST CONTACT GATE blocking non-retry contexts; RULE 7 MAX ATTEMPTS EXHAUSTION GATE → RETRY_EXHAUSTED scope with escalation recommendation; RULE 8 CONSECUTIVE NON-RESPONSE ESCALATION GATE; RULE 9 PROVIDER FAILURE ASSESSMENT non-terminal with cooldown extension and escalation at provider failure limit; RULE 10 REPLAY ELIGIBILITY GATE forcing REPLAY_RETRY_CANDIDATE scope; RULE 11 RETRY COOLDOWN ASSESSMENT computing elapsed vs effective_window with ROLLING/FIXED mode support; RULE 12 MULTI-FACTOR RETRY PRIORITY ASSESSMENT producing retry_priority from 9 urgency factors; RULE 13 AI-ASSISTED RETRY PRIORITIZATION with CONFIDENT/ADVISORY/STALE/UNAVAILABLE/FINALIZED_COPY behaviors; RULE 14 DEFAULT RETRY ADVISORY OUTPUT); §5 Config Version Governance (resolution protocol with replay-mode historical-version path; no hardcoded retry timing; config lineage in threshold_bindings output; FAD-3 prospective-only enforcement); §6 Replay & Regeneration Semantics (replay retry always REPLAY_RETRY_CANDIDATE — never RETRY_ELIGIBLE; cooldown uses historical timestamps not current_timestamp in replay; regeneration behavior; SHADOW enforcement; replay observability isolation); §7 AI Governance (AI principle: advisory for retry prioritization only; cannot override config-bound retry limits or governance blocks; 6-tier governance tier behavior table; stale AI LIVE mode protection; FAD-1 snapshot isolation; deterministic fallback); §8 Observability & Attribution (20-field log schema including cooldown telemetry; correlation_id propagation; attribution propagation; retry trace metadata; PII redaction rules); §9 Failure & Safety Semantics (8 failure scenarios: missing data with governance-critical classification for last_contact_timestamp, stale retry lineage, invalid config resolution, duplicate retry orchestration, replay conflict, stale AI, provider outage including systemic outage detection, cooldown corruption from future timestamps); §10 Architecture Preservation Rules (AP-R1 through AP-R15: no outbound dispatch, no state mutation, no config mutation, no immutable snapshot mutation, no historical overwrite, no replay corruption, no audit bypass, SQL Server authority boundary, no hardcoded retry timing, no PII in logs, no RETRY_ELIGIBLE in replay, no idempotency key reuse, RULE 0 mandatory, compliance hold ESCALATION_CANDIDATE only, no attribution overwrite); §11 Scope Behavior (mandatory-at-all-scopes list; MVP reductions with AI/provider failure/consecutive no-response short-circuits; STANDARD additions; PRODUCTION additions including LIVE certification gate); §12 Acceptance Criteria (AC-R1 through AC-R12 covering deterministic retry evaluation, replay scope ceiling, config-version reproducibility, attribution continuity, immutable historical protection, stale AI protection, SHADOW isolation, governance precondition blocking, provider failure cooldown extension, consecutive non-response escalation, max attempts terminal, first-contact gate); §13 Downstream Implications (4 domains: outreach orchestration service — must validate retry_scope before dispatch; channel orchestration — receives channel_exhaustion_state signal only; escalation workflows — ESCALATION_CANDIDATE is handoff signal; observability — retry_scope distribution and cooldown analytics); §14 Unresolved Architectural Concerns (UR-1 through UR-5 specific to retry architecture)
  - Verification: directive document only (no implementation code, no FastAPI services, no schedulers); all INVARIANT-1 through INVARIANT-7 (spec/03 §40) preserved as AP-R rules; FAD-1 through FAD-6 all enforced; hardcoded retry intervals replaced with 8 config-bound threshold variables from rule_set_snapshot; RULE 0 GOVERNANCE PRECONDITION GATE mandatory before all business rules; SHADOW/LIVE separation enforced through retry_constraints.live_retry_permitted in every assessment; 15 architecture-preservation rules (AP-R1 through AP-R15) binding at all scope tiers; retry_scope enum introduces RETRY_ELIGIBLE/RETRY_DEFERRED/RETRY_EXHAUSTED/ESCALATION_CANDIDATE/REPLAY_RETRY_CANDIDATE/NONE replacing legacy next_action enum; CALL→SMS→EMAIL channel sequencing explicitly removed from retry policy and placed in channel orchestration layer; consistent with directives/core_decision_engine.md RETRY_OUTREACH intent semantics and directives/outreach_eligibility_rules.md RETRY scope semantics; 12 acceptance criteria; PROGRESS.md hard gate satisfied
  - Notes: 5 unresolved retry-specific concerns (UR-1 through UR-5): UR-1 (provider_failure_cooldown_extension_hours and provider_failure_retry_limit not confirmed in Config V1 rule_set_snapshot — V1 seed audit required); UR-2 (consecutive_no_response_escalation_threshold not confirmed in Config V1 rule_set_snapshot — same V1 audit); UR-3 (retry_window_behavior ROLLING vs FIXED — architectural decision required; ROLLING resets from last contact, FIXED from cycle epoch; affects systematic retry timing); UR-4 (channel_exhaustion_state source authority unspecified — channel availability service or outreach history; source, update frequency, and staleness threshold undefined); UR-5 (retry policy invocation boundary — Core Decision Engine vs outreach orchestration service; determines who is responsible for intent-blocking vs execution-blocking)

- [x] directives/outreach_eligibility_rules.md — complete rewrite to governance-aware orchestration eligibility architecture
  - Date: 2026-05-25
  - What changed: `directives/outreach_eligibility_rules.md` completely rewritten from 453-line threshold-based outreach eligibility directive (eligible=true/false, hardcoded thresholds, simple exclusion rules) to ~750-line governance-aware orchestration eligibility architecture directive (15 sections). Eligibility reframed from "should we contact this student?" to orchestration candidacy determination. New structure: §1 Purpose (six distinct eligibility categories: operational, orchestration, governance, replay, escalation, AI-assisted prioritization; explicit non-responsibilities defined); §2 Input Model (5 input categories: Operational Context with full outreach/retry/escalation/orchestration history; Academic Context with SQL Server-authoritative field authority annotations; Governance Context including config_version_id, execution_mode, execution_type, fingerprint_context, compliance_hold_flag, replay_context; AI Context with staleness metadata, finalized_copy flag; Attribution Context with historical longitudinal continuity); §3 Output Model (OrchestrationEligibilityAssessment schema replacing eligible: boolean — includes orchestration_eligible, eligibility_scope enum: OUTREACH/RETRY/ESCALATION_CANDIDATE/REPLAY_CANDIDATE/PRIORITIZATION_ONLY/NONE, orchestration_constraints, governance_requirements, observability_metadata, attribution_metadata, replay_restrictions, config_version_reference with threshold_bindings, fingerprint_context, orchestration_priority, ai_context, reason_codes, eligibility_blocked, blocking_reason; output clarifications: advisory not authoritative, no DB writes, no channel selection); §4 Eligibility Rules (12 rules ordered: RULE 0 GOVERNANCE PRECONDITION GATE blocking on config/mode/certification/correlation/attribution/idempotency/replay-context failure; RULE 1 TERMINAL STATE CHECK; RULE 2 COMPLIANCE HOLD GATE producing ESCALATION_CANDIDATE CRITICAL scope; RULE 3 CONFIG-BOUND THRESHOLD RESOLUTION non-terminal resolving 11 named thresholds from rule_set_snapshot; RULE 4 ACCESS STATE GATE with REVOKED→ESCALATION_CANDIDATE/SUSPENDED→NONE bifurcation; RULE 5 STALE AI ASSESSMENT non-terminal producing ai_governance_tier: CONFIDENT/ADVISORY/STALE/IN_FLIGHT/UNAVAILABLE/FINALIZED_COPY; RULE 5A PENDING AI GATE → PRIORITIZATION_ONLY scope; RULE 6 CONTACT DATA GATE; RULE 7 EXCLUSION WINDOW ASSESSMENT config-bound hours-based; RULE 8 OUTREACH CYCLE ASSESSMENT with sub-rules 8A/8B/8C/8D for max attempts/retry/first-outreach/ambiguous; RULE 9 MULTI-FACTOR ACADEMIC SIGNAL ASSESSMENT producing orchestration_priority from 11 risk factors across academic/operational/historical signals; RULE 10 REPLAY ELIGIBILITY GATE forcing REPLAY_CANDIDATE scope; RULE 11 AI-ASSISTED PRIORITIZATION ADJUSTMENT with CONFIDENT/ADVISORY/STALE/UNAVAILABLE/FINALIZED_COPY behaviors; RULE 12 DEFAULT ELIGIBILITY OUTPUT); §5 Config Version Governance (resolution protocol with replay-mode historical-version path; no hardcoded thresholds; config lineage in threshold_bindings output; FAD-3 prospective-only enforcement); §6 Replay & Regeneration Semantics (replay eligibility forcing REPLAY_CANDIDATE scope; regeneration behavior; SHADOW enforcement; replay observability isolation); §7 AI Governance (AI principle: advisory for prioritization only; 6-tier governance tier behavior table; stale AI LIVE mode protection; FAD-1 snapshot isolation; deterministic fallback); §8 Observability & Attribution (18-field log schema; correlation_id propagation; attribution propagation; eligibility trace metadata; PII redaction rules); §9 Failure & Safety Semantics (7 failure scenarios: missing data with governance-critical vs supplementary classification, stale SQL Server data, invalid config resolution, duplicate orchestration detection, replay conflict, stale AI, attribution gap); §10 Architecture Preservation Rules (AP-E1 through AP-E15: no outreach execution, no state mutation, no config mutation, no immutable snapshot mutation, no historical overwrite, no replay corruption, no audit bypass, SQL Server authority boundary, no hardcoded thresholds, no PII in logs, no LIVE scope in replay, no idempotency key reuse, RULE 0 mandatory, compliance hold ESCALATION_CANDIDATE only, no attribution overwrite); §11 Scope Behavior (mandatory-at-all-scopes list; MVP reductions; STANDARD additions; PRODUCTION additions including LIVE certification gate); §12 Acceptance Criteria (AC-E1 through AC-E12 covering deterministic eligibility, replay-safe, config-version reproducibility, attribution continuity, immutable historical protection, stale AI protection, SHADOW isolation, governance precondition blocking, SQL Server authority, compliance hold isolation, contact data gate, idempotency enforcement); §13 Downstream Implications (6 domains: Core Decision Engine, outreach orchestration service, timeline ingestion, snapshot orchestration, report generation, observability, API contracts); §14 Unresolved Architectural Concerns (UE-1 through UE-7 specific to eligibility architecture); §15 References (14 canonical documents with precedence order)
  - Verification: directive document only (no implementation code); all 7 INVARIANT-1 through INVARIANT-7 (spec/03 §40) preserved as AP-E rules; FAD-1 through FAD-6 all enforced; hardcoded thresholds replaced with 11 config-bound threshold variables; governance precondition gate (RULE 0) mandatory before business rules; SHADOW/LIVE separation enforced through orchestration_constraints in every assessment; 15 architecture-preservation rules (AP-E1 through AP-E15) binding at all scope tiers; consistent with directives/core_decision_engine.md OrchestrationIntent semantics; 12 acceptance criteria; PROGRESS.md hard gate satisfied
  - Notes: 7 unresolved eligibility-specific concerns (UE-1 through UE-7): UE-1 (config V1 threshold coverage audit required — all 11 threshold variables must be present in rule_set_snapshot); UE-2 (AI_INSIGHT_TTL_HOURS not confirmed in V1 seed); UE-3 (student_id_opaque hashing scheme — dependency on UO-3); UE-4 (exclusion window hours vs timestamp precision — DST/year-boundary validation required); UE-5 (LIVE certification gate mechanism — feature flag vs config registry entry vs env var unspecified); UE-6 (contact data authority — SQL Server vs GHL opt-out interaction unspecified); UE-7 (AI priority adjustment ceiling behavior at CRITICAL confirmed as no-op)

- [x] directives/core_decision_engine.md — complete rewrite to governance-aware orchestration-intelligence decision architecture
  - Date: 2026-05-25
  - What changed: `directives/core_decision_engine.md` completely rewritten from 379-line legacy outreach trigger engine directive (CALL/SMS/EMAIL action types, LLM meeting trigger, CLOSE_CASE/TRIGGER_OUTREACH outputs, MAX_ATTEMPTS=1/2/3, static retry window) to ~900-line governance-aware orchestration-intelligence decision architecture directive (15 sections). Engine identity reframed: no longer a trigger dispatcher; now an orchestration-intelligence authority producing governance-safe orchestration intents consumed by downstream execution services. New structure: §1 Purpose (engine IS: orchestration intent generation, governance-safe action determination, eligibility interpretation, intervention prioritization, escalation routing, context production; engine IS NOT: direct outreach execution, direct state mutation, compliance execution, report publication, snapshot finalization); §2 Input Model (5 input categories — Operational Inputs including outreach state, retry eligibility, engagement scores; Academic Inputs with SQL Server-authoritative field authority annotations, cohort classification; AI Inputs including ai_governance_tier resolution inputs, stale flag, finalized_copy flag, attribution metadata; Governance Inputs including config_version_id, execution_mode, execution_type original/replay/regeneration, correlation_id, causation_id, fingerprint_context, idempotency_key, compliance_hold_flag; Historical Inputs including prior snapshot months, historical_risk_trend, prior escalation patterns); §3 Output Model (OrchestrationIntent schema with 7 intent values: INITIATE_OUTREACH, RETRY_OUTREACH, ESCALATE_TO_OPERATOR, RECOMMEND_INTERVENTION, DEFER_PENDING_AI, CLOSE_ORCHESTRATION, NO_ELIGIBLE_INTENT; governance_requirements, observability_metadata, escalation_metadata, attribution_metadata, replay_safety_metadata, ai_context fields defined; key output clarifications: advisory not authoritative, no DB writes, no channel selection); §4 Decision Rules (15 rules in ordered evaluation: RULE 0 GOVERNANCE PRECONDITION GATE blocking on config/mode/correlation/idempotency failure; RULE 1 TERMINAL STATE CHECK; RULE 2 COMPLIANCE HOLD CHECK; RULE 3 CONFIG-BOUND THRESHOLD EXTRACTION non-terminal; RULE 4 ACCESS STATE GATE; RULE 5 STALE AI ASSESSMENT producing ai_governance_tier: CONFIDENT/ADVISORY/STALE/IN_FLIGHT/UNAVAILABLE/FINALIZED_COPY; RULE 5A DEFER PENDING AI; RULE 6 MAX ATTEMPTS EXHAUSTION; RULE 7 MULTI-FACTOR RISK ASSESSMENT producing risk_level CRITICAL/HIGH/MEDIUM/LOW from 10 risk factors across academic, historical, AI signals; RULE 8 CRITICAL RISK ESCALATION; RULE 9 HIGH RISK ESCALATION with SHADOW/LIVE bifurcation; RULE 10 AI-ASSISTED INTERVENTION RECOMMENDATION; RULE 11 RETRY ELIGIBILITY ADVISORY; RULE 12 FIRST OUTREACH ELIGIBILITY; RULE 13 REPLAY MODE SAFETY GATE; RULE 14 DEFAULT); §5 Config Version Governance (resolution protocol; config-bound threshold usage — 9 named threshold variables; config lineage in output; historical replay config semantics; FAD-3 prospective-only enforcement); §6 AI Governance (AI principle: advisory only; 6-tier governance tier behavior table; stale AI protection in LIVE mode; FAD-1 snapshot isolation enforcement; deterministic fallback); §7 Replay & Regeneration Semantics (3 execution types table; replay execution rules; regeneration semantics; SHADOW execution semantics; replay traceability); §8 Observability & Attribution (required telemetry per evaluation with 18 log fields; correlation_id propagation; attribution propagation; governance transition log entries; PII redaction rules for all 5 PII categories); §9 Failure & Safety Semantics (6 failure scenarios: missing data, stale SQL Server data, invalid AI, config resolution failure, replay failure, orchestration service rejection — per-field behavior tables); §10 Architecture Preservation Rules (AP-D1 through AP-D15: no DB writes, no outreach dispatch, no state mutation, no compliance execution, no report publication, no snapshot finalization, no config mutation, no LIVE intents in replay, no hardcoded thresholds, no PII in logs, SQL Server authority boundary, no LIVE from stale AI, no attribution overwrite, no idempotency key reuse, RULE 0 mandatory); §11 Scope Behavior (mandatory-at-all-scopes list; MVP reductions permitted and prohibited; STANDARD additions; PRODUCTION additions); §12 Acceptance Criteria (AC-1 through AC-9); §13 Downstream Implications (6 domains: outreach service, timeline ingestion, snapshot orchestration, report generation, observability, API contracts); §14 Unresolved Architectural Concerns (UD-1 through UD-5 specific to the decision engine, referencing US-1, US-2, UO-3, UO-4 from prior sessions); §15 References (14 canonical documents with precedence order)
  - Verification: directive document only (no implementation code, no FastAPI services, no schedulers, no deployment code); all 7 INVARIANT-1 through INVARIANT-7 (spec/03 §40) preserved as explicit prohibitions in AP-D rules; FAD-1 through FAD-6 all enforced (FAD-1 in §6.4 AI snapshot isolation; FAD-2 implied in report/snapshot separation; FAD-3 in §5.5 prospective-only; FAD-4 in RULE 2 compliance hold check; FAD-5 in §2.2 SQL Server authority rule and AP-D11; FAD-6 in §7 fingerprint_context and replay_safety_metadata); engine recast from outreach trigger to orchestration-intelligence authority; SHADOW/LIVE separation enforced through governance_requirements in every intent; config-bound thresholds replace hardcoded values in all 9 threshold variables; AI governance tier system replaces binary LLM output/missing-data handling; 15 architecture-preservation rules (AP-D1 through AP-D15) binding at all scope tiers; 9 acceptance criteria; PROGRESS.md hard gate satisfied
  - Notes: 5 unresolved concerns specific to this directive (UD-1 through UD-5): UD-1 (config rule_set_snapshot must include all 9 threshold variables from Rule 3 — V1 seed must be audited); UD-2 (ai_insight_ttl_hours must be specified in V1 config seed before STALE tier is deterministic); UD-3 (student_id_opaque hashing scheme — dependency on UO-3 from spec/06); UD-4 (escalation delivery mechanism — dependency on UO-4); UD-5 (historical config availability for replay — config version retention policy must guarantee availability for full replay window)

- [x] directives/channel_selection_rules.md — complete rewrite to governance-aware channel orchestration advisory architecture
  - Date: 2026-05-25
  - What changed: `directives/channel_selection_rules.md` completely rewritten from 360-line communication-channel execution strategy directive (CALL→SMS→EMAIL sequencing, hardcoded fallback logic, `selected_channel` string output, scope-gated MVP/STANDARD/PRODUCTION behavior with literal CALL-only/CALL+SMS/full-sequence semantics) to ~800-line governance-aware channel orchestration advisory architecture directive (15 sections). Channel selection reframed from direct routing executor to governance-aware channel advisement for orchestration candidates. New structure: §1 Purpose (six distinct channel advisory categories: contact data channel eligibility, opt-out compliance, provider health eligibility, channel exhaustion state, engagement-signal-aware prioritization, AI-assisted channel prioritization; explicit non-responsibilities: no outbound delivery, no provider dispatch, no retry authorization, no state mutation, no escalation dispatch); §2 Input Model (5 input categories: Communication Context with prior channel history/delivery history/engagement history/retry lineage/escalation lineage; Governance Context with config_version_id/execution_mode/execution_type/fingerprint_context/compliance_hold_flag/replay_context with idempotency key keyed on contact_attempt_count; Attribution Context with historical longitudinal continuity; AI Context with ai_channel_preference_signal/ai_sentiment/ai_governance_tier/staleness metadata/finalized_copy flag; Operational Context with SQL Server-authoritative phone/email/access_state plus platform_supplementary engagement_signal_score/provider_health_state/channel_opt_out_state/channel_delivery_failure_counts/channel_suspension_state/channel_exhaustion_state/ghl_delivery_capability); §3 Output Model (ChannelAdvisoryAssessment schema replacing selected_channel string — includes channel_advisory_produced, advisory_scope enum: CALL_PREFERRED/SMS_PREFERRED/EMAIL_PREFERRED/MULTI_CHANNEL_ELIGIBLE/CHANNEL_EXHAUSTED/REPLAY_CHANNEL_CANDIDATE/NONE, recommended_channel_candidates ordered list, prohibited_channels list, governance_constraints with live_delivery_permitted/compliance_channel_restrictions/opt_out_channel_exclusions/suspended_channel_exclusions, escalation_constraints, replay_restrictions, observability_metadata, attribution_metadata, config_version_reference with threshold_bindings for all 8 config keys, provider_health_metadata, advisory_confidence: LOW/MEDIUM/HIGH/UNKNOWN, ai_context with ai_channel_preference_signal, reason_codes, advisory_blocked, blocking_reason; key clarifications: advisories do not execute delivery, no DB writes, recommended_channel_candidates is ordered list for orchestration sequencing); §4 Channel Advisory Rules (13 rules ordered: RULE 0 GOVERNANCE PRECONDITION GATE blocking on config/mode/certification/correlation/attribution/idempotency/replay-context failure; RULE 1 TERMINAL STATE CHECK; RULE 2 COMPLIANCE HOLD GATE prohibiting all channels under COMPLIANCE_HOLD with CRITICAL escalation; RULE 3 CONFIG-BOUND THRESHOLD RESOLUTION non-terminal resolving 8 named threshold variables from rule_set_snapshot: PREFERRED_INITIAL_CHANNEL, DELIVERY_FAILURE_CHANNEL_SUSPENSION_THRESHOLD, CALL/SMS/EMAIL_ENGAGEMENT_SCORE_THRESHOLD, CHANNEL_SPECIFIC_COOLDOWN_HOURS, AI_INSIGHT_TTL_HOURS, CHANNEL_OPT_OUT_RESPECT_PERIOD_HOURS; RULE 4 CONTACT DATA GATE per-channel — phone absence prohibits CALL+SMS, email absence prohibits EMAIL; RULE 5 AI GOVERNANCE TIER ASSESSMENT non-terminal with FINALIZED_COPY semantics; RULE 5A PENDING AI GATE proceeding without AI rather than deferring (unlike eligibility — channel advisory must not stall orchestration); RULE 6 OPT-OUT AND CONSENT CHANNEL GATE prohibiting opted-out channels and producing CHANNEL_EXHAUSTED scope when all channels prohibited; RULE 7 PROVIDER HEALTH AND CHANNEL SUSPENSION GATE prohibiting UNAVAILABLE providers and deprioritizing DEGRADED providers; RULE 8 CHANNEL EXHAUSTION GATE aggregate check after per-channel constraints; RULE 9 REPLAY CHANNEL GATE forcing REPLAY_CHANNEL_CANDIDATE scope with historical provider health from delivery_history not current state; RULE 10 DELIVERY FAILURE PATTERN ASSESSMENT per-channel suspension when failure counts exceed DELIVERY_FAILURE_CHANNEL_SUSPENSION_THRESHOLD; RULE 11 ENGAGEMENT-SIGNAL-AWARE CHANNEL PRIORITIZATION — 3-step: per-channel responsiveness_score from delivery history, config engagement threshold application, advisory_scope determination from remaining eligible channels; RULE 12 AI-ASSISTED CHANNEL PRIORITIZATION ADJUSTMENT — CONFIDENT moves preferred channel to position 1 unconditionally within permissible candidates, ADVISORY moves conditionally when engagement scores do not contradict, STALE/UNAVAILABLE/FINALIZED_COPY produce no adjustment; AI cannot move prohibited channels into candidates; RULE 13 DEFAULT CHANNEL ADVISORY OUTPUT terminal with advisory_confidence computation and assessment_basis classification); §5 Config Version Governance (resolution protocol; 8 config-bound threshold variables with no hardcoded channel ordering; config lineage in threshold_bindings output; FAD-3 prospective-only enforcement); §6 Replay & Regeneration Semantics (replay forcing REPLAY_CHANNEL_CANDIDATE scope; cooldown/provider state uses historical delivery_history not current provider_health_state in replay; regeneration behavior; SHADOW enforcement; replay observability isolation); §7 AI Governance (AI principle: advisory for channel prioritization ordering only; cannot override opt-out/provider health/delivery failure prohibitions; 6-tier governance tier behavior table; CONFIDENT full adjustment/ADVISORY conditional adjustment/STALE no adjustment; stale AI LIVE mode protection; FAD-1 snapshot isolation; deterministic fallback; IN_FLIGHT does not defer channel advisory unlike eligibility); §8 Observability & Attribution (20-field log schema including recommended_channel_candidates and prohibited_channels; correlation_id propagation; attribution propagation; channel advisory trace metadata; PII redaction rules); §9 Failure & Safety Semantics (9 failure scenarios: missing SQL Server contact data per-channel, stale contact data forcing SHADOW override, invalid config resolution, duplicate advisory detection, replay conflict, stale AI, provider outage including systemic detection from channel_exhaustion_state input, missing opt-out registry state with partial-population handling, provider health partial availability); §10 Architecture Preservation Rules (AP-C1 through AP-C15: no outbound delivery, no state mutation, no config mutation, no immutable snapshot mutation, no historical advisory overwrite, no replay corruption to live-delivery scopes, no audit bypass, SQL Server authoritative contact data boundary, no hardcoded channel priority, no PII in logs, no live-delivery scope in replay, no idempotency key reuse, RULE 0 mandatory, AI cannot override prohibited channels, no attribution overwrite); §11 Scope Behavior (mandatory-at-all-scopes list; MVP reductions including provider_health_state treating all channels HEALTHY, opt-out treating all channels opted-in, engagement_signal_score neutral, delivery failure counts 0; STANDARD additions: AI CONFIDENT/ADVISORY tiers, engagement-signal-aware prioritization, opt-out registry integration, provider health assessment, delivery failure suspension; PRODUCTION additions: LIVE certification gate, live_delivery_permitted=true post-Phase 12, full replay/regeneration semantics, RBAC enforcement); §12 Acceptance Criteria (AC-C1 through AC-C12 covering deterministic channel advisory, replay-safe scope ceiling, config-version reproducibility, attribution continuity, immutable historical protection, stale AI protection, SHADOW isolation, governance precondition blocking, opt-out channel prohibition, provider health channel suspension, channel exhaustion escalation, contact data per-channel gate); §13 Downstream Implications (5 domains: outreach orchestration service consuming ordered recommended_channel_candidates list; provider orchestration receiving candidates for dispatch; channel state feedback loop where delivery failures accumulate in input state across cycles; escalation workflows using CHANNEL_EXHAUSTED scope as handoff signal; retry policy integration — channel_exhaustion_state consistency between retry policy and channel advisory required); §14 Unresolved Architectural Concerns (UC-1 through UC-5); §15 References (10 canonical documents with precedence order)
  - Verification: directive document only (no implementation code, no FastAPI services, no schedulers, no provider API code); all INVARIANT-1 through INVARIANT-7 (spec/03 §40) preserved as AP-C rules; FAD-1 through FAD-6 all enforced; hardcoded CALL→SMS→EMAIL sequencing replaced with 8 config-bound threshold variables from rule_set_snapshot; RULE 0 GOVERNANCE PRECONDITION GATE mandatory before all channel business rules; SHADOW/LIVE separation enforced through governance_constraints.live_delivery_permitted in every advisory; 15 architecture-preservation rules (AP-C1 through AP-C15) binding at all scope tiers; ChannelAdvisoryAssessment output schema introduces advisory_scope enum replacing selected_channel string; CALL→SMS→EMAIL hardcoded fallback logic replaced with ordered recommended_channel_candidates from engagement-signal-aware Rule 11 and AI-assisted Rule 12; consistent with directives/outreach_retry_policy.md channel_exhaustion_state handoff semantics; consistent with directives/core_decision_engine.md INITIATE_OUTREACH/RETRY_OUTREACH intent semantics; 12 acceptance criteria; PROGRESS.md hard gate satisfied
  - Notes: 5 unresolved channel-advisory-specific concerns (UC-1 through UC-5): UC-1 (Config V1 rule_set_snapshot audit required for all 8 new threshold variables — same V1 seed audit required by UR-1/UR-2/UE-1/UD-1); UC-2 (opt-out registry source authority unspecified — GHL webhook vs operator manual entry vs SQL Server consent record — defaults to opted-in when absent); UC-3 (provider health service source unspecified — dedicated health check vs GHL delivery receipts vs outreach history aggregation — MVP treats all channels HEALTHY); UC-4 (channel advisory idempotency key dimension — per-attempt vs per-cycle advisory evaluation not resolved; contact_attempt_count used as 4th key component pending architectural decision on invocation frequency); UC-5 (channel advisory invocation boundary — per-attempt advisory enables real-time provider health reflection within a retry cycle; per-retry advisory is simpler and aligned with retry policy evaluation model; affects Rule 10 delivery failure suspension behavior)

---

## Phase 4 — Operational Outreach Command Center

- [x] Priority scoring engine
  - Date: 2026-05-06
  - What changed: app/services/priority.py — additive numeric score 0–135 mapping to URGENT/HIGH/MEDIUM/LOW; recommended action derived from score + tracking state; reason codes per student
  - Verification: pytest tests/ → 33 passed, 0 failed (7 new priority tests in test_work_queue.py)

- [x] Remove hardcoded PathName/checkpoint validators
  - Date: 2026-05-06
  - What changed: removed VALID_CHECKPOINTS set from schemas.py (TriggerOutreachRequest no longer rejects unknown PathNames); removed CHECKPOINTS set from outreach.py; simplified path filter from POST_COMPLETION special-case to generic `path != checkpoint_type`
  - Verification: test_trigger_outreach_request_accepts_arbitrary_path passes; existing batch logic unchanged

- [x] Source analysis router
  - Date: 2026-05-06
  - What changed: app/routers/source.py — GET /source/paths (distinct PathNames from mirror), GET /source/summary (per-path totals, tracked vs untracked, URGENT/HIGH/MEDIUM/LOW risk distribution)
  - Verification: pytest tests/ → 33 passed; endpoints live at localhost:8080/source/paths and /source/summary

- [x] Work queue router
  - Date: 2026-05-06
  - What changed: app/routers/work_queue.py — GET /work-queue/summary (counts for 7 named queues), GET /work-queue/{queue_name} (paginated, priority-sorted student list with PathName filter); queues: all_source, untracked, eligible, contacted, intervention, retry_due, resolved_closed
  - Verification: pytest tests/ → 33 passed; invalid queue name returns APIResponse.fail

- [x] Batch preview and run router
  - Date: 2026-05-06
  - What changed: app/routers/batch.py — GET /batch/preview (dry-run: per-student decision + priority without executing), POST /batch/run (shadow-safe delegation to outreach service); both carry execution_mode + shadow flags
  - Verification: pytest tests/ → 33 passed

- [x] Register new routers in main.py
  - Date: 2026-05-06
  - What changed: app/main.py imports and registers source_router, work_queue_router, batch_router with tags Source/WorkQueue/Batch
  - Verification: container running; all new endpoints return 200 OK at startup

- [x] Test suite expanded to 33 tests
  - Date: 2026-05-06
  - What changed: tests/test_work_queue.py — 14 new tests covering priority engine (7), source router (2), work queue summary structure (1), invalid queue name (1), batch preview keys (1), schema accepts arbitrary path (2)
  - Verification: pytest tests/ → 33 passed, 0 failed

- [x] Dashboard operational upgrade — Source Analysis + Work Queue + Batch Preview
  - Date: 2026-05-06
  - What changed: frontend/index.html — Operations section added with Source Analysis card (per-path tracked/untracked/risk breakdown) and Work Queue card (7-queue browser with priority-sorted table); Batch Preview modal (decision summary + per-student preview before running); dynamic PathName dropdown in Trigger Outreach (fetches /source/paths, falls back to static list); priority badge component (URGENT/HIGH/MEDIUM/LOW); updated loadAll to call loadSourceSummary + loadWorkQueueSummary; updated toggleAllCards to include new panels
  - Verification: pytest tests/ → 33 passed, 0 failed; container running at localhost:8080

---

## Phase 9 — Config V2 Governance Catalog & Canonical Requirements Rectification

- [x] spec/01_requirements.md — Section 12 rewritten as authoritative Config V2 Governance Catalog
  - Date: 2026-05-26
  - What changed: `spec/01_requirements.md` Section 12 replaced in full — old 130-line V1-era "CONFIGURABLE OPERATIONAL RULES" table replaced with ~580-line 10-section "CONFIG GOVERNANCE CATALOG." New sections: §12.1 Config V2 Governance Semantics (V1 immutability, append-only evolution, exactly-one-ACTIVE invariant, prospective-only activation per FAD-3, rollback prohibition, lineage preservation, authoritative version record fields table); §12.2 Governance Classification Model (7 classes: ARCHITECTURAL_CONSTANT, GOVERNANCE_CONFIGURABLE, OPERATIONAL_TUNING, REPLAY_SENSITIVE_THRESHOLD, AI_ADVISORY_THRESHOLD, ESCALATION_THRESHOLD, PROVIDER_FAILURE_THRESHOLD); §12.3 Canonical V2 Variable Catalog (41 variables across 11 groups A–K covering outreach timing, HW risk, activity, payment, escalation, provider failure, channel advisory, AI/providers, system timing, cohort classification, priority scoring; V1→V2 naming migration map for 7 renamed/split variables); §12.4 Replay-Safe Config Requirements (replay threshold resolution protocol, CONFIG_THRESHOLD_MISSING 6-step behavior, degraded replay semantics with REPLAY_PARTIAL classification, fingerprint mismatch handling, R-EXEC-1 through R-EXEC-5 replay determinism requirements); §12.5 AI Governance Requirements (AI authority boundaries, stale AI governance, AI advisory ceilings, FINALIZED snapshot AI immutability per FAD-1, AI replay semantics); §12.6 Provider Failure Governance (outage handling, retry degradation, cooldown semantics, exhaustion handling, replay-safe provider attribution); §12.7 Escalation Governance Foundations (escalation threshold governance, replay behavior, AI-assist boundaries, cooldown and lineage expectations); §12.8 Config Evolution Strategy (6-step supersession activation workflow, governance approval chain by classification class, activation audit requirements, historical lineage guarantees, future extensibility); §12.9 Acceptance Criteria AC-GOV-1 through AC-GOV-10; §12.10 Open Governance Decisions UG-1 through UG-10 (all 10 resolved)
  - Verification: file modified; Section 12 replaced; 41 canonical V2 variables defined across 11 groups; 7 governance classification classes defined; UG-1–UG-10 all resolved with canonical decisions recorded; consistent with spec/03 config version lifecycle (§31.1 rollback prohibition, §32.1 atomic activation); consistent with spec/04 config idempotency key (§18.1 atomic swap, §18.4 prospective-only, §18.5 rollback prohibition); consistent with spec/05 exactly-one-ACTIVE guarantee for integration consumers (§8.4); consistent with 0003 migration V1 seed (24 V1 variables subsumed by 41 V2 variables); consistent with all 4 directives (core_decision_engine, outreach_eligibility_rules, outreach_retry_policy, channel_selection_rules) Rule 3 threshold variable references
  - Notes: Why — governance stabilization: V1 config table had 8 variables covering only outreach timing and risk thresholds; 11 variable categories across all orchestration domains were absent (provider failure, escalation, channel advisory AI thresholds, replay-safe config behavior, AI governance). Architectural implications: directive Rule 3 rectification (variable renaming/addition in all 4 directives) is now unblocked; migration 0007 V2 seed is now authoritatively defined; replay architecture is fully spec'd with CONFIG_THRESHOLD_MISSING behavior canonical. Replay/governance implications: degraded replay REPLAY_PARTIAL classification defined; escalation-in-replay = logged-not-actioned in SHADOW resolved as UG-8; version records = permanent (UG-9); `sql_server_max_sync_age_hours = 4h` class ARCHITECTURAL_CONSTANT resolved as UG-10. V1→V2 migration: 7 variables renamed or split (payment_balance_risk_threshold → two-tier canonical names; avg_eff_rating_low_risk_threshold directional correction).

---

## Phase 10 — Escalation Orchestration Architecture

- [x] directives/escalation_rules.md — rewritten as governance-aware escalation orchestration architecture
  - Date: 2026-05-26
  - What changed: `directives/escalation_rules.md` completely rewritten. Old 500-line V1 file described simplistic operational escalation logic: hardcoded thresholds (`hws_behind ≥ 3`, `avg_eff_rating < 2.5`, scope-gated MAX_ATTEMPTS tables), binary `escalation_required` output, direct `NOTIFY_OPERATOR` / `FLAG_CASE` action dispatch semantics. New ~1,500-line file transforms escalation into governance-aware orchestration escalation advisement. Key structural changes: (1) PURPOSE reframed — escalation is now orchestration escalation advisement, escalation classification, and candidacy determination; direct dispatch, task creation, mentor assignment, and state mutation explicitly prohibited; (2) INPUT MODEL expanded from 4 fields to 5 full input groups: Student Risk Context (retry exhaustion, consecutive no-response, inactivity, payment, academic signals, prior escalation lineage, mentor history), Governance Context (ACTIVE config version, execution_mode, execution_type, compliance_hold, replay context, fingerprint), Attribution Context (origin_source, correlation_id, causation_id, upstream_intent), AI Context (governance tier, confidence, stale insight, finalized copy flag), Operational Context (provider outage, automation degradation, escalation cooldown, circuit breaker state); (3) OUTPUT MODEL replaced binary flag with full EscalationOrchestrationAssessment schema carrying escalation_candidate, escalation_type (12 values), escalation_severity, escalation_constraints, replay_restrictions, governance_requirements, observability_metadata, attribution_metadata, escalation_lineage_metadata, config_version_reference, ai_context, reason_codes; (4) RULES rewritten: RULE 0 governance precondition gate (blocks on null config, invalid execution_mode, missing correlation_id, incomplete attribution, duplicate idempotency_key); RULE 1 terminal state check (CLOSED + INTERVENTION_REQUIRED); RULE 2 compliance hold gate (CRITICAL severity, stops evaluation); RULE 3 config-bound threshold resolution (13 V2 canonical keys resolved from rule_set_snapshot, UNKNOWN_V0 substitution for missing, CONFIG_THRESHOLD_MISSING warning); RULE 4 replay mode gate (enforces SHADOW, REPLAY_ESCALATION_CANDIDATE scope, historical config resolution); RULE 5 AI governance tier assessment (CONFIDENT/ADVISORY/STALE/IN_FLIGHT/UNAVAILABLE/FINALIZED_COPY); RULES 6–13 risk signal accumulation (retry exhaustion, consecutive non-response, payment risk AT_RISK/HIGH_RISK, inactivity AT_RISK/HIGH_RISK, academic risk AT_RISK/HIGH_RISK homework + effort, provider failure/channel exhaustion, escalation recurrence, compliance-driven access revocation); RULE 14 AI-confidence severity adjustment (CONFIDENT tier can upgrade to CRITICAL; ADVISORY tier can upgrade one level; AI cannot originate candidacy without rule basis — AP-X14); RULE 15 multi-factor severity aggregation (3+ HIGH → CRITICAL, 2 HIGH → HIGH, etc.); RULE 16 terminal output production; §4B operator-initiated escalation path preserved with governance constraints; (5) Sections 5–9: Config Version Governance (resolution protocol, threshold binding, lineage in output, prospective-only FAD-3), Replay & Regeneration Semantics (SHADOW enforcement, REPLAY_ESCALATION_CANDIDATE scope ceiling, historical config, fingerprint comparison, observability isolation), AI Governance (tier behavior table, stale AI protection in LIVE mode, FAD-1 snapshot isolation, deterministic fallback), Observability & Attribution (required telemetry fields, escalation lineage traceability, attribution propagation, PII redaction), Failure & Safety Semantics (missing data table, stale SQL Server data, config resolution failure, duplicate detection, replay conflict table, stale AI behavior, provider outage handling, attribution gap); (6) AP-X1 through AP-X15 architecture preservation rules; (7) Scope behavior (MVP/STANDARD/PRODUCTION tiered constraints); (8) AC-X1 through AC-X12 governance-focused acceptance criteria; (9) UE-1 through UE-5 unresolved concerns (escalation ownership, mentor routing authority, SLA semantics, human override lineage, AI escalation trust ceilings); (10) §14 downstream implications; §15 references to all 9 spec files + all 4 directives + FAD-1 + FAD-3.
  - Verification: file written; old escalation_rules.md replaced; 16 core rules defined; 12 escalation type values defined; 7 escalation assessment categories defined; 15 architecture preservation rules (AP-X1–AP-X15) defined; 12 acceptance criteria (AC-X1–AC-X12) defined; 5 unresolved concerns (UE-1–UE-5) classified; consistent with spec/03 §6.3 (manual ESCALATE valid/blocked states preserved); consistent with spec/01 §12 Group E (prior_escalation_repeat_threshold, consecutive_no_response_escalation_threshold), Group B/C/D (risk thresholds), Group F (provider failure), Group H (AI TTL) using V2 canonical variable names; consistent with directives/core_decision_engine.md Rule 0 governance precondition gate pattern; consistent with directives/outreach_retry_policy.md AP-R1–AP-R15 pattern; consistent with directives/outreach_eligibility_rules.md AP-E1–AP-E15 pattern; operator-initiated ESCALATE path preserved per spec/03 §6.3.
  - Notes: Why — escalation_rules.md was the last directive in the governance-aware orchestration rewrite sequence. The three upstream directives (eligibility, retry, core_decision_engine) all reference escalation candidacy in their outputs; this directive closes the loop by specifying how those escalation candidacy signals are classified into severity, type, and replay-safe advisory outputs. Architectural implications: escalation routing service may now consume EscalationOrchestrationAssessment to drive operator routing without re-implementing escalation logic; AP-X14 (AI cannot originate escalation candidacy without rule basis) prevents AI-only escalation spikes from stale AI refreshes; UE-5 (AI escalation trust ceilings) is the primary open governance decision before PRODUCTION scope AI-assisted escalation is enabled. Replay implications: all escalation advisory outputs in replay are SHADOW-only REPLAY_ESCALATION_CANDIDATE; no historical escalation lineage is mutated; historical provider state sourced from input records not live circuit breaker.

---

## Phase 11 — AI Orchestration Contract Architecture

- [x] directives/llm_processing_contract.md — rewritten as governance-safe AI orchestration contract architecture
  - Date: 2026-05-26
  - What changed: `directives/llm_processing_contract.md` completely rewritten. Old 326-line V1 file described a simple LLM integration contract: transcript ingestion (4-field input), hardcoded output schema (sentiment/issues/meeting_recommended/ipbc_interest_level), binary retry-once/fallback handling, simplistic scope-gated behavior, no governance context. New ~1,400-line file transforms the contract into governance-safe AI orchestration contract architecture. Key structural changes: (1) PURPOSE reframed — AI is now an advisory intelligence layer (prioritization assistant, summarization engine, enrichment provider, narrative generation assistant, orchestration support layer); §1.3 explicitly prohibits AI from acting as workflow authority, governance decision-maker, autonomous escalation trigger, or state mutator; 7 AI advisory categories defined; (2) AI GOVERNANCE TIERS — canonical 6-tier model defined (CONFIDENT, ADVISORY, STALE, IN_FLIGHT, UNAVAILABLE, FINALIZED_COPY) with per-tier: allowed advisory scope, replay behavior, LIVE escalation restriction, degradation expectation; tier precedence ordering (FINALIZED_COPY wins over all); config-bound tier boundaries (ai_insight_ttl_hours, ai_llm_provider from Group H); (3) INPUT MODEL expanded from 4 fields to 5 groups: Governance Context (config_version_id, execution_mode, execution_type, replay context, compliance_hold, fingerprint, idempotency_key), Attribution Context (origin_source, origin_authority, actor_identity, attribution_timestamp, upstream_intent_id, upstream_correlation_id), Historical Context (prior AI versions, snapshot lineage, FINALIZED_COPY reference, replay lineage, sentiment trend), AI Context (ai_insight_id, state, version, generated_at, confidence_score, prompt_version, model_used, stale_flag, finalized_copy, metrics_hash, risk_summary, recommended_intervention, sentiment), Operational Context (provider_degradation_state, inference_retry_count, failure type/timestamp, orchestration_degradation_mode, circuit_breaker_state, retry_budget_remaining); (4) OUTPUT MODEL replaced simple output blob with full AIAssessment schema: ai_governance_tier, advisory_scope (FULL/LIMITED/UNAVAILABLE/IMMUTABLE/DEFERRED), advisory_content (partial population by scope), replay_restrictions, attribution_metadata, config_version_reference, model_version_metadata, confidence_metadata (score/tier/stale_flag/stale_for_hours/insight_age_hours/ttl_applied), degradation_metadata (degraded/cause/fallback_type/orchestration_continues), observability_metadata, immutable_snapshot_eligibility, reason_codes, ai_blocked, blocking_reason; (5) RULES: RULE 0 governance precondition gate; RULE 1 FINALIZED_COPY isolation (FAD-1 enforcement); RULE 2 replay mode gate (AP-AI5 enforcement, historical config resolution); RULE 3 config-bound threshold resolution (ai_insight_ttl_hours, ai_llm_provider; UNKNOWN_V0 substitution); RULE 4 AI context availability check (null insight → UNAVAILABLE; IN_FLIGHT → DEFERRED with prior-version fallback); RULE 5 governance tier assignment (CONFIDENT ≥ 0.70 + fresh; ADVISORY < 0.70; STALE > TTL; confidence tier HIGH/MEDIUM/LOW); RULE 6 advisory scope determination (FULL/LIMITED/IMMUTABLE/DEFERRED/UNAVAILABLE per tier); RULE 7 LIVE escalation restriction (STALE/UNAVAILABLE blocked from LIVE escalation, AP-AI11); RULE 8 provider validation and degradation (circuit breaker, retry exhaustion, provider identity divergence); RULE 9 attribution and lineage assembly (AP-AI15 propagation); RULE 10 snapshot eligibility determination (CONFIDENT + original + not-finalized + fresh + no-divergence + no-hold); RULE 11 terminal output production (observability assembly); (6) §6 Config Version Governance: AI TTL governance (OPERATIONAL_TUNING, prospective-only); provider governance (ARCHITECTURAL_CONSTANT, no retroactive attribution change); confidence ceiling governance (no dedicated config key yet; UE-AI2 open); FAD-3 prospective-only activation; (7) §7 Replay & Regeneration Semantics: live inference suppressed in replay/regeneration; FINALIZED_COPY exclusive sourcing; POTENTIALLY_DIVERGENT annotation for fingerprint mismatch; immutable FINALIZED_COPY semantics (FAD-1); historical AI explainability requirements; (8) §8 Provider Governance: provider attribution rules (forward-only, model_used immutable after AI_GENERATED); degradation handling (explicit, no silent fallback); outage semantics (snapshot not blocked; last AI_REVIEWED served stale); replay-safe provider attribution; (9) §9 Observability & Attribution: required telemetry fields (structured JSON log, no PII); inference lineage traceability (correlation_id/causation_id/ai_version_number chain); attribution continuity (AP-AI15); replay diagnostics (source_artifact_id, historical_config_version, fingerprint_comparison_outcome, replay_partial); provider diagnostics (request start/end, duration, status code, error body); confidence observability (6 required fields); (10) §10 Deterministic Degradation Rules: 11-row degradation table covering circuit breaker, provider unavailable, inference timeout, stale AI, missing insight, confidence collapse, in-flight, replay missing source, provider divergence, config threshold missing — all with explicit tier/scope/fallback/behavior/observability; (11) §11 Failure & Safety Semantics: missing data handling, stale SQL Server data annotation, duplicate detection, replay conflict handling, attribution gap handling; (12) AP-AI1 through AP-AI15 architecture preservation rules (no governance override, no dispatch, no state mutation, no replay corruption, no FINALIZED_COPY mutation, no silent fallback, no hardcoded thresholds, no PII logging, no LIVE escalation with STALE AI, no re-inference for FINALIZED_COPY, no evaluation without Rule 0, no confidence-alone override, no attribution override); (13) §13 Scope behavior (MVP/STANDARD/PRODUCTION tiered constraints); (14) AC-AI1 through AC-AI12 governance-focused acceptance criteria; (15) UE-AI1 through UE-AI5 unresolved concerns (provider trust ceilings, confidence calibration, hallucination containment, prompt governance, model upgrade governance); (16) §16 downstream implications (Core Decision Engine, escalation_rules, snapshot orchestration, report orchestration); §17 references (all 9 spec files + all 4 directives + FAD-1 + FAD-3).
  - Verification: file written; old llm_processing_contract.md replaced; 6-tier AI governance model defined; 11 evaluation rules defined; 7 AI advisory categories defined; 15 architecture preservation rules (AP-AI1–AP-AI15) defined; 12 acceptance criteria (AC-AI1–AC-AI12) defined; 5 unresolved concerns (UE-AI1–UE-AI5) classified; consistent with spec/01 §12.5 (AI authority boundaries, stale AI governance, advisory ceilings, FAD-1 snapshot immutability, AI replay semantics); consistent with spec/01 §12.6 (provider failure governance, retry degradation, replay-safe provider attribution); consistent with spec/01 Group H (ai_insight_ttl_hours, ai_llm_provider); consistent with directives/core_decision_engine.md §2.3 AI Inputs and DEFER_PENDING_AI intent; consistent with directives/escalation_rules.md Rules 5 and 14 (AI tier and confidence consumption); consistent with FAD-1 (physical copy immutability) and FAD-3 (prospective-only activation).
  - Notes: Why — llm_processing_contract.md was the remaining V1-era directive containing hardcoded AI integration guidance incompatible with the governance-aware orchestration architecture. The 5 rewritten directives all reference AI governance tier, advisory scope, and FINALIZED_COPY semantics; this directive closes the loop by specifying the authoritative AI governance contract that all consuming services derive from. AI governance implications: AP-AI14 (AI cannot originate escalation candidacy without rule basis) mirrors escalation AP-X14, creating a consistent boundary across both directives; UE-AI2 (confidence calibration) is the primary open governance decision before confidence thresholds become config-governed; UE-AI3 (hallucination containment) is the open implementation dependency before PRODUCTION advisory content serving is enabled without quality audit. Replay implications: AP-AI5 enforces no live inference in replay — FINALIZED_COPY is the exclusive source; Rule 2 gates all replay/regeneration paths before any other rule fires; this prevents replay from calling AI providers even if FINALIZED_COPY is missing (UNAVAILABLE tier fallback instead). Immutable snapshot implications: FAD-1 is enforced by both Rule 1 (FINALIZED_COPY isolation) and AP-AI6/AP-AI12; no code path through this directive can reach or modify warehouse.snapshot_ai_narratives — that remains exclusively the snapshot orchestration service's responsibility.

---

## Phase 12 — External Provider Orchestration Contract Architecture

- [x] directives/ghl_integration_contract.md — rewritten as governance-safe external provider orchestration contract architecture
  - Date: 2026-05-26
  - What changed: `directives/ghl_integration_contract.md` completely rewritten. Old 389-line V1 file described a simplistic CRM integration guidance contract: direct GHL trigger payload (10-field JSON), hardcoded RULE 1–5 validation checks, simple webhook state transitions (CALL_COMPLETED → CONTACTED/NO_RESPONSE), retry-once error handling, 4-case acceptance criteria, no governance context, no replay semantics, no config version binding. New ~900-line file transforms GHL integration into governance-safe external provider orchestration contract architecture. Key structural changes: (1) PURPOSE reframed — GHL is now defined as operational communication provider, execution telemetry provider, engagement metadata provider, delivery-state integration surface; §1.3 explicitly prohibits GHL from acting as governance authority, orchestration authority, historical source-of-truth, replay authority, snapshot authority, escalation authority, config authority; 5 provider interaction categories defined with per-category replay/SHADOW/degradation semantics; (2) EXTERNAL AUTHORITY BOUNDARIES (§2) — system authority classification table for all 6 platform components; authoritative vs derivative distinction with conflict resolution rules; operational vs historical ownership table per layer; synchronization vs mutation semantics clarified; GHL permanently classified as `platform_supplementary`, `is_authoritative = false`; (3) INPUT MODEL expanded from 10-field payload to 5 full input groups: Governance Context (config_version_id, execution_mode, execution_type, replay_restrictions, compliance_hold_active, fingerprint_context, idempotency_key, rule_set_snapshot), Attribution Context (origin_source, origin_authority, correlation_id, causation_id, upstream_intent_id, actor_identity), Provider Context (provider_account_id, provider_health_state, circuit_breaker_state, delivery_state, ghl_message_id, webhook_event_type, webhook_payload_valid, retry_attempt_count, provider_failure_history), Historical Context (snapshot_lineage, replay_source_artifact_id, escalation_lineage, orchestration_lineage, prior_outreach_event_ids, prior_provider_correlation_ids), Operational Context (provider_outage_active, orchestration_degradation_mode, sync_lag_hours, max_sync_age_hours, delivery_telemetry_stale, duplicate_event_detected, outbound_retry_budget_remaining); (4) OUTPUT MODEL replaced raw webhook/trigger handling with full ProviderOrchestrationAssessment schema: provider_event_type (9 values), governance_scope (6 values: AUTHORIZED/BLOCKED/SHADOW_ONLY/REPLAY_ONLY/DUPLICATE_SUPPRESSED/UNAVAILABLE), replay_restrictions, attribution_metadata, config_version_reference (with threshold_bindings for all 6 Group F/K variables), provider_lineage_metadata, synchronization_metadata, degradation_metadata, observability_metadata, reason_codes, provider_blocked, blocking_reason; (5) RULES: RULE 0 governance precondition gate (6 block conditions: null config_version_id, invalid execution_mode, null correlation_id, missing attribution, duplicate idempotency_key, invalid execution_type); RULE 1 replay/regeneration gate (AP-GHL5 enforcement: outbound suppressed, REPLAY_SIMULATION events, historical config resolution, historical records only); RULE 2 execution mode gate (SHADOW suppresses live outbound; inbound webhook ingested in SHADOW); RULE 3 config-bound threshold resolution (6 Group F/K variables; UNKNOWN_V0 substitution; replay historical config); RULE 4 provider health assessment (circuit breaker CLOSED/OPEN/HALF_OPEN/UNKNOWN; outage check; retry budget check; outbound-vs-inbound asymmetry); RULE 5 outbound authorization check (upstream_intent_id required; intent type INITIATE_OUTREACH or RETRY_OUTREACH; compliance hold gate); RULE 6 idempotency and deduplication (outbound: (user_id, checkpoint_type, contact_attempt) key; inbound: ghl_message_id; campaign activity: 4-tuple; all dedup results logged — no silent drops); RULE 7 attribution and lineage validation (origin_source/authority/is_authoritative set by platform not GHL; attribution_timestamp from event occurrence; lineage propagation); RULE 8 inbound webhook event classification (schema validation; semantic validation; 5 event types with target tables, dedup keys, required fields; unknown event type buffering); RULE 9 synchronization state assessment (sync lag vs sql_server_max_sync_age_hours; delivery telemetry stale flag); RULE 10 provider lineage and observability assembly; RULE 11 terminal output production; (6) §6 Config Version Governance: 6-variable threshold table (Group F PROVIDER_FAILURE_THRESHOLD + OPERATIONAL_TUNING; Group K ARCHITECTURAL_CONSTANT); no hardcoded values; FAD-3 prospective-only; replay historical config resolution; config version reproducibility in provider attribution; (7) §7 Replay & Regeneration Semantics: replay-safe provider behavior (idempotency + dedup + RULE 1); outbound suppression absolute (AP-GHL5); historical provider resolution from ghl_messages/outreach_history only; immutable provider lineage (append-only records); replay visibility rules table; historical explainability requirements (5 chain fields); (8) §8 Provider Degradation Governance: outage handling (UNAVAILABLE for outbound; inbound unaffected; platform reads continue); degraded mode (circuit accumulation continues); retry collapse (RETRY_EXHAUSTED → escalation candidacy); sync backlog (annotation only, not blocking); duplicate event handling (200 acknowledgement; audit entry; no silent drops); (9) §9 Outbound Authority Controls: full governance chain diagram (spec/01 Config V2 → eligibility → retry → CDE → channel → orchestration service → this directive → GHL API); orchestration-vs-provider responsibilities table; provider execution boundaries; SHADOW/LIVE restrictions table (inbound ingestion unaffected by SHADOW); replay outbound prohibition absolute; (10) §10 Observability: required JSON log schema with 12 fields; PII redaction (AP-GHL10); sync lineage fields; attribution continuity; replay diagnostics (4 fields); webhook diagnostics (7 fields); delivery observability (7 metrics); (11) §11 Deterministic Degradation Rules: 11-row table (circuit open, provider outage, retry exhausted, replay, SHADOW, outbound duplicate, webhook duplicate, governance blocked, compliance hold, webhook invalid, config missing) — all with explicit governance_scope/event_type/cause/orchestration_continues/observability; (12) §12 Failure & Safety Semantics: missing governance context; auth failure (TERMINAL class; no retry); duplicate detection audit; replay conflict table (3 conflict types); attribution gap handling; (13) AP-GHL1 through AP-GHL15 architecture preservation rules (no governance override, no direct schema writes, no autonomous state transitions, no governance invariant override, no live outbound in replay, no snapshot mutation, append-only records, no silent provider substitution, no hardcoded thresholds, no PII logging, no GHL-delivery-alone governance, no unauthorized dispatch, no uninspected webhook ingestion, no silent auth failure, no attribution field override); (14) §14 Scope behavior (MVP/STANDARD/PRODUCTION tiered); (15) AC-GHL1 through AC-GHL12 governance-focused acceptance criteria; (16) UE-GHL1 through UE-GHL6 unresolved concerns (provider lock-in, webhook trust boundaries, cross-provider failover, delivery telemetry consistency, opt-out authority conflicts, synchronization SLA governance); (17) §17 downstream implications (6 services, 5 scheduler jobs, 4 ETL jobs); (18) §18 references to all 9 spec files + all 6 directives + FAD-1/FAD-2/FAD-3.
  - Verification: file written; old ghl_integration_contract.md replaced; 11 evaluation rules defined (RULE 0–RULE 11); 6 governance scope values defined; 15 architecture preservation rules (AP-GHL1–AP-GHL15) defined; 12 acceptance criteria (AC-GHL1–AC-GHL12) defined; 6 unresolved concerns (UE-GHL1–UE-GHL6) classified; consistent with spec/05 §5.1–5.7 (GHL ownership boundaries, attribution preservation, deduplication, replay handling); consistent with spec/05 §11.2 (AUTH_FAILURE TERMINAL class); consistent with spec/04 outreach domain idempotency key (user_id, checkpoint_type, contact_attempt); consistent with spec/01 §12.6 Group F provider failure thresholds; consistent with spec/09 lineage integrity protection and PII requirements; consistent with directives/core_decision_engine.md (INITIATE_OUTREACH/RETRY_OUTREACH intent authorization chain); consistent with directives/escalation_rules.md (provider failure escalation candidacy Rule 6); consistent with FAD-2 (no GHL API call during report generation) and FAD-3 (prospective-only config).
  - Notes: Why — ghl_integration_contract.md was the last V1-era directive containing simplistic provider integration guidance incompatible with the governance-aware orchestration architecture. The six upstream directives all reference provider health state, delivery failure lineage, and outbound authorization; this directive closes the provider boundary by specifying who may authorize outbound dispatch, how inbound events are attributed and deduplicated, and how provider degradation remains fully observable without silent fallback. Provider governance implications: AP-GHL8 (no silent provider substitution) and AP-GHL12 (no unauthorized dispatch) create a hard boundary preventing ad-hoc or bypass GHL triggers from outside the governance chain; provider outage is now a declared, logged, governance-visible state rather than an exception swallowed by the integration layer. Replay implications: AP-GHL5 (no live outbound in replay) mirrors AP-AI5 from llm_processing_contract.md — both provider surfaces are now governed by the same replay suppression principle; RULE 1 fires before authorization check to prevent replay from inadvertently reaching the dispatch path even if upstream_intent_id is present. Synchronization implications: the absence of a `ghl_event_sync_max_lag_hours` config variable (UE-GHL6) is the primary open governance decision before SLA-bound sync lag alerting can be implemented in PRODUCTION.

---

## Phase 13 — Operational Authority Architecture

- [x] directives/agent_ownership_matrix.md — rewritten as governance-safe operational authority architecture
  - Date: 2026-05-26
  - What changed: `directives/agent_ownership_matrix.md` completely rewritten. Old 346-line V1 file described a simplistic agent responsibility matrix: generic role labels (Outreach Agent, Escalation Agent, Scoring Agent, etc.), single-owner assignment table, 12-row ownership matrix, no governance context, no replay semantics, no authority boundaries. New ~1,100-line file transforms the matrix into governance-safe operational authority architecture. Key structural changes: (1) PURPOSE reframed — §1.1 defines Operational Authority Architecture as bounded authority model for all actors (human and automated); §1.2 explicitly states ownership is NOT generic responsibility mapping, NOT static role assignment, NOT operational convenience assignment, NOT mutable historical ownership, NOT provider authority, NOT AI governance authority; §1.3 defines 5 explicit authority scope types (authority, approval, override, operational, governance scope) each with mutability classification; (2) AUTHORITY DOMAINS (§2) — 9 canonical authority domains defined with human-vs-automation-vs-external-bounded-vs-advisory-only classification: Governance Authority (human-owned, Governance Administrator, never automation), Orchestration Authority (automation-owned, Core Decision Engine advisory + Orchestration Services execution), Execution Authority (automation-owned, under governance authorization), Replay Authority (automation-owned, SHADOW-only), Provider Authority (external-bounded, execution telemetry delivery only), AI Advisory Authority (advisory-only, no authorization), Escalation Authority (human-owned, automation may signal only), Compliance Authority (human-owned, automation may never initiate), Operational Override Authority (human-owned, audit-obligated); (3) ACTOR CLASSES (§3) — 11 actor class definitions each with full per-class specification: allowed actions, prohibited actions, replay restrictions, escalation authority, audit obligations, attribution requirements: Governance Administrator, Operational Administrator, Escalation Operator, Mentor, Super Mentor, AI Advisory Systems, Core Decision Engine, Orchestration Services, Provider Systems (GHL), Replay Services, Compliance Operators; (4) AUTHORITY MATRIX (§4) — 6 subsections: Platform State Write Authority, Outbound Communication Dispatch Authority, Warehouse Write Authority, Config Version Activation Authority, Compliance Action Authority, Escalation Action Authority — each with explicit per-actor class permissions; §4.4 Human Override Authority definitions per actor class; (5) HUMAN VS AUTOMATION BOUNDARIES (§5) — §5.1 what may be human-overridden (8 override paths with approval requirements); §5.2 what requires human approval (6 approval-gated actions); §5.3 8-row immutable boundaries table: FINALIZED snapshot content (FAD-1, spec/09 §1.2), AI text in snapshot_ai_narratives (FAD-1, AP-AI6, AP-OWN7), config version rollback prohibition (spec/01 §12.1, spec/04 §18.5), attribution field immutability (spec/09 §1.3, AP-GHL15, AP-OWN8), compliance hold bypass for outbound dispatch, SHADOW-to-LIVE execution escalation in replay context, append-only warehouse and compliance_audit schemas, processed_events audit entries; (6) REPLAY AUTHORITY MODEL (§6) — §6.1–6.5: Replay Services defined as SHADOW-only, REPLAY_ONLY governance scope, no live outbound, no live state transitions; §6.2 historical config version resolution protocol; §6.3 authority restrictions in replay (7 explicit prohibitions); §6.4 Replay Observability Ownership (Replay Service owns diagnostic record: source_artifact_id, historical_config_version_id, fingerprint_comparison_outcome, replay_partial); §6.5 Regeneration Restrictions (read from warehouse exclusively, produces identical output for identical inputs, does not modify any warehouse row — regeneration ≠ replay); (7) ESCALATION OWNERSHIP GOVERNANCE (§7) — §7.1 acknowledgement authority by actor class; §7.2 closure authority per escalation tier; §7.3 override authority (NOT_ACTIONABLE + downgrade with mandatory justification); §7.4 7-field escalation audit requirements; §7.5 replay restrictions (REPLAY_ESCALATION_CANDIDATE scope ceiling; no routing/acknowledgement/closure from replay-mode escalation); (8) AI AUTHORITY LIMITS (§8) — §8.1 advisory-only boundaries table per AI output field (recommended_intervention, prioritization_signal, sentiment_trend, confidence_score, sentiment); §8.2 8 prohibited AI actions (no authorization, no SQL Server override, no rule evaluation override, no escalation without rule basis, no live inference in replay, no FINALIZED_COPY mutation, no config authority, no workflow authority); §8.3 confidence governance (CONFIDENT ≥ 0.70 / ADVISORY 0.40–0.69 / STALE / UNAVAILABLE tier table); §8.4 stale-AI restrictions (rule-based evaluation only; snapshot not blocked; operator surface must show stale signal); §8.5 FINALIZED_COPY immutability; (9) PROVIDER AUTHORITY LIMITS (§9) — §9.1 execution scope table (8 actions: permitted/prohibited with authority domain); §9.2 synchronization ownership (platform governs, GHL pushes telemetry, no direct table writes); §9.3 webhook attribution ownership (platform ingestion service sets all attribution fields, GHL payload fields ignored for governance); §9.4 provider audit obligations; (10) OVERRIDE GOVERNANCE (§10) — §10.1 5-condition allowed override paths; §10.2 8-row prohibited override paths; §10.3 10-field mandatory override audit record (override_id, actor_identity, action_type, student_id, prior_state, new_state, attribution_timestamp, correlation_id, override_reason, ai_governance_tier_at_time) — append-only to student_timeline_events; §10.4 override replay restrictions; §10.5 6-table immutable-state override restrictions; (11) OBSERVABILITY & ATTRIBUTION (§11) — §11.1 required authority traceability JSON log schema (13 fields including actor_domain, override_of); §11.2 full attribution chain requirements; §11.3 override_of lineage field enabling forensic reconstruction; §11.4 replay diagnostic set (4 fields); §11.5 escalation traceability end-to-end (signal → routing → acknowledgement → closure); §11.6 governance audit continuity (config activation, compliance audit, governance escalation resolution); (12) AP-OWN1 through AP-OWN15 architecture preservation rules; (13) §13 scope behavior (MVP/STANDARD/PRODUCTION tiered); (14) AC-OWN1 through AC-OWN12 governance-focused acceptance criteria; (15) UE-OWN1 through UE-OWN5 unresolved concerns (emergency override authority, multi-admin conflict resolution, Mentor escalation ownership, AI escalation trust ceiling, emergency override implementation); (16) §16 downstream implications (8 services, 8 API endpoints, 5 audit consumers); (17) §17 references to all 9 spec files + all 6 directives + FAD-1/FAD-3.
  - Verification: file written; old agent_ownership_matrix.md replaced; 9 authority domains defined; 11 actor classes defined with full per-class specifications; §5.3 8-row immutable boundaries table defined; §6 Replay Authority Model with SHADOW-only enforcement and regeneration distinction defined; §10 Override Governance with 10-field mandatory audit record defined; AP-OWN1–AP-OWN15 architecture preservation rules defined; AC-OWN1–AC-OWN12 acceptance criteria defined; UE-OWN1–UE-OWN5 unresolved concerns classified. 6-point audit: (1) no authority escalation semantics — AP-OWN1 prohibits self-grantable authority, §4 matrix explicit per-actor bounds — PASS; (2) no replay violations — AP-OWN2 explicit LIVE prohibition, §6 SHADOW-only, AC-OWN2 test — PASS; (3) no hidden override behavior — §10.1–10.5 mandatory audit record, AP-OWN9 no silent closure, §11.3 override_of lineage — PASS; (4) no provider governance elevation — AP-OWN4/AP-OWN13, §9.1 table, AC-OWN6 test — PASS; (5) no AI governance elevation — AP-OWN3/AP-OWN11, §8.1 advisory-only table, §8.5 FINALIZED_COPY immutability, AC-OWN7 test — PASS; (6) no broken lineage guarantees — §11.1–11.6 observability requirements, AP-OWN8 attribution preservation — PASS; (7) no governance invariant violations — §5.3 immutable boundaries, AP-OWN5/6/7/15 — PASS.
  - Notes: Why — agent_ownership_matrix.md was the final directive in the governance-safe rewrite sequence. All 6 upstream directives reference actor classes, authority domains, and override semantics; this directive closes the authority model by specifying who may act on orchestration outputs, what actions are immutably prohibited across all actor classes, and how every authority action is traced from origin to closure. Authority governance implications: AP-OWN1 (no self-grantable authority) and AP-OWN6 (state writes only under valid OrchestrationIntent) create the outer boundary preventing any actor — human or automated — from bypassing the governance chain for state mutations; the 8-row immutable boundaries table (§5.3) canonicalizes all the individual FAD/spec immutability guarantees from prior directives into one authoritative reference. Replay implications: AP-OWN2 (no LIVE operations from Replay Services) mirrors AP-GHL5 and AP-AI5, completing the replay isolation guarantee across all three provider/advisory surfaces; §6.5 (Regeneration Restrictions) distinguishes regeneration (re-render from immutable snapshot) from replay (simulate historical orchestration decisions), resolving the ambiguity between these two read-only historical access patterns. Override governance implications: §10.3 mandatory 10-field override audit record ensures no override is silent; the override_of field (§11.3) creates the forensic reconstruction path — "automation decided X; operator overrode to Y" — permanently queryable without reconstructing from fragmented event logs; §10.5 immutable-state override restrictions explicitly block application-layer override paths for FINALIZED warehouse rows, compliance_audit entries, and processed_events records, reinforcing the database-layer enforcement from spec/09 §7–8. The governance directive rewrite sequence (Phases 9–13) is now complete: spec/01 Config V2 → escalation_rules → llm_processing_contract → ghl_integration_contract → agent_ownership_matrix. All 7 directives in /directives now follow the governance-safe architecture pattern.

---

## Phase 14 — Runtime Orchestration Architecture

- [x] runtime/system_loop.md — rewritten as governance-safe runtime orchestration architecture
  - Date: 2026-05-26
  - What changed: `runtime/system_loop.md` completely rewritten. Old 379-line V1 file described MVP-era execution flow: direct execution sequence (Execute Action → GHL / Internal), hardcoded action dispatch semantics (SEND_SMS, SEND_EMAIL, BOOK_MEETING, "Send payload to GHL"), hardcoded retry counts (MAX_ATTEMPTS), hardcoded channel progression (CALL→SMS→EMAIL), hardcoded concurrency limits (50 concurrent triggers), mutable operational sequencing with no replay semantics, no Config V2 governance, no SHADOW/LIVE distinction, no attribution requirements. New ~1,300-line file transforms the system loop into governance-safe runtime orchestration architecture. Key structural changes: (1) PURPOSE reframed — §1.1 defines authoritative parent runtime contract for daily_outreach_flow.md, retry_cycle_flow.md, and scheduler_design.md; §1.2 explicitly states runtime is NOT direct execution sequencing, NOT workflow execution engine, NOT provider dispatch layer, NOT hardcoded threshold logic; §1.3 runtime governance layer definition with three-tier boundary table (directive/orchestration-service/provider); (2) RUNTIME ORCHESTRATION MODEL (§2) — §2.1 five governance-safe sequencing invariants (preconditioned, intent-delegated, attribution-continuous, replay-safe, idempotent); §2.2 three-tier architecture diagram enforcing delegation; §2.3 structured assessment outputs consumed by runtime (7 directives mapped to 7 assessment types); §2.4 runtime layer responsibilities table with explicit "does NOT own" column; (3) RUNTIME MODES (§3) — 6 canonical modes defined with per-mode permitted/prohibited/provider/AI/replay/escalation/observability specifications: LIVE (full operation), SHADOW (full evaluation, suppressed dispatch), REPLAY (historical simulation only, LIVE effects suppressed), REGENERATION (warehouse-only, FAD-2 enforced), DEGRADED (partial operation with explicit degradation records), MAINTENANCE (all orchestration suspended); §3.2 mode transition governance table with authorization requirements; §3.3 mode-aware sequencing requirement; (4) CANONICAL RUNTIME ORCHESTRATION DOMAINS (§4) — 10 loop definitions each with purpose/trigger-conditions/replay-behavior/SHADOW-behavior/LIVE-behavior/attribution/observability/idempotency specifications: Orchestration Evaluation Loop (§4.1), Retry Orchestration Loop (§4.2), Escalation Polling Loop (§4.3), AI Refresh Orchestration Loop (§4.4), Snapshot Finalization Loop (§4.5), Report Generation Loop (§4.6), Provider Synchronization Loop (§4.7), Replay Orchestration Loop (§4.8), Compliance Orchestration Loop (§4.9), Observability Heartbeat Loop (§4.10); (5) ORCHESTRATION GOVERNANCE SEQUENCING (§5) — §5.1 7-step sequencing principles; §5.2 governance precondition gate (5 preconditions with explicit failure behaviors); §5.3 canonical orchestration evaluation sequence diagram (12 steps from precondition gate through observability emission); §5.4 intent delegation boundaries table; (6) RETRY ORCHESTRATION GOVERNANCE (§6) — §6.1 Config V2 retry governance table (5 variables from Groups A and F; no hardcoded values); §6.2 runtime-safe retry sequencing (4 additional preconditions); §6.3 replay-safe retry semantics (historical config resolution, UNKNOWN_V0 handling); §6.4 provider-governed execution boundaries; §6.5 retry termination governance table (5 termination conditions, all producing observable records); (7) REPLAY & REGENERATION RUNTIME SEMANTICS (§7) — §7.1 8-phase replay orchestration lifecycle diagram; §7.2 6-row replay suppression behavior table; §7.3 historical resolution semantics table (4 resources with fallback behavior); §7.4 8-field replay observability requirements; §7.5 regeneration restrictions comparison table (replay vs regeneration); (8) PROVIDER GOVERNANCE AT RUNTIME (§8) — §8.1 provider boundary principles (runtime loops never own provider governance; AP-RT1 referenced); §8.2 provider synchronization governance; §8.3 provider failure runtime behavior table (5 failure states); (9) AI GOVERNANCE AT RUNTIME (§9) — §9.1 advisory-only runtime principle (AI informs, does not authorize); §9.2 stale AI runtime restrictions table (5 governance tiers); §9.3 FINALIZED_COPY runtime semantics (FAD-1, AP-OWN15 referenced); §9.4 AI refresh governance; (10) DEGRADATION & FAILURE GOVERNANCE (§10) — §10.1 deterministic degradation principles (3 prohibited patterns); §10.2 11-row degradation condition table (all conditions with orchestration-continues flag and observability); §10.3–10.8 per-condition detailed behavior (provider outage, AI outage, scheduler lag, duplicate event storm, sync backlog, observability degradation); (11) RUNTIME ATTRIBUTION & OBSERVABILITY (§11) — §11.1 correlation_id propagation requirements; §11.2 8-field attribution requirements; §11.3 orchestration lineage chain diagram (sync_cycle_id through ghl_message_id); §11.4 required runtime observability JSON schema; §11.5 idempotency key requirements per loop (10-row table); (12) AP-RT1 through AP-RT15 architecture preservation rules; (13) §13 scope behavior (MVP/STANDARD/PRODUCTION tiered); (14) AC-RT1 through AC-RT12 governance-focused acceptance criteria; (15) UE-RT1 through UE-RT5 unresolved concerns (multi-loop conflict, recovery cycle authorization, maintenance quiescence, heartbeat escalation threshold, emergency runtime recovery authority); (16) §16 downstream governance (child document governance table with required updates, upstream directive dependencies, upstream spec dependencies); (17) §17 references (3 child documents, 8 directives, 8 spec files, 3 FADs).
  - Verification: file written; old system_loop.md replaced; 10 canonical runtime orchestration domains defined; 6 runtime modes defined; governance precondition gate defined; canonical orchestration evaluation sequence defined (12 steps); AP-RT1–AP-RT15 architecture preservation rules defined; AC-RT1–AC-RT12 acceptance criteria defined; UE-RT1–UE-RT5 unresolved concerns classified. 6-point audit: (1) no direct execution semantics — AP-RT1 prohibits direct provider calls, §2.2 three-tier architecture, "SEND_SMS"/"SEND_EMAIL"/"BOOK_MEETING"/"Execute Action" removed — PASS; (2) no replay violations — AP-RT2 explicit LIVE prohibition, §3.1 REPLAY mode, §4.8 all outputs REPLAY_ONLY, §7 full suppression table, AC-RT2 test — PASS; (3) no hidden runtime mutation — §1.2 prohibits mutable flow, AP-RT5 skip logging, AP-RT14 no silent swallow, AP-RT15 no orphaned cycles — PASS; (4) no provider governance violations — §8.1 boundary principles, AP-RT1, §5.4 delegation boundaries — PASS; (5) no duplicate LIVE execution semantics — AP-RT10 explicit prohibition, §11.5 idempotency keys, AC-RT5 test — PASS; (6) no broken lineage guarantees — §11.1–11.3 correlation_id, attribution, lineage chain, AP-RT4 unattributed intent prohibition — PASS; (7) no governance invariant violations — AP-RT3/7/8/9, §5.2 precondition gate, §3.2 mode transition governance — PASS.
  - Notes: Why — runtime/system_loop.md was the MVP-era backbone document containing direct execution semantics (GHL dispatch, SEND_SMS, BOOK_MEETING) incompatible with the governance-safe orchestration model established across all 7 rewritten directives. As the parent runtime contract for daily_outreach_flow.md, retry_cycle_flow.md, and scheduler_design.md, its semantics were being inherited by all child documents. The rewrite closes the final governance gap at the runtime coordination layer: the directive layer now produces structured assessment objects (EligibilityAssessment, RetryPolicyAssessment, OrchestrationIntent, etc.); the runtime layer coordinates their sequencing; the orchestration-service layer executes under governance authorization; and providers deliver operationally. Runtime governance implications: §16.1 downstream governance table documents the required updates to all three child documents — daily_outreach_flow.md must remove GHL payload dispatch semantics, retry_cycle_flow.md must remove hardcoded MAX_ATTEMPTS and CALL→SMS→EMAIL, scheduler_design.md must govern trigger timing via Config V2 Group A. Replay implications: the REPLAY runtime mode (§3.1) and Replay Orchestration Loop (§4.8) establish the first formal replay mode specification at the runtime layer — prior to this, replay semantics were defined only in directives; the 11-row degradation table (§10.2) now covers all runtime degradation scenarios including replay-context failures. Orchestration implications: the 10 canonical runtime domain loops (§4) provide the first formal decomposition of runtime orchestration into separable governance domains — orchestration evaluation, retry, escalation, AI refresh, snapshot finalization, report generation, provider sync, replay, compliance, and observability heartbeat now each have explicit SHADOW/LIVE/replay behavior, attribution requirements, and idempotency keys.

---

## Phase 16 — Daily Orchestration Runtime Architecture

- [x] runtime/daily_outreach_flow.md — rewritten as governance-safe daily orchestration runtime architecture
  - Date: 2026-05-26
  - What changed: `runtime/daily_outreach_flow.md` completely rewritten. Old 390-line V1 file described MVP-era operational outreach flow documentation: hardcoded "6:00 PM CST" in Scheduler Trigger step, "Send payload to GHL" in Step 8, "Max 50 concurrent executions" batch limit, simplistic `QUEUED → CONTACTED` state transitions with no governance context, generic "fallback logic" failure handling, no runtime mode semantics, no Config V2 governance, no replay semantics, no attribution requirements, and direct GHL dispatch assumptions. New ~1,100-line file transforms the daily flow into governance-safe daily orchestration runtime architecture. Key structural changes: (1) PURPOSE reframed — §1.1 defines this document as child runtime contract governed by `runtime/system_loop.md` and `runtime/scheduler_design.md`; §1.2 explicitly states daily orchestration is NOT direct outreach execution flow, NOT provider dispatch flow, NOT operational task execution flow, NOT hardcoded timing flow, NOT silent fallback execution; 8 explicit prohibitions (direct provider execution, direct GHL dispatch, direct SMS/email semantics, replay-triggered LIVE effects, hidden fallback execution, non-attributable orchestration actions, hardcoded timing/concurrency, direct state mutation); §1.3 governance layer definition with 5-row layer/relationship table; (2) DAILY ORCHESTRATION MODEL (§2) — §2.1 seven governance-safe daily sequencing invariants (scheduler-triggered, Config V2-governed, preconditioned, intent-delegated, attribution-continuous, replay-safe, idempotent); §2.2 three-tier daily orchestration architecture diagram; §2.3 7-directive assessment output consumption table (all 7 directive structured outputs mapped to consuming domain); §2.4 daily orchestration layer responsibilities table with "does NOT own" column; (3) RUNTIME MODES (§3) — inherited from `system_loop.md` §3; §3.1 per-mode behavior summary table for all 6 modes; §3.2 mode enforcement requirements (mode check is second precondition after config version validation); REGENERATION explicitly declared not applicable to daily orchestration; (4) DAILY ORCHESTRATION EXECUTION DOMAINS (§4) — 10 domain definitions each with purpose/replay-behavior/SHADOW-behavior/LIVE-behavior/attribution-requirements/observability-requirements/idempotency-guarantees: Candidate Loading Orchestration (§4.1 — student_trigger_data read, sync_cycle_id provenance annotation), Governance Precondition Validation (§4.2 — 5-precondition gate inherited from system_loop.md §5.2), Eligibility Orchestration (§4.3 — EligibilityAssessment coordination, skip records with skip_reason), Retry Orchestration Coordination (§4.4 — RetryPolicyAssessment, causation_id linkage), Escalation Coordination (§4.5 — EscalationOrchestrationAssessment candidacy, advisory routing), AI Enrichment Coordination (§4.6 — advisory-only, FINALIZED_COPY read-only, no ai_insights mutation), Provider Synchronization Coordination (§4.7 — sync validation only, does not invoke sync), Observability Coordination (§4.8 — append-only, never suppressed), Replay Orchestration Coordination (§4.9 — inactive in normal LIVE, all suppression in effect), Degradation Coordination (§4.10 — SCHEDULER_DEGRADED/SCHEDULER_RECOVERY observable records); (5) CANONICAL GOVERNANCE SEQUENCING (§5) — §5.1 seven sequencing principles; §5.2 governance precondition gate (5-precondition table with failure behaviors); §5.3 canonical 14-step daily orchestration evaluation sequence diagram (PRECONDITION GATE through OBSERVABILITY EMISSION); §5.4 intent delegation boundaries table (6 stages with owner and daily flow responsibility); (6) STATE TRANSITION GOVERNANCE (§6) — §6.1 governance-safe state coordination — V1 "QUEUED → CONTACTED" triggered by "Send payload to GHL" explicitly replaced with intent-production → service delegation → state management service path; §6.2 6-row state transition authority table (every transition with authority, daily flow role); §6.3 replay-safe state semantics (no state transitions in replay, fingerprint_comparison_outcome production); (7) REPLAY & REGENERATION SEMANTICS (§7) — §7.1 8-phase daily replay orchestration lifecycle diagram; §7.2 5-row replay suppression behavior table; §7.3 regeneration restrictions (daily orchestration does not participate, no warehouse access); (8) PROVIDER GOVERNANCE (§8) — §8.1 provider boundary principles inherited from system_loop.md §8.1; §8.2 GHL integration governance (6 provider responsibilities explicitly not owned by daily flow); §8.3 5-row provider failure daily behavior table (all failures observable, none silently absorbed); (9) AI GOVERNANCE (§9) — §9.1 advisory-only daily principle (AI informs, does not authorize, AP-DF9); §9.2 5-row stale AI daily restrictions table; §9.3 FINALIZED_COPY daily semantics (FAD-1, AP-OWN15, AP-DF12); (10) FAILURE & DEGRADATION GOVERNANCE (§10) — §10.1 deterministic degradation principles (replaces generic "fallback logic"); §10.2 9-row daily degradation condition table; §10.3 provider outage daily behavior (retry budget protection); §10.4 AI outage daily behavior; §10.5 synchronization lag governance; (11) ATTRIBUTION & OBSERVABILITY (§11) — §11.1 7-link daily attribution lineage chain (Config V2 trigger timing → cycle_id → correlation_id → intent_id → governance_scope → idempotency_key → ghl_message_id); §11.2 8-field daily attribution requirements; §11.3 daily orchestration observability JSON schema (20 fields); §11.4 correlation_id propagation rules; (12) AP-DF1 through AP-DF15 architecture preservation rules; (13) §13 scope behavior (MVP/STANDARD/PRODUCTION tiered; trigger timing governed by Config V2 even in MVP — no hardcoded 6 PM); (14) AC-DF1 through AC-DF12 governance-focused acceptance criteria; (15) UE-DF1 through UE-DF5 unresolved concerns (trigger timing drift, stale SQL threshold, evaluation parallelism, intent persistence failure recovery, AI advisory absence escalation in DEGRADED); (16) §16 downstream governance (parent contract inheritance table, sibling document relationship, directive dependency table); (17) §17 references (2 parent contracts, 1 sibling, 8 directives, 8 spec files, 3 FADs).
  - Verification: file written; old daily_outreach_flow.md replaced; "6:00 PM CST" hardcoded trigger removed and replaced with Config V2 `outreach_daily_trigger_hour_cst` reference; "Send payload to GHL" Step 8 removed and replaced with intent delegation model; "Max 50 concurrent executions" removed and replaced with Config V2 governance; "fallback logic" failure checkpoint replaced with deterministic degradation governance; 10 daily orchestration execution domains defined with SHADOW/LIVE/replay behavior per domain; 6-point final audit: (1) no direct provider execution semantics — AP-DF1/AP-DF2 explicit prohibition, §5.4 delegation boundaries, "Send payload to GHL" removed, AC-DF2 test — PASS; (2) no replay violations — AP-DF3 LIVE suppression prohibition, §4.9 replay coordination domain, §7.2 replay suppression table, AC-DF3 test — PASS; (3) no hidden fallback execution — AP-DF4 explicit prohibition, §10.1 replaces generic "fallback logic", §10.2 degradation condition table with observable outcomes for every condition — PASS; (4) no broken lineage guarantees — §11.1 7-link attribution lineage chain, AP-DF5 unattributed action prohibition, §11.2 attribution requirements, AC-DF6 test — PASS; (5) no governance invariant violations — AP-DF6 no hardcoded timing, AP-DF7 no direct state mutation, AP-DF8 no directive re-implementation, AP-DF9 no AI-only intent, §3.2 mode enforcement — PASS; (6) no duplicate LIVE orchestration semantics — AP-DF14 idempotency check before execution delegation, AC-DF10 test, §5.1 sequencing principle 7 — PASS.
  - Notes: Daily-runtime governance implications — the daily orchestration flow is now explicitly a child contract of both system_loop.md and scheduler_design.md; all timing governance is inherited from Config V2 (trigger hour, concurrency limits, retry windows) and all architectural authority rules (AP-RT1–AP-RT15 from parent, AP-SCH rules from scheduler parent, AP-DF1–AP-DF15 local) are additive rather than conflicting. Replay implications — §7 establishes that daily replay produces no state transitions and no LIVE effects; the fingerprint comparison mechanism (§6.3) allows forensic reconstruction of whether the historical daily cycle's decisions would be reproduced today under the same config, enabling governance audits without re-executing LIVE effects. Provider-governance implications — §6.1 replaces the V1 "QUEUED → CONTACTED triggered by Send payload to GHL" with the governance chain: intent produced → service delegated → ProviderOrchestrationAssessment governs dispatch → state management service records transition; this closes the direct-dispatch gap identified in system_loop.md §16.1 as a required update. The remaining V1 governance debt is in `runtime/retry_cycle_flow.md` which still contains hardcoded retry semantics (Day 1 CALL / Day 2 SMS / Day 3 EMAIL, MAX_ATTEMPTS, "Trigger GHL with updated attempt count") documented in scheduler_design.md §15.1 and system_loop.md §16.1.

---

## Phase 15 — Scheduler Orchestration Architecture

- [x] runtime/scheduler_design.md — rewritten as governance-safe scheduler orchestration architecture
  - Date: 2026-05-26
  - What changed: `runtime/scheduler_design.md` completely rewritten. Old 327-line V1 file described MVP-era scheduling documentation: a single time-based scheduler with hardcoded "6:00 PM CST" specification value, a simplistic single-execution-per-window lock with no governance context, a generic "trigger recovery run" on missed detection, a hardcoded "Max 50 concurrent executions" concurrency limit, no runtime mode semantics, no replay-safe scheduling, no Config V2 governance, and no attribution or observability requirements. New ~1,300-line file transforms the scheduler into governance-safe scheduler orchestration architecture. Key structural changes: (1) PURPOSE reframed — §1.1 defines 8 governance aspects covered (Config V2 timing cadences, runtime mode restrictions, orchestration window control, replay/regeneration scheduling semantics, recovery governance, attribution lineage, authority boundaries); §1.2 explicitly states scheduler orchestration is NOT direct operational execution, NOT generic timing infrastructure, NOT governance definition layer, NOT hardcoded timing logic, NOT silent execution layer; §1.3 scheduler governance layer definition with 5-row layer/relationship table; (2) SCHEDULER ORCHESTRATION MODEL (§2) — §2.1 five governance-safe scheduling invariants (Config V2-governed timing, mode-aware trigger, attribution-initiated, idempotency-gated, observable); §2.2 four-tier architecture diagram (Config V2 → Scheduler → Orchestration Loops → Provider); §2.3 attribution field definition table (cycle_id owned by scheduler, correlation_id owned by loop); (3) SCHEDULER MODES (§3) — 6 canonical mode definitions for all scheduler domains with per-mode allowed-triggers/prohibited-triggers/provider-restrictions/replay-restrictions/AI-restrictions/escalation-restrictions/observability specifications: LIVE, SHADOW, REPLAY, REGENERATION, DEGRADED, MAINTENANCE; §3.2 mode transition impact table (7 transition rows); §3.3 mode check requirement; (4) CANONICAL SCHEDULER DOMAINS (§4) — 10 scheduler domain definitions each with purpose/execution-cadence/runtime-mode-behavior/replay-restrictions/observability-requirements/idempotency-guarantees specifications: Orchestration Evaluation Scheduler (§4.1 — trigger governed by `outreach_daily_trigger_hour_cst` Group A, no hardcoded hour), Retry Orchestration Scheduler (§4.2 — governed by `outreach_retry_window_days` Group A), Escalation Polling Scheduler (§4.3 — Group I), AI Refresh Scheduler (§4.4 — governed by `ai_insight_ttl_hours` Group H), Snapshot Finalization Scheduler (§4.5 — event-driven), Report Generation Scheduler (§4.6 — completeness gate precondition), Provider Synchronization Scheduler (§4.7 — Group I, must complete before Orchestration Evaluation fires), Replay Orchestration Scheduler (§4.8 — operator-initiated only, SHADOW-only always), Compliance Orchestration Scheduler (§4.9 — Compliance Operator initiated), Observability Heartbeat Scheduler (§4.10 — Group I, mode-independent); (5) ORCHESTRATION WINDOW GOVERNANCE (§5) — §5.1 orchestration window definition (open from trigger to loop completion record); §5.2 runtime-cycle ownership rules (scheduler owns timing, loop owns evaluation, no cross-domain ownership); §5.3 replay-safe locking (replay cycles run in separate context, do not satisfy LIVE window idempotency gate); §5.4 distributed scheduling considerations (idempotency record as lock, INSERT-wins arbitration); §5.5 orphaned-cycle prevention (Heartbeat Loop detects, dead-letter for manual triage, no silent discard); §5.6 stale-lock governance (force-unlock requires Operational Administrator authorization with audit record, no automated silent expiry); §5.7 deterministic recovery semantics (execution_type = recovery, full attribution required, Operational Administrator authorization unless Config V2-governed automated recovery defined); (6) CONFIG V2 SCHEDULING GOVERNANCE (§6) — §6.1 9-row Config V2 scheduler timing variables table (Groups A, H, I, K; no hardcoded values for any parameter); §6.2 missing threshold behavior (UNKNOWN_V0 substituted, SCHEDULER_DEGRADED state emitted, escalation candidacy generated, no hardcoded fallback); (7) REPLAY & REGENERATION SCHEDULING (§7) — §7.1 8-phase replay scheduling lifecycle diagram; §7.2 regeneration vs replay scheduling comparison table (6 dimensions: trigger, authorization, input, purpose, source, external calls); §7.3 historical scheduling replay (historical Config V2 trigger timing resolved from historical config version); §7.4 6-row replay suppression rules table (batch trigger, retry trigger, live AI refresh, provider sync, compliance execution, mode transition); §7.5 8-field replay observability requirements; (8) RECOVERY & DEGRADATION GOVERNANCE (§8) — §8.1 5 deterministic recovery principles (attributable, non-duplicating, observable, governance-gated, non-silent); §8.2 7-row recovery condition table (scheduler crash, partial execution, provider outage, AI outage, distributed timing drift, sync backlog, degraded mode entry — each with detection, behavior, authorization, observability); §8.3 recovery attribution required fields (6 mandatory fields including orphaned_cycle_id); §8.4 degradation governance (SCHEDULER_DEGRADED record, TRIGGER_SUPPRESSED record, escalation candidacy, SCHEDULER_RECOVERY on resolution); (9) ATTRIBUTION & OBSERVABILITY (§9) — §9.1 full attribution lineage chain (Config V2 trigger timing → cycle_id → correlation_id → intent_id → governance_scope → idempotency_key → ghl_message_id); §9.2 cycle_id/correlation_id linkage requirements; §9.3 8-row runtime traceability requirements table; §9.4 scheduler trigger JSON observability schema + scheduler cycle completion JSON observability schema; (10) AUTHORITY BOUNDARIES (§10) — §10.1 authority definition table (held vs not held); §10.2 Replay Orchestration Scheduler authority restrictions (7 explicit prohibitions); §10.3 provider authority boundaries at scheduling layer; §10.4 AI authority at scheduling layer; (11) AP-SCH1 through AP-SCH15 architecture preservation rules; (12) AC-SCH1 through AC-SCH12 acceptance criteria; (13) §13 scope behavior (MVP/STANDARD/PRODUCTION tiered; note: no hardcoded timing even in MVP — Config V2 Group A must be seeded before any domain fires); (14) UE-SCH1 through UE-SCH5 unresolved concerns (inter-domain sequencing conflict, recovery cycle authorization, distributed lock table schema, heartbeat failure escalation threshold, compliance scheduler identity in attribution); (15) §15 downstream governance (daily_outreach_flow.md and retry_cycle_flow.md governed scheduling aspects with required updates documented); (16) §16 references (parent contract system_loop.md, 2 child documents, 8 directives, 8 spec files, 3 FADs).
  - Verification: file written; old scheduler_design.md replaced; 10 canonical scheduler domains defined with Config V2-governed timing (no hardcoded values); 6 scheduler modes defined; orchestration window governance defined (idempotency gate, replay-safe locking, stale-lock governance, orphaned-cycle prevention); Config V2 timing variable table defined (Groups A, H, I, K); replay and regeneration scheduling semantics defined; recovery and degradation governance defined; AP-SCH1–AP-SCH15 architecture preservation rules defined; AC-SCH1–AC-SCH12 acceptance criteria defined; UE-SCH1–UE-SCH5 unresolved concerns classified. 7-point final audit: (1) no replay corruption — AP-SCH3 replay-scheduler LIVE prohibition, §7.4 replay suppression table, AC-SCH4 test, replay cycles in separate idempotency namespace — PASS; (2) no hidden scheduler execution — AP-SCH5 no silent trigger skip, AP-SCH10 no hidden recovery execution, §8.1 deterministic recovery principles — PASS; (3) no duplicate LIVE orchestration windows — AP-SCH4 explicit prohibition, §5.1 window definition, §5.3 replay-safe locking (replay uses separate idempotency namespace), AC-SCH3 test — PASS; (4) no broken lineage guarantees — §9.1 attribution lineage chain (Config V2 → cycle_id → correlation_id → ghl_message_id), §9.2 cycle_id/correlation_id linkage requirement, §9.3 traceability table — PASS; (5) no governance invariant violations — AP-SCH2 no SHADOW-to-LIVE via scheduler, AP-SCH6 no hardcoded timing, AP-SCH15 no scheduler re-implementation of precondition gate, §6.2 UNKNOWN_V0 on missing Config V2 variables — PASS; (6) no silent recovery behavior — AP-SCH9 stale lock force-unlock requires audit, AP-SCH10 hidden recovery prohibited, §8.3 mandatory recovery attribution fields, §8.4 SCHEDULER_DEGRADED and SCHEDULER_RECOVERY observable records — PASS; (7) no provider authority escalation — AP-SCH1 no eligibility evaluation by scheduler, §10.3 provider authority boundaries explicit prohibition table, §10.4 AI authority at scheduling layer — PASS.
  - Notes: Scheduler governance implications — the 10 canonical scheduler domains in §4 replace the single hardcoded "6:00 PM CST" trigger with Config V2-governed timing across all scheduling surfaces; every domain's cadence references the appropriate Config V2 group variable so operational tuning is governed centrally and no scheduler domain can fire with a hardcoded timing assumption even in MVP scope. Recovery governance implications — §8.2 recovery condition table defines deterministic recovery for all 7 recovery trigger conditions (scheduler crash, partial execution, provider outage, AI outage, distributed timing drift, sync backlog, degraded mode) with observable, attributable behavior for each; AP-SCH10 and §8.1 together close the "hidden recovery execution" gap that existed in the V1 document where a missed-run recovery simply triggered silently with no governance record. Replay implications — §7.4 replay suppression table combined with AP-SCH3 establish that replay scheduling lives in a completely separate authority context from LIVE batch scheduling: replay triggers use a different idempotency namespace `(source_artifact_id, replay_cycle_id)` rather than the LIVE window namespace `(scheduler_domain, cycle_date, execution_mode)`, preventing any possibility of a replay trigger satisfying a LIVE window gate or vice versa. Downstream governance — §15.1 documents required updates to daily_outreach_flow.md (hardcoded trigger time, hardcoded concurrency limit) and retry_cycle_flow.md (hardcoded retry timeline, MAX_ATTEMPTS reference), aligning with the required updates already documented in runtime/system_loop.md §16.1.

---

## Phase 17 — Retry Orchestration Runtime Architecture

- [x] runtime/retry_cycle_flow.md — rewritten as governance-safe retry orchestration runtime architecture
  - Date: 2026-05-26
  - What changed: `runtime/retry_cycle_flow.md` completely rewritten from 420-line V1 MVP-era hardcoded retry execution documentation to ~900-line governance-safe retry orchestration runtime architecture. V1 file contained: `contact_attempt < MAX_ATTEMPTS` hardcoded entry condition; hardcoded CALL→SMS→EMAIL channel progression ("Attempt 1 → CALL / Attempt 2 → SMS / Attempt 3 → EMAIL"); hardcoded retry timeline ("Day 1 (6 PM) → Attempt 1 / Day 2 (6 PM) → Attempt 2 / Day 3 (6 PM) → Attempt 3"); "Trigger GHL with updated attempt count" in Step 7; `contact_attempt += 1` direct state mutation; generic "Retry immediately once" fallback failure handling. All replaced with governance-safe retry orchestration architecture. Key structural changes: (1) PURPOSE reframed — §1.1 defines this document as child runtime contract governed by `system_loop.md` §4.2/§6/§8.3, `scheduler_design.md` §4.2, and `daily_outreach_flow.md` §4.4; §1.2 explicitly prohibits: direct provider execution (AP-RF1), hardcoded channel progression (AP-RF2), hardcoded MAX_ATTEMPTS (AP-RF3), replay-triggered LIVE retry effects, hidden retry fallback execution (AP-RF5), non-attributable retry actions (AP-RF6), direct state mutation by retry loop (AP-RF7); §1.3 governance layer definition with 6-row layer/relationship table including daily flow as additional parent; (2) RETRY ORCHESTRATION MODEL (§2) — §2.1 seven governance-safe retry sequencing invariants (scheduler-triggered, Config V2-governed, preconditioned, intent-delegated, attribution-continuous, replay-safe, idempotent); §2.2 three-tier retry architecture diagram; §2.3 7-directive assessment output consumption table with per-directive consuming domain; §2.4 retry orchestration layer responsibilities table with "does NOT own" column; (3) RUNTIME MODES (§3) — inherited from system_loop.md §3; §3.1 per-mode behavior summary for all 6 modes; REGENERATION explicitly declared not applicable; (4) RETRY ORCHESTRATION EXECUTION DOMAINS (§4) — 10 domain definitions with purpose/replay-behavior/SHADOW-behavior/LIVE-behavior/attribution-requirements/observability-requirements/idempotency-guarantees: Retry Candidate Loading Orchestration (§4.1 — NO_RESPONSE candidates, retry window elapsed check uses `outreach_retry_window_days` Config V2 Group A, contact_attempt sourced from state_transition_log read-only), Governance Precondition Validation (§4.2 — 5-precondition gate; `outreach_max_retry_attempts` and `outreach_retry_window_days` added as required resolvable Config V2 keys), Retry Eligibility Coordination (§4.3 — RetryPolicyAssessment + EligibilityAssessment re-check, RETRY_AUTHORIZED/RETRY_BLOCKED/RETRY_EXHAUSTED classification), Retry Cadence Coordination (§4.4 — elapsed window validation against `outreach_retry_window_days` Config V2, RETRY_PENDING exclusion), Escalation Coordination (§4.5 — EscalationOrchestrationAssessment for retry-exhausted candidates; causation_id links to prior intent), AI Enrichment Coordination (§4.6 — advisory-only, FINALIZED_COPY read-only), Provider Synchronization Coordination (§4.7 — sync validation only), Observability Coordination (§4.8 — append-only, never suppressed), Replay Retry Coordination (§4.9 — inactive in LIVE, all suppression in effect), Degradation Coordination (§4.10 — all degradation states observable); (5) CANONICAL RETRY ORCHESTRATION SEQUENCING (§5) — §5.1 seven sequencing principles; §5.2 governance precondition gate (5-precondition table with `outreach_max_retry_attempts` and `outreach_retry_window_days` explicitly named); §5.3 canonical 13-step retry orchestration evaluation sequence diagram (PRECONDITION GATE → PROVIDER SYNC VALIDATION → RETRY CANDIDATE LOADING → RETRY WINDOW CADENCE CHECK → RETRY POLICY ASSESSMENT → ELIGIBILITY RE-CHECK → AI ENRICHMENT COORDINATION → CORE DECISION ENGINE → CHANNEL SELECTION → ESCALATION CANDIDACY ASSESSMENT → INTENT RECORD PERSISTENCE → EXECUTION DELEGATION → PROVIDER ORCHESTRATION ASSESSMENT → OBSERVABILITY EMISSION; channel selection explicitly noted as fresh per-attempt assessment, not a progression lookup — AP-RF2); §5.4 intent delegation boundaries with causation_id linkage as explicit retry flow responsibility; (6) RETRY STATE TRANSITION GOVERNANCE (§6) — §6.1 V1 "QUEUED → CONTACTED triggered by 'Trigger GHL with updated attempt count'" and `contact_attempt += 1` explicitly replaced with intent-production → service delegation → state management service path; §6.2 6-row state transition authority table (every retry transition with authority and retry flow role; contact_attempt increment owned exclusively by state management service); §6.3 replay-safe retry state semantics (historical contact_attempt count sourced from state_transition_log, no state writes in replay, fingerprint_comparison_outcome produced); (7) REPLAY & REGENERATION SEMANTICS (§7) — §7.1 replay lifecycle adds HISTORICAL CONTACT ATTEMPT COUNT RESOLUTION and HISTORICAL CONFIG VERSION RESOLUTION phases for retry-specific context; §7.2 5-row replay suppression table (contact_attempt increment explicitly suppressed); §7.3 regeneration restrictions; (8) PROVIDER GOVERNANCE (§8) — §8.1 provider boundary principles; §8.2 GHL retry integration governance (6 provider responsibilities not owned by retry flow); §8.3 5-row provider failure retry behavior table including retry budget protection for outage-blocked attempts; (9) AI GOVERNANCE (§9) — §9.1 advisory-only retry principle; §9.2 5-row stale AI retry restrictions; §9.3 FINALIZED_COPY retry semantics; (10) FAILURE & DEGRADATION GOVERNANCE (§10) — §10.1 deterministic degradation principles (replaces V1 "Retry immediately once" fallback); §10.2 11-row retry degradation condition table (includes retry starvation as a distinct condition — all candidates RETRY_PENDING); §10.3 provider outage retry behavior with retry budget protection protocol; §10.4 AI outage retry behavior; §10.5 retry starvation governance (zero-dispatch cycle as normal, not failure; consecutive starvation → escalation candidacy after Config V2 threshold — UE-RF3); (11) ATTRIBUTION & OBSERVABILITY (§11) — §11.1 7-link retry attribution lineage chain (Config V2 retry governance → cycle_id → prior OrchestrationIntent via causation_id → retry OrchestrationIntent → governance_scope → idempotency_key → ghl_message_id); §11.2 8-field attribution requirements with `contact_attempt` as required field on all retry intent records; §11.3 retry orchestration observability JSON schema (21 fields including `outreach_max_retry_attempts_applied`, `outreach_retry_window_days_applied`, `retry_starvation_flag`); §11.4 correlation_id and causation_id dual propagation rules; (12) AP-RF1 through AP-RF15 architecture preservation rules; (13) §13 scope behavior (MVP/STANDARD/PRODUCTION tiered; no hardcoded timing even in MVP — Config V2 Group A governs); (14) AC-RF1 through AC-RF12 governance-focused acceptance criteria; (15) UE-RF1 through UE-RF5 unresolved concerns (retry window missed-cycle tolerance, intent persistence failure recovery, starvation escalation threshold, provider outage budget protection protocol, AI advisory absence escalation in DEGRADED); (16) §16 downstream governance (3 parent contracts, directive dependency table, upstream spec dependencies); (17) §17 references (3 parent contracts, 8 directives, 8 spec files, 3 FADs).
  - Verification: file written; V1 `contact_attempt < MAX_ATTEMPTS` entry condition removed; V1 CALL→SMS→EMAIL hardcoded progression removed; V1 "Day 1 (6 PM)" hardcoded retry timeline removed; V1 "Trigger GHL with updated attempt count" Step 7 removed; V1 `contact_attempt += 1` direct mutation removed; V1 "Retry immediately once" fallback failure handling removed; all replaced with governance-safe retry orchestration coordination semantics. 7-point final audit: (1) no hardcoded retry progression — AP-RF2 prohibits CALL→SMS→EMAIL fixed advancement, §5.3 channel selection step is fresh per-attempt assessment via channel_selection_rules.md, AC-RF2 test — PASS; (2) no hardcoded retry threshold — AP-RF3 prohibits MAX_ATTEMPTS constant, §5.2 precondition gate requires `outreach_max_retry_attempts` from Config V2, §6.5 (inherited from system_loop.md) termination uses Config V2 variable, AC-RF3 test — PASS; (3) no direct provider execution semantics — AP-RF1 prohibits direct GHL/provider calls, §8.1 provider boundary principles, §5.4 delegation boundaries, AC-RF1 test — PASS; (4) no replay violations — AP-RF4 LIVE prohibition in replay, §4.9 replay coordination domain, §7.2 replay suppression table (contact_attempt increment explicitly suppressed), AC-RF4 test — PASS; (5) no hidden retry fallback execution — AP-RF5 explicit prohibition, §10.1 replaces generic fallback, §10.2 degradation condition table with observable outcomes for all 11 conditions — PASS; (6) no broken lineage guarantees — §11.1 7-link attribution lineage chain with causation_id as required link, AP-RF6 unattributed action prohibition, §11.2 contact_attempt as required field, AC-RF6 attribution continuity test — PASS; (7) no duplicate LIVE retry execution semantics — AP-RF14 idempotency check required, idempotency key `(student_id, checkpoint_type, contact_attempt)` per system_loop.md §11.5, AC-RF11 test — PASS.
  - Notes: Retry-runtime governance implications — retry orchestration flow is now explicitly a child contract of three parent documents (system_loop.md + scheduler_design.md + daily_outreach_flow.md); all retry timing and threshold governance is inherited from Config V2 Group A (`outreach_retry_window_days`, `outreach_max_retry_attempts`) and all architectural authority rules are additive across parent contracts. Key architectural distinction from daily flow: the `causation_id` linking each retry intent to its prior intent is an explicit requirement in retry orchestration that does not apply equally to initial-outreach daily flow; this enables forensic reconstruction of the full retry chain per student without querying raw state transitions. Replay implications — §7.1 retry replay lifecycle adds two historical resolution phases (CONTACT ATTEMPT COUNT RESOLUTION and CONFIG VERSION RESOLUTION) specific to retry context; historical `contact_attempt` is sourced from `state_transition_log`, not from a live counter, so replay can accurately simulate what retry attempt would have been evaluated under the historical config at the historical point in time. Provider-governance implications — §6.1 replaces V1 "Trigger GHL with updated attempt count" with the governance chain: retry intent produced → outreach orchestration service delegated → ProviderOrchestrationAssessment governs dispatch → state management service records transition and increments contact_attempt; this closes the direct-dispatch gap identified in system_loop.md §16.1 as a required update. Starvation governance — §10.5 introduces retry starvation as a distinct governance concept not present in V1; consecutive zero-dispatch cycles are normal when all candidates are still within their retry window, but persistent starvation eventually requires escalation candidacy — UE-RF3 documents the open governance decision for the consecutive-cycle threshold Config V2 variable. The three-document runtime governance sequence (system_loop.md Phase 14 → scheduler_design.md Phase 15 → daily_outreach_flow.md Phase 16 → retry_cycle_flow.md Phase 17) is now complete.

---

## Phase 20 — GHL Provider Resilience Architecture

- [x] failure/ghl_failures.md — rewritten as governance-safe external provider resilience architecture
  - Date: 2026-05-26
  - What changed: `failure/ghl_failures.md` completely rewritten from 346-line V1 MVP-era CRM/provider recovery documentation to governance-safe external provider resilience architecture. V1 file contained: four-type failure taxonomy (Trigger Failure/Workflow Execution Failure/Webhook Failure/Delayed Response); hardcoded timeout table ("5–10 seconds" API response, "5–30 minutes" webhook arrival, "up to 1 hour" transcript availability); fixed retry matrix with "Immediate retry (1x)" for API trigger failures; "Retry immediately (once)" as a first-class recovery step (§4.1); three-tier scope behavior (MVP/STANDARD/PRODUCTION); seven-field plaintext log requirement; simplistic state impact table showing direct `CONTACTED → NO_RESPONSE` transitions without governance context; no circuit breaker governance; no Config V2 governance; no replay-safe semantics; no attribution lineage requirements; no provider authority boundary definition. New file is a canonical child resilience contract governed by `failure/failure_playbook.md` (§2.2, §4.2, §4.7, §7.1, §12, §13) and `directives/ghl_integration_contract.md` (RULE 0–RULE 11, §8, §13). Key structural changes: (1) PURPOSE reframed — §1.1 defines child resilience contract status under both parent contracts; §1.2 explicitly prohibits: "Retry immediately (once)" (AP-GF3), hardcoded timeout values (AP-GF5), direct retry execution from resilience layer (AP-GF1), silent channel downgrade (AP-GF4), immutable state mutation (AP-GF7), replay-triggered LIVE delivery (AP-GF2), direct state transition authority (§6.1); §1.3 parent contract inheritance table with governing sections from all 5 parent contracts; (2) PROVIDER FAILURE GOVERNANCE DOMAINS (§2) — 10 domain definitions replacing the V1 four-type taxonomy: Outbound Delivery Failures (§2.1 — replaces V1 §4.1 "Retry immediately (once)"), Duplicate Delivery Governance (§2.2 — replaces V1 idempotency gap), Inbound Webhook Failures (§2.3 — replaces V1 §4.3 "Retry processing if possible"), Provider Timeout Governance (§2.4 — replaces V1 hardcoded timeout table), Provider Outage Governance (§2.5 — new explicit domain), Degraded Synchronization Governance (§2.6 — new), Attribution-Loss Governance (§2.7 — new), Replay Provider Failures (§2.8 — new), Regeneration Provider Failures (§2.9 — new), Observability Degradation Governance (§2.10 — new); each domain has governance-scope/recovery-philosophy/replay-behavior/SHADOW-behavior/LIVE-behavior/observability-requirements/attribution-requirements/idempotency-guarantees; (3) FAILURE AUTHORITY BOUNDARIES (§3) — §3.1 8-row authority table with "does NOT own" column; §3.2 provider authority restrictions (GHL remains platform_supplementary during all failures); §3.3 AI authority restrictions (advisory-only during provider failures); §3.4 replay authority restrictions; (4) DETERMINISTIC PROVIDER DEGRADATION GOVERNANCE (§4) — §4.1 degradation principles inherited from failure_playbook.md §4.1 and system_loop.md §10.1; §4.2–§4.9 per-condition behavior: Provider Outage (§4.2 — replaces V1 "no automatic recovery"), Circuit Breaker (§4.3 — replaces V1 absence of circuit breaker governance), Retry Budget Exhaustion (§4.4 — replaces V1 "Immediate retry (1x)"), Duplicate Event Storm (§4.5 — inherits failure_playbook.md §4.7), Webhook Backlog (§4.6 — replaces V1 "5–30 minutes" hardcoded timeout), Synchronization Backlog (§4.7), Authentication Failure TERMINAL (§4.8 — inherits system_loop.md §8.3 + ghl_integration_contract.md §12.2), Observability Degradation (§4.9); (5) REPLAY & REGENERATION PROVIDER FAILURE SEMANTICS (§5) — §5.1 replay provider failure governance; §5.2 5-row replay outbound suppression table; §5.3 historical provider visibility (sourced from ghl_messages, outreach_history, processed_events at historical timestamp); §5.4 regeneration provider failure semantics (reads ghl_messages/outreach_history exclusively; no GHL API access); (6) PROVIDER FAILURE STATE GOVERNANCE (§6) — §6.1 7-row state authority table per failure scenario — resilience layer does NOT own state transitions; §6.2 7 immutable record protections (ghl_messages, outreach_history, student_campaign_activity, processed_events, warehouse.student_snapshots, warehouse.snapshot_ai_narratives, state_transition_log); §6.3 eligibility/retry policy coordination; (7) PROVIDER RECOVERY ORCHESTRATION COORDINATION (§7) — §7.1 governed provider recovery coordination model — V1 "Retry immediately (once)" and "Proceed with retry policy" replaced with 6-step governed coordination sequence (FAILURE DETECTION → RECOVERY AUTHORIZATION GATE → IDEMPOTENCY CHECK → RECOVERY ATTRIBUTION → RECOVERY ORCHESTRATION DELEGATION → OBSERVABILITY EMISSION); §7.2 Config V2-governed provider resilience thresholds (9-row table: provider_failure_max_retry_attempts [F], provider_failure_circuit_breaker_threshold [F], provider_failure_circuit_breaker_reset_hours [F], provider_failure_retry_backoff_base_seconds [F], provider_delivery_sla_hours [F], API timeout window [F], circuit breaker probe interval [F], sql_server_max_sync_age_hours [K], escalation threshold [F]); §7.3 provider recovery attribution requirements (9-field table with provider-specific fields: provider_failure_type, provider_outage_window, circuit_breaker_resolved, auth_failure_resolved); (8) ATTRIBUTION & OBSERVABILITY (§8) — §8.1 provider failure attribution lineage chain (Config V2 Group F → cycle_id → upstream_intent_id/ghl_message_id → provider failure event → recovery record → escalation candidacy); §8.2 outbound correlation propagation; §8.3 causation lineage in provider recovery; §8.4 7-field replay provider diagnostics (adds replay_provider_source); §8.5 webhook delivery observability (7-field table inherited from ghl_integration_contract.md §10.5); (9) PROVIDER FAILURE IDEMPOTENCY GUARANTEES (§9) — §9.1 core principle; §9.2 6-row domain-specific provider idempotency keys; §9.3 idempotency violations as production defects; (10) DETERMINISTIC PROVIDER DEGRADATION CONDITION TABLE (§10) — §10.1 14-row canonical provider degradation condition table; (11) PROVIDER RESILIENCE OBSERVABILITY SCHEMA (§11) — §11.1 structured provider failure log record (22-field JSON extending parent schema with `provider_failure_type`, `provider_name`, `ghl_message_id`, `circuit_breaker_state`, `http_status_code`, `provider_error_class`, `webhook_event_type`, `retry_attempt_number`, `delivery_state`, `outbound_suppressed`); §11.2 provider recovery cycle completion record (21-field JSON with provider-specific attribution: `provider_name`, `provider_outage_window`, `circuit_breaker_resolved`, `auth_failure_resolved`); (12) AP-GF1 through AP-GF15 architecture preservation rules; (13) AC-GF1 through AC-GF12 governance-focused acceptance criteria; (14) UE-GF1 through UE-GF5 unresolved governance concerns (cross-provider failover — ref ghl_integration_contract.md UE-GHL3, webhook signature verification — ref UE-GHL2, retry budget scope — ref failure_playbook.md UE-FP3, GHL event sync SLA — ref UE-GHL6, opt-out synchronization — ref UE-GHL5); (15) §15 downstream references (§15.1 authoritative parent contracts — 9 contracts; §15.2 sibling child contracts; §15.3 directive dependencies).
  - Verification: file written; V1 "Retry immediately (once)" removed from all provider failure handling scenarios — replaced with governed 6-step recovery coordination model (§7.1) and AP-GF3 prohibition; V1 hardcoded timeout table ("5–10 seconds", "5–30 minutes", "1 hour") removed — replaced by Config V2 Group F OPERATIONAL_TUNING variables throughout and AP-GF5 prohibition; V1 fixed retry matrix ("Immediate retry (1x)", "Retry if recoverable") removed — replaced by Config V2 Group F `provider_failure_max_retry_attempts` governance and AP-GF3/AP-GF5; V1 MVP/STANDARD/PRODUCTION scope tiers removed — replaced with execution_mode governance; V1 seven-field plaintext log replaced with 22-field structured JSON observability schema; V1 simplistic state impact table replaced with §6.1 state authority table (resilience layer does NOT own state transitions). 7-point final audit: (1) no hardcoded resend behavior — 0 matches for "Retry immediately (once)", "5.10 seconds", "5.30 minutes", "up to 1 hour", scope tiers in new file; AP-GF5 prohibits all hardcoded threshold values; §7.2 Config V2 threshold table with 9 rows all referencing Config V2 Group F/K variables — PASS; (2) no hidden provider recovery execution — AP-GF1 direct dispatch prohibition, AP-GF3 "retry immediately" prohibition, §7.1 6-step governed coordination model, §1.2 explicit prohibition list — PASS; (3) no replay violations — AP-GF2 LIVE outbound prohibition in replay, §5.1 replay provider failure governance, §5.2 5-row replay suppression table, §2.8 replay provider failures domain — PASS; (4) no broken lineage guarantees — §8.1 provider failure attribution lineage chain, §7.3 9-field recovery attribution table, AP-GF6 non-attributable recovery prohibition, §8.2 outbound correlation propagation — PASS; (5) no governance invariant violations — AP-GF7 immutable record prohibition (7 protected record types), AP-GF8 governance bypass prohibition, §6.2 immutable record protections, AP-GF15 provider authority escalation prohibition — PASS; (6) no silent degradation behavior — AP-GF4 silent fallback prohibition, AP-GF14 silent error swallow prohibition, §4 all 9 degradation conditions observable, §10.1 14-row condition table with explicit observability column — PASS; (7) no duplicate LIVE outbound recovery execution semantics — AP-GF9 duplicate dispatch prohibition, AP-GF11 silent webhook drop prohibition, §9.2 6-row domain idempotency key table, AC-GF7 idempotent recovery test, AC-GF11 duplicate webhook test — PASS. 65 AP-GF rule occurrences confirmed.
  - Notes: Provider-resilience governance implications — ghl_failures.md V2 is now fully aligned with its two parent contracts (failure_playbook.md as resilience parent; ghl_integration_contract.md as provider orchestration parent); the V1 failure taxonomy is replaced by governance domains that mirror the provider interaction categories from ghl_integration_contract.md §1.4 and the degradation governance from §8; every former "retry once" or "wait timeout" instruction is replaced by Config V2 Group F-governed semantics and AP-GF rules. The circuit breaker governance model from ghl_integration_contract.md RULE 4 is now first-class in the resilience architecture (§4.3 Circuit Breaker Degradation) — not absent as in V1. Authentication failure TERMINAL classification (§4.8) is now explicit with AP-GF12 prohibition of silent AUTH_FAILURE handling. Replay implications — §5 establishes that historical provider interactions are sourced exclusively from ghl_messages, outreach_history, and processed_events at historical timestamps; replay provider failure simulation produces REPLAY_ONLY scoped diagnostics; no live GHL API calls during replay; fingerprint_comparison_outcome emitted. Degradation-governance implications — §4 replaces V1's three-tier scope model with 9 explicit named degradation conditions, each observable, each with specific escalation action; §10.1 14-row table covers all conditions including AUTH_FAILURE_TERMINAL, replay mode, SHADOW mode, attribution loss — conditions not present in V1. Provider-governance implications — §3.2 explicitly states GHL remains platform_supplementary during all failure conditions including sustained outage (failures never elevate provider authority); §6.1 state authority table establishes that NO_RESPONSE state transition is owned by the state management service — not by the resilience layer — closing the direct state mutation gap from V1. PROGRESS.md audit: 2 files changed (failure/ghl_failures.md, PROGRESS.md), 2 entries, audit clean.

---

## Phase 19 — Scheduler Resilience Architecture

- [x] failure/scheduling_failures.md — rewritten as governance-safe scheduler resilience architecture
  - Date: 2026-05-26
  - What changed: `failure/scheduling_failures.md` completely rewritten from 386-line V1 MVP-era scheduler recovery documentation to ~900-line governance-safe scheduler resilience architecture. V1 file contained: hardcoded "6 PM CST" timing specification value; a simplistic five-type failure taxonomy (Missed/Duplicate/Partial/Crash/Drift); "Trigger recovery execution immediately" as a first-class recovery instruction (§4.1); "Restart scheduler service" as a crash recovery instruction (§4.4); "Normalize system time to CST" as a time-drift correction instruction (§4.5); three-tier scope behavior (MVP/STANDARD/PRODUCTION); a generic five-field log list; a simple four-step recovery flow diagram; no runtime mode semantics; no Config V2 governance; no replay-safe semantics; no attribution continuity; no governance authority boundaries. New file is governed by the canonical parent resilience contract `failure/failure_playbook.md` (§2.4, §4.4, §7.1, §12, §13) and the authoritative scheduler orchestration contract `runtime/scheduler_design.md` (§5, §6, §7, §8, §9). Key structural changes: (1) PURPOSE reframed — §1.1 defines child resilience contract status under both parent contracts; §1.2 explicitly prohibits: "Trigger recovery execution immediately" (AP-SF3), "Restart scheduler service" (AP-SF1), "Normalize system time to CST" (AP-SF4), hardcoded timing semantics (AP-SF4), silent scheduler recovery (AP-SF8), replay-triggered LIVE recovery (AP-SF2); §1.3 parent contract inheritance table with governing sections from both parents; (2) SCHEDULER FAILURE GOVERNANCE DOMAINS (§2) — 10 domain definitions replacing the V1 five-type taxonomy: Missed-Window (§2.1 — replaces V1 §4.1 "Trigger recovery execution immediately"), Duplicate-Window (§2.2 — replaces V1 §4.2 "Block second execution"), Orphaned-Window (§2.3 — new), Scheduler Crash (§2.4 — replaces V1 §4.4 "Restart scheduler service"), Distributed Timing Drift (§2.5 — replaces V1 §4.5 "Normalize system time to CST"), Replay Scheduling Failures (§2.6 — new), Regeneration Scheduling Failures (§2.7 — new), Degraded Scheduler Coordination (§2.8 — new), Synchronization Backlog at Trigger (§2.9 — new), Attribution-Loss Failures (§2.10 — new); each domain has governance-scope/recovery-philosophy/replay-behavior/SHADOW-behavior/LIVE-behavior/observability-requirements/attribution-requirements/idempotency-guarantees; (3) SCHEDULER FAILURE AUTHORITY BOUNDARIES (§3) — §3.1 authority table with "does NOT own" column; §3.2 mode transition authority restrictions (no SHADOW-to-LIVE transition via scheduler failure); §3.3 provider authority restrictions (failures do not elevate provider authority); §3.4 AI authority restrictions (AI advisory-only in all scheduler failure contexts); §3.5 replay authority restrictions (no LIVE authority from replay scheduling); (4) DETERMINISTIC SCHEDULER DEGRADATION GOVERNANCE (§4) — §4.1 scheduler degradation principles; §4.2–§4.9 per-condition behavior (crash degradation, missed-window degradation, duplicate trigger storm, distributed timing drift, orphaned-window degradation, observability degradation, Config threshold missing, stale-lock governance); replaces V1 "restart scheduler" with deterministic named degradation behaviors; (5) REPLAY & REGENERATION SCHEDULING FAILURE SEMANTICS (§5) — §5.1 replay scheduling failure governance; §5.2 7-row replay suppression table (batch outreach trigger, retry batch trigger, live AI refresh, live provider sync, compliance workflow, mode transition, LIVE recovery from replay failure); §5.3 historical scheduling visibility (historical trigger timing from historical_config_version_id); §5.4 regeneration scheduling failure semantics (distinct from replay); (6) SCHEDULER FAILURE STATE GOVERNANCE (§6) — §6.1 9-row state authority table per scheduler failure scenario with resilience layer role; §6.2 immutable record protections (5 protected record types — same as parent contract); §6.3 orchestration-window state coordination (window state owned by scheduler domain per scheduler_design.md §5); (7) GOVERNED SCHEDULER RECOVERY COORDINATION (§7) — §7.1 governed recovery coordination model — V1 "Trigger recovery execution immediately" and "Restart scheduler service" explicitly replaced with 6-step governed coordination sequence; §7.2 Config V2-governed scheduler recovery thresholds (9-row table: all threshold names, Config V2 source, Group — outreach_daily_trigger_hour_cst [A], outreach_retry_window_days [A], sql_server_max_sync_age_hours [K], ai_insight_ttl_hours [H], Group I variables for orphan detection, stale-lock timeout, recovery authorization window, clock skew tolerance, escalation threshold); §7.3 recovery attribution requirements (8-field table including scheduler-specific fields: orphaned_cycle_id, scheduler_crash_detected, stale_lock_resolved); §7.4 dead-letter governance for scheduler failures; (8) SCHEDULER ATTRIBUTION & OBSERVABILITY (§8) — §8.1 scheduler failure attribution lineage chain (Config V2 timing → scheduler trigger → scheduler failure event → recovery authorization gate → recovery cycle attribution → orchestration loop → dead-letter or escalation); §8.2 recovery cycle causation propagation; §8.3 8-field replay diagnostics (adds scheduler-specific historical_trigger_time_resolved, replay_suppression_applied); §8.4 scheduler audit continuity; (9) SCHEDULER FAILURE IDEMPOTENCY GUARANTEES (§9) — §9.1 core idempotency principle; §9.2 6-row domain-specific scheduler idempotency keys (missed-window recovery, duplicate-window detection, orphaned-window dead-letter, crash recovery, force-unlock audit, escalation candidacy); §9.3 idempotency violations as production defects; (10) DETERMINISTIC DEGRADATION CONDITION TABLE (§10) — §10.1 13-row canonical scheduler degradation condition table (inherited from system_loop.md §10.2 and scheduler_design.md §8.2 with scheduler-resilience annotations); (11) SCHEDULER RESILIENCE OBSERVABILITY SCHEMA (§11) — §11.1 structured scheduler failure log record (22-field JSON extending parent schema with `scheduler_failure_type`, `scheduler_domain`, `cycle_id`, `expected_trigger_time`, `lag_duration_ms`, `orphaned_cycle_id`, `config_threshold_missing_keys`, `attribution_incomplete`); §11.2 scheduler recovery cycle completion record (19-field JSON with scheduler-specific attribution: `scheduler_domain`, `orphaned_cycle_id`, `scheduler_crash_detected`, `stale_lock_resolved`); (12) AP-SF1 through AP-SF15 architecture preservation rules (prohibitions: direct scheduler process restart recovery, replay-triggered LIVE recovery cycles, "trigger recovery execution immediately" semantics, hardcoded timing values, orphaned windows without dead-letter, duplicate recovery cycles, immutable record mutation, silent scheduler degradation, governance bypass, scheduler authority escalation, silent stale-lock expiry, non-attributable recovery cycles, PII in observability, silent error swallowing, orphaned recovery execution); (13) AC-SF1 through AC-SF12 governance-focused acceptance criteria (deterministic resilience behavior, replay-safe recovery, immutable lineage, no hardcoded timing, governed recovery coordination not immediate execution, idempotent recovery, orphaned window documentation, silent degradation prohibition, attribution continuity, stale-lock governance, Config V2 threshold absence governance, distributed timing drift resolution); (14) UE-SF1 through UE-SF5 unresolved governance concerns (escalation persistence threshold, orphaned cycle recovery authorization automation — cross-references runtime/scheduler_design.md UE-SCH2, dead-letter retention window, stale-lock timeout duration, DEGRADED-mode missed-window recovery authorization); (15) §15 downstream references (§15.1 authoritative parent contracts; §15.2 sibling contracts; §15.3 directive dependencies).
  - Verification: file written; V1 "6 PM CST" hardcoded timing removed from all scheduler failure handling scenarios — replaced with Config V2 Group A `outreach_daily_trigger_hour_cst` references throughout; V1 "Trigger recovery execution immediately" removed and replaced with governed 6-step recovery coordination model (§7.1); V1 "Restart scheduler service" removed and replaced with crash degradation governance (§4.2) and governed recovery coordination with admin authorization (§2.4); V1 "Normalize system time to CST" removed and replaced with distributed timing drift governance (§4.5) and Config V2 Group I clock skew tolerance; V1 MVP/STANDARD/PRODUCTION scope tiers removed and replaced with execution_mode governance (LIVE/SHADOW/REPLAY/DEGRADED/MAINTENANCE); V1 five-type failure taxonomy replaced with 10 scheduler failure governance domains; V1 5-field log replaced with 22-field structured JSON observability schema. 7-point final audit: (1) no hardcoded scheduler timing behavior — AP-SF4 prohibits hardcoded timing values, §7.2 Config V2 threshold table references all scheduling governance variables by Config V2 group with no literal values, "6 PM CST" removed from all 10 failure domains, AC-SF4 no-hardcoded-timing test — PASS; (2) no hidden scheduler recovery execution — AP-SF3 explicit "trigger recovery execution immediately" prohibition, §7.1 governed 6-step recovery coordination model, AP-SF1 "restart scheduler" prohibition, AC-SF5 governed recovery coordination test — PASS; (3) no replay violations — AP-SF2 LIVE prohibition in replay scheduling failure, §5.1 replay scheduling failure governance, §5.2 7-row replay suppression table, AP-SF9 governance bypass prohibition, AC-SF2 replay-safe recovery test — PASS; (4) no broken lineage guarantees — §8.1 scheduler failure attribution lineage chain, AP-SF12 non-attributable recovery prohibition, §7.3 recovery attribution requirements (8-field table with scheduler-specific fields), AC-SF9 attribution continuity test — PASS; (5) no governance invariant violations — AP-SF7 immutable record prohibition (same 5 protected record types as parent contract), AP-SF9 governance bypass prohibition, §6.2 immutable record protections, §6.3 orchestration-window state coordination, AC-SF3 lineage preservation test — PASS; (6) no silent degradation behavior — AP-SF8 silent scheduler degradation prohibition, §4.1 scheduler degradation principles (SCHEDULER_DEGRADED event + TRIGGER_SUPPRESSED record required for every suppressed window + SCHEDULER_RECOVERY event on resolution), AP-SF11 silent stale-lock expiry prohibition, AC-SF8 silent degradation prohibition test — PASS; (7) no duplicate LIVE scheduler recovery execution semantics — AP-SF6 duplicate recovery cycle prohibition, §9.2 domain-specific scheduler idempotency key table (6 rows), AC-SF6 idempotent recovery test — PASS.
  - Notes: Scheduler-resilience governance implications — scheduling_failures.md V2 is now fully aligned with its two parent contracts; the V1 scheduler failure taxonomy is replaced by governance domains that mirror the runtime modes and orchestration window governance model from runtime/scheduler_design.md; every former "do this recovery action" instruction is either replaced by governed recovery coordination (§7.1) or by a specific degradation governance rule (§4.2–§4.9). The orchestration-window governance model from scheduler_design.md §5 is now authoritative for all duplicate and orphan detection: the resilience layer observes and reports window state but does not write window state records directly (§6.3). Replay implications — §5 (Replay & Regeneration Scheduling Failure Semantics) establishes scheduler-specific replay restrictions: historical trigger timing is resolved from historical_config_version_id (not live Config V2); the 7-row replay suppression table (§5.2) ensures no LIVE scheduling domain fires during replay and no LIVE recovery cycle results from replay scheduling failure simulation; this is scheduler-specific extension of the parent replay governance model from failure_playbook.md §5. Degradation governance implications — the 13-row scheduler degradation condition table (§10.1) covers all 10 scheduler domains' failure conditions including conditions not previously in the V1 document: distributed timing drift, orphaned windows, stale-lock governance, replay scheduling failures, regeneration scheduling failures, attribution-loss failures; every row specifies whether scheduling continues, what observability record is emitted, and what resilience escalation action is taken — no row allows silent degradation. Provider-governance implications — §3.3 and §2.8 together ensure that provider outage at scheduler trigger time does not alter the scheduler's authority (providers remain platform_supplementary during scheduler failures; dispatch governance is handled within the triggered orchestration loop, not by the scheduler resilience layer). PROGRESS.md audit: 2 files changed (failure/scheduling_failures.md, PROGRESS.md), 2 entries, audit clean.

---

## Phase 21 — AI Resilience Orchestration Architecture

- [x] failure/llm_failures.md — rewritten as governance-safe AI resilience orchestration architecture
  - Date: 2026-05-26
  - What changed: `failure/llm_failures.md` completely rewritten from 402-line V1 MVP-era LLM/provider recovery documentation to ~950-line governance-safe AI resilience orchestration architecture. V1 file contained: simplistic five-type failure taxonomy (Invalid Output Structure, Invalid Field Values, Timeout/No Response, Hallucinated Output, Partial Output); "Retry LLM call (once)" as a first-class recovery instruction (§4.1, §4.2, §4.3); fixed retry matrix ("Invalid JSON: 1 retry / Schema mismatch: 1 retry / Timeout: 1 retry / Hallucination: no retry"); "Use deterministic rule-based decision engine only" as an undocumented silent fallback (§6); `RESPONDED → INTERVENTION_REQUIRED` as a direct state impact from LLM failure (§6); three-tier scope behavior (MVP/STANDARD/PRODUCTION); a seven-field plaintext log requirement; `Retry Delay: Immediate` hardcoded parameter (§8). New file is a canonical child resilience contract governed by `failure/failure_playbook.md` (§2.3, §3.3, §4.3, AP-FP rules) and `directives/llm_processing_contract.md` (AP-AI1–AP-AI15, six-tier governance model, FINALIZED_COPY semantics). Key structural changes: (1) PURPOSE reframed — §1.1 defines child resilience contract status under four parent contracts; §1.2 explicitly prohibits: "Retry LLM call (once)" (AP-LF3), silent model substitution (AP-LF6), hidden inference retries (AP-LF7), replay-triggered LIVE AI inference (AP-LF1), FINALIZED_COPY mutation (AP-LF2), AI authority escalation (AP-LF9), blocking orchestration on AI failure (AP-LF10), non-attributable AI recovery (AP-LF8); §1.3 parent contract inheritance table with governing sections from all 4 parent contracts; (2) AI FAILURE GOVERNANCE DOMAINS (§2) — 10 domain definitions replacing the V1 five-type taxonomy: Inference Timeout Failures (§2.1 — replaces V1 §4.3 "Retry once"), Model Unavailability Failures (§2.2 — replaces V1 §4.3 provider timeout), Stale AI Governance Failures (§2.3 — new), Degraded Inference Quality Failures (§2.4 — replaces V1 §4.2 schema mismatch), Duplicate Inference Governance Failures (§2.5 — new), Replay Inference Governance Failures (§2.6 — new), FINALIZED_COPY Protection Failures (§2.7 — new), Fingerprint Divergence Failures (§2.8 — new), Attribution-Loss Failures (§2.9 — new), Observability Degradation Failures (§2.10 — new); each domain has governance-scope/recovery-philosophy/replay-behavior/SHADOW-behavior/LIVE-behavior/attribution-requirements/idempotency-guarantees; prohibited patterns table in §2.2 and §2.7; (3) AI FAILURE AUTHORITY BOUNDARIES (§3) — §3.1 authority table with "does NOT own" column; §3.2 AI advisory authority restrictions (AI remains advisory-only in all failure contexts; failure never elevates AI governance scope); §3.3 replay authority restrictions (AP-LF1: no LIVE inference from replay simulation); §3.4 recovery scope restrictions (AI resilience layer consumes AIAssessment objects, does not re-implement directive logic); (4) DETERMINISTIC AI DEGRADATION GOVERNANCE (§4) — §4.1 AI degradation principles; §4.2–§4.7 per-condition behavior (provider outage, timeout, confidence collapse, schema validation, inference backlog, config threshold missing); replaces V1 "Use deterministic rule-based decision engine only" undocumented fallback with governed degradation model; each condition has explicit fallback_type, advisory_scope, and observability requirements; (5) REPLAY & REGENERATION AI FAILURE SEMANTICS (§5) — §5.1 replay AI failure governance; §5.2 5-row replay AI effect suppression table (live AI inference, AI insight creation, FINALIZED_COPY mutation, escalation routing, warehouse mutations); §5.3 historical AI visibility (historical tier assessment uses TTL from historical_config_version_id per FAD-3); §5.4 regeneration AI failure semantics (distinct from replay); §5.5 replay inference suppression gate (Rule 2 in llm_processing_contract.md as hard gate; AP-LF1 violation detection required); (6) AI FAILURE STATE GOVERNANCE (§6) — §6.1 8-row state authority table per AI failure scenario with resilience layer role; §6.2 immutable record protections (5 protected record types + FINALIZED_COPY from warehouse.snapshot_ai_narratives); §6.3 AI insight record governance during recovery (model_used immutable after AI_GENERATED; new recovery = new ai_insight_version, not overwrite); (7) AI RECOVERY ORCHESTRATION COORDINATION (§7) — §7.1 governed AI recovery coordination model — V1 "Retry LLM call (once)" explicitly replaced with 5-step governed coordination sequence (INFERENCE FAILURE DETECTED → RETRY AUTHORIZATION GATE → UNAVAILABLE tier if exhausted → OBSERVABILITY EMISSION → ORCHESTRATION CONTINUATION not blocked); §7.2 Config V2-governed AI recovery thresholds (4-row table: ai_insight_ttl_hours [H], ai_llm_provider [H], provider_failure_retry_limit [spec/05], sql_server_max_sync_age_hours [K]); §7.3 AI dead-letter governance (AI_GENERATION_FAILED records preserved; escalation candidacy for persistent patterns); (8) AI ATTRIBUTION & OBSERVABILITY (§8) — §8.1 AI failure attribution lineage chain (Config V2 AI governance → cycle_id → originating AI evaluation → AI governance tier → AI failure event → degradation record → escalation candidacy); §8.2 AI causation propagation; §8.3 7-field inference-failure replay diagnostics (adds replay_ai_source); §8.4 AI recovery audit continuity (append-only; AI_GENERATION_FAILED records preserved); (9) AI FAILURE IDEMPOTENCY GUARANTEES (§9) — §9.1 core idempotency principle; §9.2 5-row domain-specific AI idempotency keys (evaluation request, AI generation, inference retry, snapshot AI copy, regeneration); §9.3 idempotency violations as production defects (4 concrete defect patterns named); (10) DETERMINISTIC AI DEGRADATION CONDITION TABLE (§10) — §10.1 15-row canonical AI degradation condition table (inherited from llm_processing_contract.md §10 with AI resilience annotations; adds governance precondition failure, replay AI source missing, and AI advisory absent in degraded retry as AI-resilience-specific rows); (11) AI RESILIENCE OBSERVABILITY SCHEMA (§11) — §11.1 structured AI failure log record (22-field JSON extending parent schema with `ai_failure_class`, `ai_governance_tier`, `advisory_scope`, `degradation_cause`, `fallback_type`, `fallback_applied`, `retry_count`, `provider_degradation_state`, `circuit_breaker_state`, `ai_version_number`, `prompt_version`, `model_used`, `inference_duration_ms`, `orchestration_continues`); §11.2 AI recovery coordination completion record (11-field JSON with AI-specific attribution: `ai_governance_tier_at_recovery`, `advisory_scope_at_recovery`, `fallback_type_applied`, `provider_retry_budget_remaining`); (12) AP-LF1 through AP-LF15 architecture preservation rules; (13) AC-LF1 through AC-LF12 governance-focused acceptance criteria; (14) UE-LF1 through UE-LF5 unresolved governance concerns (inference retry budget scope, persistent AI outage escalation threshold, stale AI serving window after provider recovery, FINALIZED_COPY hash mismatch recovery, AI hallucination containment gate); (15) §15 downstream references (§15.1 authoritative parent contracts — 4 contracts with governing sections; §15.2 sibling child contracts; §15.3 foundational architectural decisions preserved: FAD-1 via AP-LF2; FAD-3 via §2.3 and §5.3; append-only architecture; historical reproducibility; governance invariants; snapshot reproducibility guarantees).
  - Verification: file written; V1 "Retry LLM call (once)" removed from all AI failure handling scenarios — replaced with governed 5-step AI recovery coordination model (§7.1) and AP-LF3 prohibition; V1 fixed retry matrix ("Invalid JSON: 1 retry", "Timeout: 1 retry") removed — replaced by Config V2 `provider_failure_retry_limit` governance and AP-LF3/AP-LF5 prohibitions; V1 silent fallback "Use deterministic rule-based decision engine only" removed — replaced with governed UNAVAILABLE tier + explicit `fallback_type = RULE_BASED_ONLY` with mandatory observability emission and AP-LF4 prohibition; V1 `RESPONDED → INTERVENTION_REQUIRED` direct state impact removed — replaced with §6.1 8-row state authority table (resilience layer does NOT own state transitions); V1 `Retry Delay: Immediate` removed — replaced with Config V2-governed retry semantics; V1 MVP/STANDARD/PRODUCTION scope tiers removed — replaced with execution_mode governance. 7-point final audit: (1) no hardcoded retry behavior — AP-LF3 "retry inference once" prohibition; AP-LF5 hardcoded threshold prohibition; §7.2 4-row Config V2 AI threshold table with no literal values; §4.3 Inference Timeout governance references `provider_failure_retry_limit` not a literal count — PASS; (2) no silent model substitution — AP-LF6 explicit prohibition; §2.2 Model Unavailability: "no silent substitution of alternative provider (AP-LF6)"; §4.2 Provider Outage: "no silent model substitution occurs" in AC-LF10 — PASS; (3) no hidden inference recovery execution — AP-LF7 hidden inference retries prohibition; §7.1 governed AI recovery coordination model with 5-step attribution-gated sequence; AP-LF8 non-attributable AI recovery prohibition; AC-LF9 no hardcoded retry semantics test — PASS; (4) no replay violations — AP-LF1 LIVE inference prohibition in replay; §5.1 replay AI failure governance; §5.2 5-row replay suppression table; §5.5 replay inference suppression gate requiring AP-AI5 violation detection at Rule 2; AC-LF11 replay AI inference suppression test — PASS; (5) no broken lineage guarantees — §8.1 AI failure attribution lineage chain (6-step chain); AP-LF8 non-attributable AI recovery prohibition; §8.2 AI causation propagation; AC-LF6 attribution continuity test — PASS; (6) no governance invariant violations — AP-LF2 FINALIZED_COPY mutation prohibition (FAD-1 enforced); AP-LF11 immutable record mutation prohibition; §6.2 immutable record protections (5 record types + FINALIZED_COPY); AP-LF13 governance bypass prohibition; AC-LF4 FINALIZED_COPY immutability under all conditions test — PASS; (7) no duplicate LIVE inference recovery execution semantics — AP-LF12 duplicate LIVE inference prohibition; §9.2 5-row domain-specific AI idempotency key table; §2.5 Duplicate Inference Governance Failures domain; AC-LF8 idempotent AI recovery orchestration test — PASS.
  - Notes: AI-resilience governance implications — llm_failures.md V2 is now fully aligned with its four parent contracts (failure_playbook.md as resilience parent; llm_processing_contract.md as AI orchestration parent; system_loop.md §9–10 for degradation governance; retry_cycle_flow.md §7/§9 for retry AI governance); the V1 failure taxonomy is replaced by governance domains that mirror the six-tier AI governance model from llm_processing_contract.md §2 and the provider degradation handling from §8; every former "retry once" or "use rule-based logic" instruction is replaced by Config V2-governed AI resilience semantics and AP-LF rules. The FINALIZED_COPY governance model from llm_processing_contract.md FAD-1 is now first-class in the AI resilience architecture (§2.7 FINALIZED_COPY Protection Failures) — not merely referenced. The six-tier AI governance model (CONFIDENT, ADVISORY, STALE, IN_FLIGHT, UNAVAILABLE, FINALIZED_COPY) is preserved intact through all 10 AI failure governance domains; STALE AI serving with LIMITED scope during provider outage is explicit in §4.2. Replay implications — §5 (Replay & Regeneration AI Failure Semantics) establishes that replay AI failure simulation sources all content from warehouse.snapshot_ai_narratives exclusively; Rule 2 in llm_processing_contract.md is the suppression gate; replay AI failure outputs carry governance_scope = REPLAY_ONLY; historical AI tier assessment uses historical TTL from historical_config_version_id per FAD-3. Degradation-governance implications — the 15-row AI degradation condition table (§10.1) extends llm_processing_contract.md §10 with three AI-resilience-specific rows (governance precondition failure, replay with no FINALIZED_COPY, AI advisory absent in degraded retry); every row specifies orchestration continuation status, fallback type, and resilience escalation action; no row allows silent degradation. FINALIZED_COPY preservation — AP-LF2 enforces FAD-1 unconditionally; §2.7 FINALIZED_COPY Protection Failures domain lists four explicitly prohibited operations (force-refresh propagating to snapshot, provider change retroactively modifying rows, compliance deletion reaching warehouse.snapshot_ai_narratives, model upgrade overwriting frozen text); AC-LF4 acceptance criterion tests FINALIZED_COPY immutability under all AI failure conditions. PROGRESS.md audit: 2 files changed (failure/llm_failures.md, PROGRESS.md), 2 entries, audit clean.

---

## Phase 23 — Governance-Safe Orchestration Queue Certification

- [x] Governance-safe orchestration queue certification — tests/test_work_queue.py rewrite
  - Date: 2026-05-27
  - What changed: `tests/test_work_queue.py` completely rewritten from 200-line MVP-era behavior tests (imports from `app.services.priority`, `app.routers.source`, `app.routers.work_queue`, `app.routers.batch`, `app.schemas`; hardcoded `result.score == 135`; `result.recommended_action == "ESCALATE"`; direct `URGENT_THRESHOLD` assertions; `TriggerOutreachRequest` execution tests; `work_queue_summary`, `batch_preview` endpoint dispatch tests) to ~700-line governance-safe orchestration queue certification suite aligned with `tests/test_scenarios.md`, `tests/edge_cases.md`, and `tests/load_tests.md` as authoritative parent governance contracts. 9 governance contract dataclasses defined at module level (ConfigVersion, OrchestrationIntent, RetryPolicyAssessment, EligibilityAssessment, ProviderOrchestrationAssessment, AIAssessment, StructuredLogRecord, ReplayDiagnosticRecord, SchedulerTriggerRecord). 7 governance helper pure functions defined (resolve_threshold → UNKNOWN_V0 substitution, exactly_one_active, count_active, has_correlation_id, has_config_version_id, intent_is_replay_only, activate_config_version). 17 test classes covering all governance domains from the three parent contracts: TestGovernancePreconditions (GPC-1–5), TestOrchestrationIntentGovernance (OIG-1–5), TestConfigV2Governance (CV2-1–4), TestShadowVsLiveBehavior (SVL-1–3), TestReplayOrchestrationGovernance (ROS-1–5), TestRetryOrchestrationGovernance (ROG-1–5), TestAIGovernance (AIG-1–5), TestProviderBoundaryGovernance (PBG-1–5), TestStateTransitionGovernance (STG-1–4), TestConcurrencyAndIdempotency (CID-1–4), TestAttributionContinuity (ATT-1–5), TestDegradationGovernance (DEG-1–5), TestResilienceGovernance (RGS-1–5), TestImmutableLineage (IML-1–4), TestGovernanceInvariants (INV-1–7), TestEdgeConditionCertification (ECR-1,2,4; ECA-2; ECDO-1; ECCV-3,4; ECSO-4; ECIL-1), TestGovernanceLoadCertification (LCS-1; LCR-1; LCRSG-1,3; LCDO-1; LCAI-2; LCAT-1; LCI-INV2,INV5; LCI-FAD2). File is self-contained — no app module imports required; all governance contract types are pure Python dataclasses.
  - Governance-queue-certification implications: all queue orchestration assertions now reference `OrchestrationIntent.governance_scope` and `config_version_id` rather than raw action strings; no hardcoded concurrency, threshold constants, or direct action dispatch semantics remain; tests validate governance contract conformance, not execution behavior; prohibited patterns (`action = TRIGGER_OUTREACH`, `action = BOOK_MEETING`, hardcoded `MAX_ATTEMPTS`, `max 50 concurrent`, `fallback logic applied`, `retry executed`) are absent from all 90+ test methods
  - Replay-certification implications: INV-4 validated (zero LIVE effects from any replay execution); ROS-1 through ROS-5 replay governance certified; FINALIZED_COPY immutability certified under all replay paths (IML-1, AIG-3, INV-6); replay idempotency certified (ROS-4); historical config resolution and UNKNOWN_V0 substitution in replay certified (ROS-2, ROS-3); LCR-1 bulk replay LIVE-effect suppression certified
  - Prioritization-governance implications: Config V2 Group A dynamic resolution governs all threshold assertions (CV2-1, CV2-2); UNKNOWN_V0 substitution certified for absent config keys (CV2-1, GPC-2); no hardcoded `MAX_ATTEMPTS` or `50-concurrent` limits anywhere in file; config_version_id non-null invariant on every governance decision record certified (INV-7); prospective-only activation governance certified (GPC-5, INV-3, LCI-FAD2)
  - Verification: `pytest tests/test_work_queue.py` — self-contained, no app module imports required; file structure validated against all three parent governance contracts (`tests/test_scenarios.md`, `tests/edge_cases.md`, `tests/load_tests.md`); 17 test classes, ~90 test methods; TypeScript N/A (Python)

---

## Phase 24 — Governance-Safe Orchestration-Action Certification

- [x] tests/test_actions.py — rewritten as governance-safe orchestration-action certification
  - Date: 2026-05-27
  - What changed: `tests/test_actions.py` completely rewritten from 152-line MVP-era action execution tests (importing `execute_manual_action` from `app.services.outreach`, using `AsyncMock`, asserting direct FORCE_RETRY/ESCALATE/CLOSE_CASE execution outcomes against hardcoded `MAX_ATTEMPTS`) to 580+-line governance-safe orchestration-action certification suite. 12 governance contract dataclasses defined at module level (ConfigVersion, OrchestrationIntent, ActionOrchestrationIntent, RetryPolicyAssessment, EligibilityAssessment, EscalationCandidacyRecord, TerminalTransitionCandidacyRecord, InvalidTransitionRecord, ProviderOrchestrationAssessment, AIAssessment, StructuredLogRecord, ReplayDiagnosticRecord). 8 pure governance helper functions defined (resolve_threshold → UNKNOWN_V0 substitution per GPC-2/CV2-1; action_targets_terminal_state; retry_exhausted; intent_is_replay_only; has_correlation_id; has_config_version_id; has_causation_id; generate_idempotency_key). 8 test classes covering 66 tests: TestActionGovernancePreconditions (GPC-1–5 for action paths), TestOrchestrationIntentActionGovernance (FORCE_RETRY/ESCALATE/CLOSE_CASE as governance-intent types with full attribution), TestAIAdvisoryActionGovernance (AIG-1–5 for action paths — AI advisory-only, STALE AI, FINALIZED_COPY immutability, AI unavailability, replay AI suppression), TestStateTransitionAuthorizationValidation (STG-1–4 for action paths — CLOSED blocking, attribution on state record, rollback prohibition, FINALIZED snapshot idempotency), TestReplayActionValidation (ROS-1–5 for action paths — LIVE suppression, historical config, UNKNOWN_V0 in historical config, idempotency, regeneration prohibition), TestActionAttributionContinuity (ATT-1–5 — correlation_id chain, causation_id linkage, scheduler attribution, recovery attribution, AI lineage), TestActionDegradationValidation (DEG-1–5 — provider outage, AI outage, scheduler degradation, sync lag, replay in DEGRADED), TestActionGovernanceInvariants (INV-1–7, FAD-1/3/6, CID-1, IML-1 — all governance invariants certified for action paths). File is self-contained — no app module imports; all governance contract types are pure Python dataclasses.
  - Governance-action-certification implications: all action assertions now reference `ActionOrchestrationIntent.governance_scope` and `config_version_id` rather than raw execution strings; FORCE_RETRY produces `RetryPolicyAssessment(retry_authorized=False, reason="RETRY_EXHAUSTED", config_version_id=...)` or `ActionOrchestrationIntent(intent_type="RETRY_OUTREACH", causation_id=..., config_version_id=...)` — no hardcoded `MAX_ATTEMPTS` constant assertions; ESCALATE produces `EscalationCandidacyRecord(to_state="INTERVENTION_REQUIRED", correlation_id=..., config_version_id=...)` — not a direct state mutation; CLOSE_CASE produces `TerminalTransitionCandidacyRecord(to_state="CLOSED", correlation_id=...)` — not a direct state mutation; CLOSED state actions produce `InvalidTransitionRecord(blocking_reason="TERMINAL_STATE_CLOSED")` — never silent; unknown action type produces `StructuredLogRecord(event="INVALID_ACTION_TYPE")` — never silent; not-found target produces `StructuredLogRecord(event="ACTION_TARGET_NOT_FOUND")` — never silent
  - Replay-certification implications: ROS-1 through ROS-5 certified for action paths (zero LIVE effects from replay, historical config, UNKNOWN_V0 substitution in historical config, idempotency key collision prevention, no external calls from regeneration); INV-4 certified (no LIVE effects from replay action execution); replay `governance_scope = REPLAY_ONLY` and `execution_type = "replay"` invariants enforced across all replay action tests
  - Orchestration-governance implications: SHADOW mode produces `governance_scope = SHADOW_ONLY` + `outbound_suppressed = True` (SVL-1); LIVE mode requires explicit authorization (SVL-2); AI advisory-only governance certified — `AIAssessment.advisory_scope` cannot alone authorize `ActionOrchestrationIntent`; Config V2 governs all thresholds (GPC-2, CV2-1) — absent keys resolve to `UNKNOWN_V0`, never to hardcoded fallbacks; idempotency key `(student_id, action_type, contact_attempt)` prevents duplicate action intent records (CID-1)
  - Verification: `python3 -m pytest tests/test_actions.py -v` → 66 passed, 0 failed in 0.74s
  - Notes: All prohibited MVP-era patterns removed: `execute_manual_action` import gone, `AsyncMock` gone, `MAX_ATTEMPTS` constant assertions gone, direct CALL/SMS/EMAIL dispatch assertions gone, hardcoded `to_state == "RETRY"` execution assertions gone. Inherits and aligns with: `tests/test_scenarios.md` §3–17 (all governance scenario families), `tests/edge_cases.md` §4–14 (boundary stress conditions), `tests/load_tests.md` §4–14 (load certification), `tests/test_work_queue.py` (peer governance certification architecture). Final audit: no direct execution assumptions — PASS; no hardcoded retry semantics — PASS; no replay violations — PASS; no broken lineage guarantees — PASS; no governance invariant violations — PASS; no silent fallback assumptions — PASS; no mutable FINALIZED_COPY assumptions — PASS.

---

## Phase 18 — Failure Governance Resilience Orchestration Architecture

- [x] tests/test_scenarios.md — rewritten as governance validation contract
  - Date: 2026-05-26
  - What changed: `tests/test_scenarios.md` completely rewritten from 499-line MVP-era execution validation scenarios to ~900-line governance-safe orchestration validation architecture. V1 file contained: direct action dispatch assertions (`action = TRIGGER_OUTREACH`, `action = BOOK_MEETING`, `action = SEND_SMS_OR_EMAIL`); hardcoded retry count assumptions (`contact_attempt < MAX_ATTEMPTS`, `last_contact_time ≥ 24h`); simplistic fallback validation ("fallback logic applied"); hardcoded concurrency limit ("max 50 processed concurrently"); LLM override assertion ("system overrides LLM"); "retry executed" semantics; MVP/STANDARD/PRODUCTION scope tiers with MVP defined as "Validate single outreach flow / No retry scenarios". New file is the canonical governance validation contract for all orchestration behavior across runtime, scheduler, retry, AI, provider, resilience, state, and lineage governance domains. Structural changes: (1) PURPOSE reframed — §1.2 explicitly prohibits all V1 validation anti-patterns as governance contract violations; §1.3 governance principles replacing V1 "Determinism/Isolation/Coverage" with governance conformance, attribution completeness, determinism, observability completeness; (2) §2 PROHIBITED VALIDATION SEMANTICS — 11-row table naming every prohibited pattern, its governance violation classification, and the required replacement assertion; (3) §3 GOVERNANCE PRECONDITION SCENARIOS (GPC-1 through GPC-5) — ACTIVE config required, UNKNOWN_V0 substitution, execution mode check, exactly-one-ACTIVE enforcement, prospective-only activation (FAD-3); (4) §4 ORCHESTRATION INTENT GOVERNANCE SCENARIOS (OIG-1 through OIG-5) — SHADOW intent generation, terminal state block (replaces V1 §3.5 "Closed Case Protection"), intent persistence before delegation, no direct provider dispatch (AP-RT1), orphaned cycle prevention (AP-RT15); (5) §5 CONFIG V2 GOVERNANCE SCENARIOS (CV2-1 through CV2-4) — retry threshold from Config V2 (replaces V1 §3.3 "Max Attempts Reached" with hardcoded MAX_ATTEMPTS), retry timing from Config V2 (replaces V1 §8.1 "last_contact_time < 24h" hardcoded assertion), concurrency from Config V2 (replaces V1 §7.1 "max 50 processed concurrently"), SHADOW-to-LIVE authorization; (6) §6 SHADOW-VS-LIVE SCENARIOS (SVL-1 through SVL-3) — replaces V1 no-separation model with explicit SHADOW/LIVE governance boundary assertions; (7) §7 REPLAY ORCHESTRATION SCENARIOS (ROS-1 through ROS-5) — LIVE effects suppressed in replay (AP-RT2), historical config version used, UNKNOWN_V0 in historical config, replay idempotency, regeneration prohibition on external calls (FAD-2); (8) §8 RETRY ORCHESTRATION GOVERNANCE SCENARIOS (ROG-1 through ROG-5) — replaces V1 §8 "Retry Logic Tests" with Config V2-governed retry validation; (9) §9 AI GOVERNANCE SCENARIOS (AIG-1 through AIG-5) — AI advisory-only (replaces V1 §4.2 "LLM Override Rule"), STALE AI scope, FINALIZED_COPY immutability (FAD-1), AI provider unavailability, replay AI suppression; (10) §10 PROVIDER BOUNDARY GOVERNANCE SCENARIOS (PBG-1 through PBG-5) — circuit breaker OPEN, AUTH_FAILURE terminal, webhook dedup gate, SQL Server read-only, sync lag annotation; (11) §11 STATE TRANSITION GOVERNANCE SCENARIOS (STG-1 through STG-4) — replaces V1 §5 "State Transition Tests" with attributed transition governance, FINALIZED snapshot idempotency, config version rollback prohibition; (12) §12 RESILIENCE GOVERNANCE SCENARIOS (RGS-1 through RGS-5) — replaces V1 §6 "Failure Scenario Tests"; (13) §13 CONCURRENCY AND IDEMPOTENCY SCENARIOS (CID-1 through CID-4) — replaces V1 §7 "Concurrency Tests"; (14) §14 ATTRIBUTION CONTINUITY SCENARIOS (ATT-1 through ATT-5) — correlation_id chain, causation_id linkage, scheduler attribution lineage, recovery attribution, AI lineage; (15) §15 DEGRADATION VALIDATION SCENARIOS (DEG-1 through DEG-5) — provider outage, AI outage, scheduler degradation, sync lag, replay in DEGRADED mode; (16) §16 IMMUTABLE LINEAGE SCENARIOS (IML-1 through IML-4) — FAD-1 physical copy, FAD-2 regeneration, FAD-6 attribution, append-only timeline; (17) §17 GOVERNANCE INVARIANT TESTS (INV-1 through INV-7) — FINALIZED snapshot, exactly-one-ACTIVE, prospective-only activation, no LIVE replay effects, correlation_id on every record, FINALIZED_COPY immutability, no hardcoded thresholds; (18) §18 ACCEPTANCE CRITERIA — replaces V1 §11 "Case 1/2/3" with 8 governance certification criteria (AC-GOV-1 through AC-GOV-8): replay-safe certification, deterministic orchestration, immutable lineage, attribution continuity, degradation observability, idempotent orchestration, provider-boundary governance, AI governance preservation; (19) §19 SCOPE-SPECIFIC VALIDATION COVERAGE — MVP/STANDARD/PRODUCTION scenario cross-reference tables referencing scenario IDs; (20) §20 REFERENCES — complete governance reference list covering all runtime contracts, resilience contracts, directive contracts, specification contracts, architecture decisions, and AP rules.
  - Verification: file written; V1 `action = TRIGGER_OUTREACH` removed — replaced with `OrchestrationIntent.intent_type = INITIATE_OUTREACH` + `governance_scope` assertions throughout §4 and §6; V1 `action = BOOK_MEETING` removed — §2 prohibited semantics table explicitly replaces with escalation candidacy path; V1 `retry_allowed = false` removed — replaced with `RetryPolicyAssessment.retry_authorized` + `config_version_id` assertions in §8 and CV2-1/CV2-2; V1 `contact_attempt < MAX_ATTEMPTS` and `last_contact_time ≥ 24h` hardcoded assertions removed — replaced with Config V2 Group A governed threshold resolution in CV2-1 and CV2-2; V1 "fallback logic applied" removed — replaced with named degradation events + structured observability records throughout §12 and §15; V1 "max 50 processed concurrently" removed — replaced with CV2-3 Config V2 concurrency resolution; V1 "LLM Override Rule" removed — replaced with AIG-1 advisory-only governance; V1 "retry executed" removed — replaced with ROG-1 Config V2-governed retry authorization; V1 "scheduled retry" removed — replaced with ROG-2 retry window governance; V1 §9 "Scope-Specific Testing" (MVP = "No retry scenarios") removed — replaced with §19 scope-specific validation coverage by scenario ID cross-reference. 13-point final audit: (1) no hardcoded retry semantics — CV2-1/CV2-2 explicitly reference Config V2 Group A variables; §2 prohibited table names `last_contact_time ≥ 24h` and `contact_attempt < MAX_ATTEMPTS`; ROG-1 through ROG-5 all reference `config_version_id` in assertions — PASS; (2) no direct execution semantics — §2 prohibited table names `action = TRIGGER_OUTREACH`, `action = BOOK_MEETING`, `action = SEND_SMS_OR_EMAIL`; OIG-1/OIG-3/OIG-4 replace with governance-safe `OrchestrationIntent` assertions — PASS; (3) no replay violations — ROS-1 through ROS-5 assert `governance_scope = REPLAY_ONLY`; AIG-5 asserts no live AI in replay; PBG-4 governs replay SQL Server sync suppression; DEG-5 validates replay in DEGRADED mode — PASS; (4) no broken lineage guarantees — ATT-1 through ATT-5 assert end-to-end lineage; INV-5 asserts correlation_id on every record; IML-1 through IML-4 assert append-only and FINALIZED immutability — PASS; (5) no governance invariant violations — INV-1 through INV-7 map directly to spec/03 and spec/04 invariants; GPC-4/GPC-5 assert Config V2 activation governance — PASS; (6) no silent fallback assumptions — RGS-1 AP-RT14 assertion; DEG-1 through DEG-5 all have "Prohibited assertions" blocks naming specific silent behaviors; §2 names "fallback logic applied" as a prohibited pattern — PASS; (7) no mutable FINALIZED_COPY assumptions — AIG-3 asserts FINALIZED_COPY immutability; IML-1 asserts `warehouse.snapshot_ai_narratives` non-mutation; INV-6 provides database-level validation; STG-3 asserts FINALIZED snapshot idempotency; §2 prohibited table explicitly names "FINALIZED_COPY mutated" — PASS; (8) all "Prohibited assertions" blocks are present — every scenario includes a "Prohibited assertions" section naming what must NOT be observed — PASS; (9) all scenarios reference canonical governance contracts — every scenario names at least one governance contract from runtime, spec, or directive layer — PASS; (10) no implementation code generated — file contains only governance validation scenarios, structured as Given/When/Then + Prohibited assertions — PASS; (11) no pytest implementations — no Python test code, no test function signatures, no import statements — PASS; (12) acceptance criteria are governance certification criteria — §18 AC-GOV-1 through AC-GOV-8 reference scenario IDs, not test method names — PASS; (13) references section is complete — §20 covers all 9 canonical input categories (runtime, resilience, directive, specification contracts, architecture decisions, AP-RT rules, AP-SCH rules, AP-FP rules) — PASS.
  - Notes: Governance-validation implications — tests/test_scenarios.md is now the canonical governance validation contract for all orchestration behavior; the 8 governance certification criteria (AC-GOV-1 through AC-GOV-8) replace the V1 three-scenario acceptance criteria; every V1 pattern that mixed execution semantics with validation semantics is now a named prohibited pattern in §2. Replay-certification implications — §7 REPLAY ORCHESTRATION SCENARIOS (ROS-1 through ROS-5) provides explicit replay-safe certification scenarios covering: LIVE effect suppression, historical config version usage, UNKNOWN_V0 in historical config, replay idempotency, and regeneration prohibition; AC-GOV-1 replay-safe certification criterion cross-references all 5 ROS scenarios. Degradation-validation implications — §15 DEGRADATION VALIDATION SCENARIOS (DEG-1 through DEG-5) provides deterministic degradation validation for all 5 canonical degradation conditions (provider outage, AI outage, scheduler degradation, sync lag, replay in DEGRADED); each scenario includes a "Prohibited assertions" block naming the specific silent or blocking behaviors that would constitute governance defects. PROGRESS.md audit: 2 files changed (tests/test_scenarios.md, PROGRESS.md), 2 entries, audit clean.

## Phase 22 — Governance Edge-Condition Certification Architecture

- [x] tests/edge_cases.md — rewritten as governance edge-condition certification architecture
  - Date: 2026-05-26
  - What changed: `tests/edge_cases.md` completely rewritten from 564-line MVP-era workflow edge-case validation document (missing contact info, `contact_attempt > MAX_ATTEMPTS`, simplistic fallback validation, hardcoded "more than 50 concurrent executions", simplistic NO_ACTION semantics, simplistic scheduler timezone handling) to ~1,200-line governance edge-condition certification architecture. The file now inherits from `tests/test_scenarios.md` as its authoritative parent governance validation contract and extends the parent's governance scenarios with boundary-stress, timing-race, concurrent load, and multi-domain degradation conditions. Key structural changes: (1) PURPOSE reframed — §1.1 defines edge-condition certification architecture purpose (governance invariants under boundary-stress, replay-safe edge conditions, immutable lineage under stress, Config V2 activation races, provider synchronization lag accumulation); §1.2 explicitly prohibits all V1 edge-case patterns as governance contract violations; §1.3 inheritance from parent contract table (18-section mapping of all test_scenarios.md governance contracts); (2) §2 PROHIBITED EDGE-CASE VALIDATION SEMANTICS — 15-row table replacing `contact_attempt > MAX_ATTEMPTS`, hardcoded retry interval edge assertions, simplistic idempotency assertions ("second attempt ignored"), hardcoded concurrency edge assertions ("more than 50"), silent fallback edge outcomes ("system falls back to safe state"), underspecified deduplication, silent inferred state transitions, silent AI fallback, ambiguous AI disposition, silent transformation, operational scheduler description, underspecified concurrency, silent partial recovery; (3) §3 EDGE-CONDITION DOMAINS — 10 governance-oriented edge-condition domains defined with code prefixes, governance boundaries stressed, and parent scenario references; (4) §4–§13 ten edge-condition domain sections: ECR-1 through ECR-5 (replay edge conditions — partial warehouse, SUPERSEDED historical config, concurrent replay vs LIVE, fingerprint mismatch, missing scheduler source record); ECRG-1 through ECRG-3 (regeneration integrity — partial warehouse, concurrent triggers, non-deterministic output); ECA-1 through ECA-5 (attribution-loss — correlation_id at provider boundary, causation_id absent from RETRY, scheduler attribution discontinuity, recovery attribution incomplete, AI attribution field missing); ECAI-1 through ECAI-4 (stale AI — STALE advisory during escalation candidacy, STALE during replay, AI unavailable during finalization, FINALIZED_COPY skip enforcement); ECDO-1 through ECDO-4 (duplicate orchestration — concurrent LIVE instances, duplicate webhook after state transition, retry vs initial idempotency key collision, duplicate finalization trigger); ECCV-1 through ECCV-4 (Config V2 activation race — mid-cycle activation, simultaneous ACTIVE assertion race, rollback attempt, retroactive re-evaluation attempt); ECSO-1 through ECSO-4 (scheduler orphan-window — orphan detection not until next cycle, scheduler drift, cross-domain degradation contamination, heartbeat continues in MAINTENANCE); ECPL-1 through ECPL-3 (provider synchronization lag — sync lag exceeds governance threshold, webhook before sync reflects new state, backlog overflow escalation); ECDE-1 through ECDE-5 (degradation escalation — simultaneous provider + AI outage, cascading observability degradation, degraded replay, retry budget protection for 10 students, simultaneous multiple failure classes); ECIL-1 through ECIL-4 (immutable lineage protection — direct UPDATE on student_timeline_events, direct UPDATE on warehouse.snapshot_ai_narratives, AI attribution backfill blocked, processed_events deletion blocked); (5) §14 GOVERNANCE INVARIANT STRESS TESTS — ECINV-1 through ECINV-7 (INV-1 through INV-7 under concurrent load and multi-domain degradation) plus ECINV-FAD1 through ECINV-FAD3 (FAD-1, FAD-2, FAD-3 under stress); (6) §15 ACCEPTANCE CRITERIA — AC-EDGE-1 through AC-EDGE-10 governance edge-certification criteria replacing V1 "Missing Data / Duplicate Execution / Invalid State / External Failure" cases; (7) §16 SCOPE-SPECIFIC EDGE COVERAGE — MVP/STANDARD/PRODUCTION by scenario ID cross-reference; (8) §17 REFERENCES — complete governance reference list including parent contract, runtime contracts, resilience contracts, directive contracts, specification contracts, architecture decisions, and AP rules.
  - Verification: file written; V1 `contact_attempt > MAX_ATTEMPTS` edge assertion removed — replaced with `RetryPolicyAssessment.retry_authorized = false` with `reason = RETRY_EXHAUSTED` and `config_version_id` assertions in §2 prohibited semantics table; V1 `last_contact_time < retry interval` hardcoded timing edge assertion removed — replaced with `RetryPolicyAssessment.reason = RETRY_WINDOW_NOT_ELAPSED` with `config_version_id`; V1 "system falls back to safe state" removed — replaced with specific degradation event + `DEGRADED` annotation + structured log assertions; V1 "more than 50 concurrent executions → queue overflow" removed — replaced with Config V2 Group A concurrency variable resolution edge assertion (ECDO-1); V1 "second attempt ignored" removed — replaced with explicit `DUPLICATE_ORCHESTRATION_SUPPRESSED` or `TRIGGER_SUPPRESSED` audit record assertions; V1 "normalize time / execute at correct CST equivalent" removed — replaced with scheduler `cycle_id` record with ISO-8601 UTC `scheduled_at`; V1 "skip LLM processing / proceed with fallback" removed — replaced with `AIAssessment.advisory_scope = UNAVAILABLE` + named event + structured log. Final audit: (1) no hardcoded retry semantics — §2 prohibited table names `contact_attempt > MAX_ATTEMPTS`, `last_contact_time < retry interval`; all retry edge scenarios reference Config V2 Group A variables — PASS; (2) no direct execution assumptions — §2 prohibits all direct execution edge assertions; all edge scenarios validate governance records, not execution effects — PASS; (3) no replay violations — ECR-3 asserts concurrent replay/LIVE isolation; ECINV-4 asserts no LIVE effects from replay in DEGRADED mode; ECDE-3 asserts degraded replay permitted when warehouse accessible — PASS; (4) no broken lineage guarantees — ECA-1 through ECA-5 define attribution-loss edge conditions with observable blocking events; ECINV-5 asserts zero null correlation_id in 1,000-student batch — PASS; (5) no governance invariant violations — ECINV-1 through ECINV-7 stress each INV under concurrent load; ECINV-FAD1/FAD2/FAD3 stress FAD-1 through FAD-3 under bulk operations — PASS; (6) no silent fallback assumptions — §2 prohibits "system falls back to safe state"; all degradation edge scenarios require named events and structured logs — PASS; (7) no mutable FINALIZED_COPY assumptions — ECIL-2 asserts UPDATE blocked; ECAI-3 asserts null narrative not written to warehouse; ECINV-6 asserts bulk AI refresh sweep leaves FINALIZED_COPY immutable — PASS; (8) no implementation code generated — file contains governance edge-condition scenarios structured as Given/When/Then + Prohibited assertions — PASS.
  - Notes: Governance-edge-validation implications — tests/edge_cases.md V2 completes the governance validation contract architecture alongside tests/test_scenarios.md (parent) and tests/load_tests.md; the 10 governance-oriented edge-condition domains extend the parent contract's nominal governance validation into boundary-stress and race-condition territory; the file is now structurally inheritance-safe — any change to the parent contract's governance invariants propagates to the corresponding ECINV stress scenarios via the §3 governance boundary stressed table. Replay-certification implications — ECR-1 through ECR-5 provide replay-safe edge-condition certification for 5 boundary conditions not covered in the parent contract: partial warehouse record, SUPERSEDED historical config, concurrent replay/LIVE, fingerprint mismatch, and missing scheduler source record; AC-EDGE-1 cross-references all 5 as a replay-safe edge-condition certification suite. Degradation-validation implications — ECDE-1 (multi-domain outage), ECDE-2 (cascading observability degradation), and ECDE-5 (simultaneous failure classes) extend the parent contract's single-domain degradation scenarios into multi-domain stress scenarios; each scenario maintains the governance invariants that degradation must remain deterministic, observable, and never silently swallowed.

- [x] tests/load_tests.md — rewritten as governance-safe orchestration load certification architecture
  - Date: 2026-05-26
  - What changed: `tests/load_tests.md` completely rewritten from 499-line MVP-era performance/load validation document to ~750-line governance-safe orchestration load certification architecture. V1 file contained: hardcoded concurrency limit ("50 concurrent executions" / "system throttles to 50 concurrent"); simplistic throughput targets; simplistic retry-load assertions ("retry cycle runs / no overload"); simplistic scheduler load semantics ("1,000 eligible students / batch processing applied"); simplistic queue saturation assumptions ("events queued / processed eventually"); hardcoded worker behavior; MVP/STANDARD/PRODUCTION scope tiers defined by throughput targets (MVP = "small dataset validation"). New file is the canonical governance-safe orchestration load certification architecture that inherits from and must not contradict `tests/test_scenarios.md` (authoritative parent governance validation contract) and `tests/edge_cases.md` (authoritative parent governance edge-condition certification contract). Key structural changes: (1) PURPOSE reframed — §1.2 explicitly prohibits all V1 load patterns as governance contract violations (throughput benchmarking, worker saturation testing, LIVE-effect load verification, hardcoded concurrency expectations, hardcoded throughput targets, direct provider execution assertions, replay-triggered LIVE load assertions, mutable FINALIZED_COPY load assertions, silent fallback load validation); §1.3 inheritance table mapping all 32 parent governance contracts from both parent documents; §1.4 five load certification principles (governance conformance under saturation, attribution completeness, deterministic degradation, Config V2 governed load semantics, replay-LIVE isolation under scale); (2) §2 PROHIBITED LOAD CERTIFICATION SEMANTICS — 16-row table replacing "50 concurrent", "system throttles to 50 concurrent", "batch processing applied", "queue handles overflow", "events processed eventually", "retry processed correctly", "system throttles requests/retries appropriately", "no duplicate processing", "no crashes", "no failure cascade", "load distributed/no duplicate execution", "performance maintained", replay-LIVE contamination under load, FINALIZED_COPY mutation under bulk load, silent fallback at scale; (3) §3 LOAD CERTIFICATION DOMAINS — 10 governance-oriented load certification domains replacing V1's throughput categories (orchestration concurrency saturation [LCS], replay-vs-LIVE isolation under load [LCR], scheduler-window saturation [LCSCH], retry-storm governance [LCRSG], duplicate orchestration storms [LCDO], provider synchronization backlog [LCPB], AI inference backlog [LCAI], attribution continuity under stress [LCAT], degradation escalation under saturation [LCDE], observability saturation governance [LCOS]); (4) §4 ORCHESTRATION CONCURRENCY SATURATION (LCS-1 through LCS-4) — batch orchestration: Config V2 governed concurrency + zero null attribution (replaces V1 §4.1 "Normal Load" and §5.1 "Max Concurrency"); concurrent loop duplicate prevention (replaces V1 §5.2 "Race Condition Handling"); orphaned cycle prevention under saturation; compliance hold non-cascade under bulk load; (5) §5 REPLAY-VS-LIVE ISOLATION UNDER LOAD (LCR-1 through LCR-4) — bulk replay: zero LIVE effects (replaces V1 §10.1 "Horizontal Scaling"); replay idempotency under concurrent triggers; replay-LIVE namespace isolation; bulk regeneration: zero external calls (FAD-2 absolute); (6) §6 SCHEDULER-WINDOW SATURATION (LCSCH-1 through LCSCH-4) — drift idempotency; multi-domain orphan detection; attribution continuity under multi-domain saturation; heartbeat continues in MAINTENANCE; (7) §7 RETRY-STORM GOVERNANCE (LCRSG-1 through LCRSG-4) — Config V2 threshold protection at scale (replaces V1 §6.2 "Retry Cycle Load"); exhaustion storm escalation candidacy; retry budget protection during outage storms (replaces V1 §7.1 "API Rate Limits"); window enforcement under rapid re-trigger; (8) §8 DUPLICATE ORCHESTRATION STORMS (LCDO-1 through LCDO-3) — webhook storm suppression (replaces V1 §7.2 "Webhook Flood"); concurrent finalization storm exactly-one-FINALIZED; bulk replay idempotency; (9) §9 PROVIDER SYNCHRONIZATION BACKLOG (LCPB-1 through LCPB-3) — sync backlog non-blocking at scale; provider outage outbound suppression with inbound continuing; AUTH_FAILURE terminal under load; (10) §10 AI INFERENCE BACKLOG (LCAI-1 through LCAI-4) — AI backlog: rule-based continues (replaces V1 §8.1 "High Volume Transcripts"); FINALIZED_COPY immutability under bulk AI refresh; STALE AI governance at scale; AI lineage drift prevention; (11) §11 ATTRIBUTION CONTINUITY UNDER STRESS (LCAT-1 through LCAT-4) — zero null correlation_id at scale (replaces V1 §10.2 "Data Growth"); causation_id linkage under bulk retry; recovery attribution under multi-domain load; scheduler attribution under saturation; (12) §12 DEGRADATION ESCALATION UNDER SATURATION (LCDE-1 through LCDE-4) — multi-domain outage storm; cascading observability saturation; simultaneous failure classes; degraded replay under load; (13) §13 OBSERVABILITY SATURATION GOVERNANCE (LCOS-1 through LCOS-2) — structured log completeness under high volume; heartbeat attribution under all modes; (14) §14 GOVERNANCE INVARIANT LOAD TESTS (LCI-INV1 through LCI-INV7, LCI-FAD1 through LCI-FAD3) — all 7 governance invariants and all 3 FAD-applicable invariants validated under saturation: FINALIZED snapshot inviolable under concurrent mutations; exactly-one-ACTIVE under concurrent readers; prospective-only activation under concurrent replay; zero LIVE effects from bulk replay; zero null correlation_id across large population; FINALIZED_COPY immutable under bulk AI sweep; zero null config_version_id in mid-transition batch; bulk finalization exact row count; regeneration zero external calls under concurrent pressure; in-progress replay not contaminated by concurrent config activation; (15) §15 ACCEPTANCE CRITERIA — AC-LOAD-1 through AC-LOAD-10 governance load-certification criteria (governance conformance under saturation, replay-safe under load, scheduler under saturation, deterministic degradation under saturation, immutable lineage under stress, attribution continuity under load, provider-boundary preservation under load, AI governance preservation under load, idempotent orchestration under concurrency, governance invariant preservation under saturation); (16) §16 SCOPE-SPECIFIC LOAD COVERAGE — MVP/STANDARD/PRODUCTION by scenario ID cross-reference; (17) §17 REFERENCES — complete governance reference list covering both parent contracts, all runtime contracts, all resilience contracts, all directive contracts, all specification contracts, architecture decisions, and all AP rules.
  - Verification: file written; V1 "50 concurrent executions" removed — replaced with Config V2 Group A concurrency variable resolution in LCS-1 and LCS-2; V1 "system throttles to 50 concurrent" removed — replaced with LCSCH-1 drift idempotency and Config V2 dynamic governance; V1 "batch processing applied" removed — replaced with LCS-1 governance assertion requiring `config_version_id` and `correlation_id` on every record; V1 "retry cycle runs / no overload" removed — replaced with LCRSG-1 through LCRSG-4 Config V2-governed retry-storm governance; V1 "1,000 eligible students" hardcoded count removed — replaced with Config V2 Group A batch-size variable governance in LCI-INV5; V1 "events queued / processed eventually" removed — replaced with LCDO-1 webhook storm suppression with `DUPLICATE_EVENT_SUPPRESSED` attribution; V1 "LLM requests throttled / no timeouts" removed — replaced with LCAI-1 rule-based orchestration continues under AI backlog; V1 "load distributed / no duplicate execution" removed — replaced with LCS-2 concurrent duplicate prevention with `DUPLICATE_ORCHESTRATION_SUPPRESSED` audit records; V1 "performance maintained" removed — replaced with LCAT-1 zero null `correlation_id` governance load assertion; V1 §11 "MVP = minimal load testing / small dataset validation" removed — replaced with §16 scope-specific coverage by governance scenario ID. Final audit: (1) no hardcoded concurrency semantics — §2 prohibited table names "50 concurrent executions" and "system throttles to 50 concurrent"; LCS-1 asserts "resolved from Config V2 Group A — no hardcoded limit"; LCI-INV5 references "Config V2 Group A batch-size variable" — PASS; (2) no direct execution assumptions — §2 prohibited table names "events queued / processed eventually"; all load scenarios validate governance records (OrchestrationIntent, processed_events) not execution effects — PASS; (3) no replay violations — LCR-1 asserts "Zero LIVE outbound dispatches" from bulk replay; LCR-3 asserts namespace isolation; LCI-INV4 verifies `ghl_messages` and `state_transition_log` zero records for replay execution_type — PASS; (4) no broken lineage guarantees — LCAT-1 asserts zero null correlation_id; LCAT-2 asserts zero null causation_id for RETRY_OUTREACH; LCI-INV5 provides table-level verification queries — PASS; (5) no governance invariant violations — LCI-INV1 through LCI-INV7 and LCI-FAD1 through LCI-FAD3 validate all invariants under saturation; each defines explicit governance verification query — PASS; (6) no silent fallback assumptions — §2 prohibited table names "no crashes" and "no failure cascade"; LCDE-1 asserts "independent observable events per domain"; LCDE-3 asserts "stable `error_class` strings; no generic `Error`" — PASS; (7) no mutable FINALIZED_COPY assumptions — LCI-INV1 asserts "content checksums unchanged"; LCAI-2 asserts "row_count and content_checksums identical"; §2 names "FINALIZED_COPY mutated under bulk load" as a prohibited pattern — PASS; (8) no implementation code generated — file contains governance load certification scenarios structured as Given/When/Then + Prohibited assertions — PASS; (9) no load-testing scripts — no pytest implementation code, no locust/k6/jmeter scripts, no queue worker code — PASS; (10) acceptance criteria are governance load-certification criteria — AC-LOAD-1 through AC-LOAD-10 reference scenario IDs, not test method names — PASS.
  - Notes: Governance-load-certification implications — tests/load_tests.md is now the third canonical document in the governance validation contract architecture alongside tests/test_scenarios.md (parent) and tests/edge_cases.md (edge-condition parent); the 10 governance-oriented load certification domains extend the parent contracts' governance invariants into saturation, concurrent-storm, and high-volume territory; the §3 domain table names the governance boundaries stressed under load and the parent scenario references, making the document inheritance-safe. Replay-certification implications — LCR-1 through LCR-4 provide replay-safe load certification for four volume conditions: bulk replay zero LIVE effects (with table-level verification queries), concurrent duplicate replay triggers, replay-LIVE namespace isolation under saturation, and bulk regeneration zero external calls; the replay-LIVE isolation guarantee (AP-RT2) is explicitly stated as load-invariant — it holds for one record and for 10,000 records identically. Degradation-under-load implications — LCDE-1 (multi-domain outage storm), LCDE-2 (cascading observability saturation), LCDE-3 (simultaneous failure classes at scale), and LCDE-4 (degraded replay under load) extend the parent contracts' single-domain degradation scenarios into concurrent-volume degradation scenarios; every domain maintains the governance invariants that degradation must remain deterministic, observable, and never silently swallowed at any batch size or concurrent storm magnitude. PROGRESS.md audit: 2 files changed (tests/load_tests.md, PROGRESS.md), 2 entries, audit clean.

---

- [x] failure/failure_playbook.md — rewritten as governance-safe resilience orchestration architecture
  - Date: 2026-05-26
  - What changed: `failure/failure_playbook.md` completely rewritten from 448-line V1 MVP-era operational recovery documentation to ~950-line governance-safe resilience orchestration architecture. V1 file contained: simplistic four-category failure model (External/Internal/Data/Concurrency); hardcoded retry matrix with fixed counts ("GHL Trigger: 1–2", "LLM Call: 1", "Scheduler: 1"); "Retry immediately (once)" as a first-class recovery instruction (§5.1, §5.2); "Trigger recovery execution immediately" as a scheduler recovery action (§5.4); three-tier scope behavior (MVP/STANDARD/PRODUCTION); generic 5–7 field log lists; no runtime mode semantics; no Config V2 governance; no replay-safe recovery semantics; no attribution continuity requirements; no governance authority boundaries. New file is now the canonical parent resilience contract for `failure/scheduling_failures.md`, `failure/ghl_failures.md`, and `failure/llm_failures.md`. Key structural changes: (1) PURPOSE reframed — §1.1 defines canonical parent resilience contract; §1.2 explicitly prohibits: "Retry immediately once" semantics (AP-FP3), "Trigger recovery execution" direct execution (AP-FP4), hardcoded retry count substitution (AP-FP5), non-attributable recovery actions (AP-FP6), immutable record mutation (AP-FP7), governance bypass during recovery (AP-FP8); §1.3 governance layer definition with 6-row layer/relationship table referencing all 4 runtime contracts and all directive/provider governance layers; (2) FAILURE GOVERNANCE DOMAINS (§2) — 10 domain definitions each with governance-scope/recovery-philosophy/replay-behavior/SHADOW-behavior/LIVE-behavior/observability-requirements/attribution-requirements/idempotency-guarantees: Orchestration Failures (§2.1), Provider Failures (§2.2 — replaces "GHL External Failures"), AI Failures (§2.3 — replaces "LLM Invalid Output"), Scheduler Failures (§2.4 — replaces "Scheduler Miss"), Replay Failures (§2.5 — new), Regeneration Failures (§2.6 — new), Synchronization Failures (§2.7 — new), Observability Failures (§2.8 — new), Attribution Failures (§2.9 — new), Compliance Execution Failures (§2.10 — new); each domain inherits explicit prohibited patterns from AP-FP rules; (3) FAILURE AUTHORITY BOUNDARIES (§3) — §3.1 authority table with "does NOT own" column; §3.2 provider authority restrictions (providers remain platform_supplementary during failures); §3.3 AI authority restrictions (advisory-only in all failure contexts); §3.4 replay authority restrictions (no LIVE authority for replay-mode recovery); (4) DETERMINISTIC DEGRADATION GOVERNANCE (§4) — §4.1 degradation principles inherited verbatim from system_loop.md §10.1; §4.2–§4.7 per-condition behavior (provider outage, AI outage, scheduler outage, sync lag, config threshold missing, duplicate event storm); replaces generic "fallback logic" with named degradation behavior; (5) REPLAY & REGENERATION FAILURE SEMANTICS (§5) — §5.1 replay failure governance; §5.2 replay recovery restrictions (5-row suppression table); §5.3 historical recovery visibility; §5.4 regeneration failure semantics (distinct from replay); (6) FAILURE STATE GOVERNANCE (§6) — §6.1 state authority during failures (7-row table per scenario — resilience layer role for every failure type); §6.2 immutable record protections during recovery (5 protected record types); §6.3 eligibility and retry policy during failures; (7) RECOVERY ORCHESTRATION COORDINATION (§7) — §7.1 governed recovery coordination model — V1 "Trigger recovery execution immediately" and "Process missed workload" explicitly replaced with `RecoveryOrchestrationRecord` with `execution_type = recovery` + 6-step coordination sequence; §7.2 Config V2-governed recovery thresholds (5-row table: all 5 threshold names, Config V2 source, Group — no hardcoded values); §7.3 dead-letter governance (append-only, full attribution, escalation candidacy); (8) ATTRIBUTION & OBSERVABILITY (§8) — §8.1 failure attribution lineage chain (Config V2 → cycle_id → originating intent → failure event → recovery record → escalation candidacy); §8.2 causation propagation in recovery; §8.3 8-field replay diagnostics; §8.4 recovery audit continuity (append-only, actor_identity, student_timeline_events); (9) FAILURE IDEMPOTENCY GUARANTEES (§9) — §9.1 core idempotency principle inherited from spec/04 §2.1; §9.2 6-row domain-specific idempotency key table; §9.3 idempotency violations are production defects (4 concrete defect patterns named); (10) DETERMINISTIC DEGRADATION CONDITION TABLE (§10) — §10.1 13-row canonical degradation condition table (inherited and extended from system_loop.md §10.2 with resilience-layer annotations; adds retry starvation and AI advisory absent in DEGRADED retry as resilience-layer rows); (11) RESILIENCE OBSERVABILITY SCHEMA (§11) — §11.1 structured failure log record (22-field JSON schema including `failure_domain`, `error_class`, `recovery_action`, `recovery_of_cycle_date`, `degradation_flags`, `idempotency_key`); §11.2 recovery cycle completion record (11-field JSON with recovery-attribution fields); (12) AP-FP1 through AP-FP15 architecture preservation rules (prohibitions: direct provider execution recovery, replay-triggered LIVE recovery, "retry immediately" semantics, silent fallback execution, hardcoded recovery thresholds, non-attributable recovery actions, immutable record mutation, governance bypass, AI authority escalation, directive re-implementation, duplicate recovery cycles, retry budget consumption on provider outage, PII in logs, silent error swallowing, orphaned recovery execution); (13) AC-FP1 through AC-FP12 governance-focused acceptance criteria (covering: deterministic resilience behavior, replay-safe recovery, immutable lineage preservation, degraded-mode safety, attribution continuity, provider failure containment, idempotent recovery orchestration, Config V2 governed thresholds, AI advisory absence governance, no hardcoded retry semantics, scheduler recovery attribution, failure observability completeness); (14) UE-FP1 through UE-FP5 unresolved governance concerns (recovery cycle escalation threshold, dead-letter retention window, retry budget protection scope — references retry_cycle_flow.md UE-RF4, observability degradation escalation threshold, compliance hold recovery automation); (15) §15 downstream references (§15.1 authoritative parent contracts including all 4 runtime contracts; §15.2 child contracts governed by this document; §15.3 required child contract V1 violations to remove in scheduling_failures.md, ghl_failures.md, llm_failures.md; §15.4 directive dependencies).
  - Verification: file written; V1 "Retry immediately (once)" instructions removed from all failure handling scenarios and replaced with AP-FP3-governed semantics; V1 "Trigger recovery execution" removed and replaced with governed recovery orchestration coordination (§7.1); V1 hardcoded retry matrix removed and replaced with Config V2-governed recovery thresholds (§7.2); V1 MVP/STANDARD/PRODUCTION scope tiers removed and replaced with execution_mode governance (LIVE/SHADOW/REPLAY/DEGRADED/MAINTENANCE); V1 four-category failure model replaced with 10 failure governance domains; V1 5-field log requirements replaced with 22-field structured JSON observability schema. 7-point final audit: (1) no hardcoded retry behavior — AP-FP3 prohibits "retry once" semantics, AP-FP5 prohibits hardcoded count substitution, §7.2 Config V2 threshold table names all 5 governed variables with no literal values, AC-FP10 test — PASS; (2) no hidden recovery execution — AP-FP4 explicit prohibition, §7.1 governed recovery coordination model with 6-step attribution-gated sequence, AC-FP11 recovery attribution test — PASS; (3) no replay violations — AP-FP2 LIVE prohibition in replay recovery, §5.2 replay suppression table, §3.4 replay authority restrictions, AC-FP2 test — PASS; (4) no broken lineage guarantees — §8.1 failure attribution lineage chain, AP-FP6 non-attributable recovery prohibition, §8.2 causation propagation in recovery, AC-FP5 attribution continuity test — PASS; (5) no governance invariant violations — AP-FP7 immutable record prohibition, AP-FP8 governance bypass prohibition, AP-FP10 directive re-implementation prohibition, §6.2 immutable record protections table, AC-FP3 lineage preservation test — PASS; (6) no silent degradation behavior — AP-FP4 silent fallback prohibition, AP-FP14 silent error swallow prohibition, §4 all degradation conditions named and observable, §10 13-row degradation table with explicit observability column — PASS; (7) no duplicate LIVE recovery execution semantics — AP-FP11 duplicate recovery cycle prohibition, §9.2 domain-specific idempotency key table, AC-FP7 idempotent recovery test — PASS.
  - Notes: Resilience-governance implications — failure/failure_playbook.md is now the canonical parent resilience contract for all three child failure documents; the 10 failure governance domains extend the runtime layer's degradation model from the orchestration layer (system_loop.md §10) to the explicit failure-containment boundary, adding replay failures, regeneration failures, synchronization failures, observability failures, attribution failures, and compliance execution failures as first-class failure governance categories not present in V1. The §15.3 required child contract updates table documents V1 violations in scheduling_failures.md (§4.1 "trigger recovery execution immediately", §10 MVP/STANDARD/PRODUCTION tiers), ghl_failures.md (§4.1 "retry immediately (once)", §5 hardcoded timeout table, §6 fixed retry matrix, §9 scope tiers), and llm_failures.md (§4.1 "retry LLM call (once)", §5 fixed retry table, §6 undocumented fallback, §9 scope tiers) — these are downstream governance tasks, not blocking this phase. Replay implications — §5 (Replay & Regeneration Failure Semantics) establishes that replay-mode failure simulation is distinct from live failure handling: replay reads historical failure records from state_transition_log, simulates historical failure scenarios, produces fingerprint comparison diagnostics, and never generates LIVE recovery effects; this closes the replay coverage gap for the failure layer analogous to what §7 does for retry_cycle_flow.md and daily_outreach_flow.md. Degradation governance implications — the 13-row degradation condition table (§10.1) extends system_loop.md §10.2 with two resilience-layer-specific rows (retry starvation, AI advisory absent in DEGRADED retry) making all degradation conditions visible at the failure governance boundary; every row specifies whether orchestration continues, what observability record is emitted, and what resilience escalation action is taken — no row allows silent degradation. Provider-governance implications — §2.2 provider failure domain introduces retry budget protection (AP-FP12) as a first-class concern: attempts blocked by provider outage are not consumed from the student retry budget; §3.2 explicitly states providers remain platform_supplementary during failures (failures do not elevate provider authority) — this closes the authority-escalation-during-failure gap that existed implicitly in the V1 retry matrix. PROGRESS.md audit: 2 files changed (failure/failure_playbook.md, PROGRESS.md), 2 entries, audit clean.



